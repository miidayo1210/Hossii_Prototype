# 101 管理者発行参加者アカウント

> **分類:** `[Core / Admin]` スペース参加者の事前発行ログイン
> **ステータス:** ✅ Production反映済み（2026-07-21）。⚠️ 管理画面実画面・最小発行 smoke のみ保留
> **最終更新:** 2026-07-21（50スロット・一括発行・Production 反映）
> **関連:**
> - [123_URLからスペース参加UI.md](./docs/123_URLからスペース参加UI.md) — `ParticipantLoginScreen` / 共有URL参加導線の正本
> - [41_個人ユーザー登録](./41_個人ユーザー登録.md) — 将来の一般向け自己登録（本仕様では **未開放**）
> - [43_ゲスト参加の人がアカウントからログインする](./43_ゲスト参加の人がアカウントからログインする.md)
> - [16_ゲスト入室フロー](./16_ゲスト入室フロー.md)
> - [02_匿名利用機能](./02_匿名利用機能.md)
> - [10_ログインユーザー管理設計](./10_ログインユーザー管理設計.md)
> - [83_ログ一覧の私のログ切替](./83_ログ一覧の私のログ切替.md)
> - [27_User別.md](./27_User別.md)

> **関連コード（現状）:**
> `src/core/contexts/AuthContext.tsx`
> `src/core/utils/userProfilesApi.ts` / `profilesApi.ts`
> `src/core/utils/profileStorage.ts`
> `src/components/Auth/GuestEntryScreen.tsx`
> `src/components/Auth/ParticipantLoginScreen.tsx`
> `src/components/AccountScreen/AccountScreen.tsx`
> `src/App.tsx`
> `src/components/SpaceSettingsScreen/*`
> `src/core/hooks/HossiiStoreProvider.tsx`

---

## 0. 要約（変更差分の見積もり）

**結論: 差分は中〜小規模で実現可能。**

既存の Supabase Auth・`LoginScreen`・`GuestEntryScreen`・`user_profiles`・`space_nicknames` を活かし、**新規テーブル 1 本 + Edge Function 1 本 + 管理 UI 1 タブ + 参加者ログイン導線の有効化 + `authorId` を Auth UID に寄せる 1 箇所** が中心になる。

| レイヤ | 新規 | 既存活用 | 備考 |
|--------|------|----------|------|
| DB | `space_participant_accounts` | `user_profiles`, `space_nicknames`, `auth.users` | 発行スロット管理のみ追加 |
| 認証 | Edge Function（発行時のみ service role） | `AuthContext.login()` / セッション永続化 | 参加者ログインは既存 API |
| 管理 UI | SpaceSettings に「参加者アカウント」タブ | 既存タブ・保存 UX | 最大 **50** スロット・一括発行（§8.5） |
| 参加 UI | 初回ニックネームモーダル（簡易版） | `GuestEntryScreen` / `LoginScreen` | Coming soon 解除 |
| 本人識別 | ログイン時 `profile.id = auth.uid()` | 投稿 `authorId` パイプライン | **端末横断の要** |

**スコープ外（本フェーズ）:** 一般向け自己登録、OAuth、ゲスト投稿のアカウント引き継ぎ、パスワード変更 UI。

---

## 1. 背景・目的

### 1.1 現状

| 参加方法 | 状態 | 本人識別 |
|----------|------|----------|
| ゲスト（ニックネームのみ） | ✅ 利用可 | 端末ローカル `hossii.profile.id` |
| 管理者発行アカウントでログイン | ✅ 利用可 | `auth.uid()` |
| 新規会員登録（自己登録） | UI は **Coming soon** | [41](./41_個人ユーザー登録.md) で将来想定 |

ゲストは同じブラウザなら再入室が楽だが、**端末を変えると別人** になる（[02](./02_匿名利用機能.md)）。

### 1.2 目標

ワークショップ・内省スペースなど **限定人数** の場で、管理者が事前に **参加 ID とパスワードを発行** し、参加者がそれでログインして記録を残せるようにする。

> **初版（MVP）:** 最大 **20** スロット。現行は §8.5（最大 **50** スロット・一括発行）。

| 要件 | 内容 |
|------|------|
| 自己登録 | **まだ不可**（LoginScreen の新規登録タブ・GuestEntryScreen の登録導線は非表示のまま） |
| 管理者発行 | スペースごとに最大 **50** スロットの ID / パスワードを発行（初版は 20） |
| 初回ログイン | **ニックネーム登録のみ**（メール・生年月日等は不要） |
| 同一ブラウザ | Supabase セッションにより **そのまま再ログイン**（GuestEntryScreen をスキップ） |
| 別端末 | 「アカウントでログイン」→ 同じ ID / パスワード → **同一人物** として扱う |
| 記録 | ログイン後の投稿は `authorId = auth.uid()` でサーバーに紐づく。「私のログ」が端末横断で一致 |

---

## 2. ユーザー種別の整理

本仕様で追加する第 4 種別（既存 3 種別は [27_User別.md](./27_User別.md) 参照）。

| 種別 | 識別 | 登録経路 | ログイン |
|------|------|----------|----------|
| スーパー管理者 | `isSuperAdmin` | 運営手動 | `/admin/login` |
| コミュニティ管理者 | `isAdmin` | コミュニティ申請 | `/admin/login` |
| **発行参加者**（本仕様） | `auth.uid()` + `space_participant_accounts` | **管理者がスペース設定から発行** | スペース参加画面 |
| ゲスト | 端末ローカル ID | ニックネームのみ | 不要 |
| 一般ユーザー（将来） | `auth.uid()` | 自己登録 [41] | LoginScreen |

> 発行参加者は `app_metadata.role` を付与しない（管理者権限なし）。`isAdmin: false` の一般参加者として扱う。

---

## 3. 管理者側フロー

### 3.1 入口

**スペース設定画面（`SpaceSettingsScreen`）** に新タブ **「参加者アカウント」** を追加する。

- 表示条件: コミュニティ管理者（`isAdmin: true`）のみ
- 対象: **アクティブな 1 スペース** につき最大 **50** スロット（初版 UI モックは 20 件表記）

> **共有URL参加 UI:** 参加者側ログイン導線の詳細は [123](./docs/123_URLからスペース参加UI.md) を正とする。

### 3.2 発行 UI

> **初版 UI モック（20 件表記）。** 現行は §8.5（最大 50 件・一括発行 UI）。

```
┌─ 参加者アカウント ─────────────────────────────┐
│ このスペース専用のログイン ID を最大 20 件まで     │
│ 発行できます。参加者に ID とパスワードを共有       │
│ してください。                                   │
│                                                  │
│ 発行済み: 3 / 20                                 │
│                                                  │
│ ┌────┬──────────┬────────────┬────────────────┐ │
│ │ #  │ 参加 ID   │ 状態       │ 操作           │ │
│ ├────┼──────────┼────────────┼────────────────┤ │
│ │ 1  │ ws-01    │ 発行済み   │ [再表示][無効化]│ │
│ │ 2  │ ws-02    │ 初回未使用 │ [再表示][無効化]│ │
│ │ 3  │ —        │ 未発行     │ [発行]          │ │
│ │ …  │          │            │                │ │
│ └────┴──────────┴────────────┴────────────────┘ │
└──────────────────────────────────────────────────┘
```

| 操作 | 動作 |
|------|------|
| **発行** | 空きスロットに参加 ID・初期パスワードを生成。モーダルで **一度だけ** 平文パスワードを表示（コピーボタン付き） |
| **再表示** | 参加 ID のみ表示。**パスワードは再表示不可**（セキュリティ）。必要なら **パスワード再発行** |
| **パスワード再発行** | 新パスワードを生成し、旧パスワードは無効化。初回ログイン済みかどうかは維持 |
| **無効化** | スロットを「未発行」に戻す。紐づく Auth ユーザーは `banned` または論理削除。既存投稿は保持 |

### 3.3 参加 ID・パスワードの形式

| 項目 | 仕様 |
|------|------|
| 参加 ID | スペース内一意。例: `{spaceSlug の短縮}-01` 〜 `-20`、またはランダム 6 桁英数字。管理者向け UI では **人間が読みやすい連番** をデフォルトとする |
| 初期パスワード | ランダム 8 文字（英大小 + 数字）。発行時のみ平文表示 |
| 上限 | **1 スペースあたり 50 スロット**（初版は 20 固定。migration `20260721130000` で拡張） |
| Auth 内部識別子 | 参加者本人のメールアドレスは不要。Supabase Auth 内部でのみ使うシステム生成識別子（例: `{space_id}.{login_id}@participants.internal`）を持ってよい。**利用者へ表示しない**。通知用メールアドレスとは別物。将来メール通知を追加する場合も、参加 ID 認証用識別子とは別に管理する |

### 3.4 発行 API（Edge Function）

クライアントから service role で Auth ユーザーを作れないため、**Edge Function `issue-participant-account`** を用意する。

```
POST /functions/v1/issue-participant-account
Authorization: Bearer {管理者の access token}

Body: { spaceId, slotNumber?, regeneratePassword?, revoke? }
```

| チェック | 内容 |
|----------|------|
| 呼び出し元 | JWT の `app_metadata.role === 'admin'` かつ対象 `spaceId` が自コミュニティ配下 |
| 副作用 | `auth.admin.createUser` → `space_participant_accounts` INSERT/UPDATE |
| 返却 | `{ loginId, password }`（再発行・新規発行時のみ password を含む） |

---

## 4. 参加者側フロー

### 4.1 全体像

```
/s/[slug] にアクセス
  │
  ├─ Supabase セッションあり（発行参加者）
  │     │
  │     ├─ ニックネーム未設定 → 初回ニックネームモーダル → SpaceScreen
  │     └─ ニックネーム済み → GuestEntryScreen スキップ → SpaceScreen
  │
  └─ 未ログイン
        │
        ↓
  GuestEntryScreen
        │
        ├─「ゲストとして参加」→ 従来どおり（本仕様で変更なし）
        │
        └─「アカウントでログイン」→ ParticipantLoginScreen
              │
              ├─ 参加 ID + パスワード入力
              ├─ 認証成功
              ├─ 初回のみ → ニックネーム入力（1 項目のみ）
              └─ SpaceScreen へ入室

ゲスト入室後（スペース内）
  │
  └─ アカウントタブ →「アカウントでログイン」→ ParticipantLoginScreen
        （新規会員登録ボタンは Coming soon のまま無効）
```

### 4.2 GuestEntryScreen の変更

| 項目 | 変更 |
|------|------|
| 「アカウントでログイン」 | **有効化**（Coming soon バッジ削除） |
| 「新規登録の方はこちら」 | **非表示のまま**（自己登録は未開放） |
| ログイン済み参加者 | セッション検知時は GuestEntryScreen を **スキップ**（[16](./16_ゲスト入室フロー.md) のゲスト自動入室と同様の分岐を Auth 側に追加） |

### 4.2.1 App.tsx のルーティング優先順位

`pendingParticipantSpaceId` がセットされたとき **ParticipantLoginScreen を GuestEntryScreen より先に表示** する。
GuestEntryScreen の表示条件と pendingParticipantSpaceId が同時に真になると、ログインボタンを押しても画面が切り替わらない不具合が起きるため、本順序を仕様として固定する。

| 順序 | 条件 | 表示 |
|------|------|------|
| 1 | 未ログイン かつ pendingParticipantSpaceId あり | ParticipantLoginScreen |
| 2 | 未ログイン かつ guestSpaceId あり かつ ゲスト入室前 | GuestEntryScreen |
| 3 | 未ログイン かつ pendingLoginSlug あり | LoginScreen（汎用・自己登録用。本機能では未使用） |

ゲストモード中（`isGuestMode === true`）でも、pendingParticipantSpaceId がセットされれば ParticipantLoginScreen を全画面表示する。

### 4.3 参加者ログイン画面

**ParticipantLoginScreen** を新設済み。汎用 LoginScreen は使わない。

**入力項目（ログインのみ）:**

| 項目 | 必須 |
|------|------|
| 参加 ID | ✅ |
| パスワード | ✅ |

- メールアドレス欄は **出さない**
- Google / Facebook は **出さない**
- 新規登録タブは **出さない**
- スペース文脈（`spaceId`）は URL / 参加フローから自動で渡す（参加者が space を選ばない）

**認証処理:**

```
loginParticipant(spaceId, loginId, password)
  │
  ├─ resolve_participant_login(spaceId, loginId)
  │     → 発行時に保存した Auth 内部識別子（auth_email）を返す
  │     → auth_user_id は返さない / 利用者本人のメールは要求・取得しない
  │     → login_id は lower(trim(...)) で正規化 / active のみ / space_id 単位
  │     → ログイン前の can_access_space ゲートはかけない
  │       （public / private / invite_only いずれも発行済み参加 ID でログイン可）
  ├─ supabase.auth.signInWithPassword({ email: 内部識別子, password })
  └─ resolveAppUser() → isAdmin: false, uid 確定
```

**メールアドレス方針（参加 ID ログイン）:**

- 参加 ID ログインに **利用者本人のメールアドレスは不要**
- Supabase Auth 内部では、システム生成の認証用識別子（`auth_email`）を使う場合がある
- その識別子は **利用者へ表示しない**（UI・エラーメッセージ・不要なログに出さない）
- **通知用メールアドレスとは別物**。外部メール送信にも使わない
- 将来メール通知を追加する場合も、参加 ID 認証用識別子とは **別に管理** する

### 4.4 初回ニックネーム登録（参加 ID 正本）

参加 ID アカウント（`app_metadata.participant === true` / `isIssuedParticipant`）の初回名前登録判定は、**発行元スペースのスペース内ニックネーム**を正本とする。通常のメールアカウント等のアカウント共通名・既存 `hasNicknameForSpace` 判定とは **別判定** である。

```text
参加IDログイン成功
→ 発行元 space_id（ログイン対象 / issuedParticipantScope）
→ space_nicknames（当該 space）の有無を確認
→ 未登録 → NicknameModal（既存 UI 再利用）→ 保存成功後に発行元スペースへ
→ 登録済み → モーダルをスキップ → 発行元スペースへ
```

| 項目 | 仕様 |
|------|------|
| 判定の正本 | 発行元 `space_id` に対する `space_nicknames`（クライアント state / `fetchSpaceNicknames`） |
| 使わないもの | `user_profiles.username`・`profiles.default_nickname`・表示 fallback・placeholder「ユーザー」単独では **登録済みとみなさない** |
| 表示条件 | ログイン成功後、発行元スペースの space nickname が未登録 |
| 入力 | ニックネーム **1 項目のみ**。前後 trim。空白のみ不可。文字数・制御文字は既存 `normalizeSpaceNickname`（最大 50）に従う |
| 保存先 | **正本:** `space_nicknames`（`profile_id = auth.uid()`）。可能なら `update_my_space_nickname` で `space_memberships.space_nickname` も同期（投稿者現在名 RPC 用）。`user_profiles.username` へのミラーは **必須にしない** |
| 再表示 | 発行元 space nick 登録済みなら **再入力不要**（`username='ユーザー'` が残っていてもスキップ） |
| 保存失敗 | 入力を保持し、スペースへ進まない |
| scope / space 不明 | 発行元以外の space へ推測保存しない。ログイン対象 `spaceId` のみに保存 |
| UI | 既存 `NicknameModal`（`variant=guest`）。参加 ID 用の新規画面は作らない |

#### 「ユーザー」placeholder の扱い

- `ensureUserProfileExists` が `user_profiles.username='ユーザー'` を入れる場合がある（DB `NOT NULL` 制約のため、既存行の一括 cleanup はしない）
- 参加 ID の初回判定・Account 表示・membership auto-join では「ユーザー」を正式名として扱わない
- 名前登録画面を出せる場合、入力欄の初期値にも「ユーザー」を載せない

#### 表示名の優先順位（参加 ID）

1. 発行元スペースの `space_nicknames` / 同期済み `space_memberships.space_nickname`
2. （任意）非 placeholder のプロフィール名
3. placeholder「ユーザー」や未登録時は正式名として表示しない（登録モーダルを優先）

#### NicknameModal 初期値（参加 ID）

- 発行元 space に保存済み nickname がある場合のみ、その値を初期入力に使う
- 未登録時は **空欄**
- `user_profiles.username` / `profiles.default_nickname` / `user_metadata.display_name` / 「ユーザー」/ 発行時の機械生成表示名は **prefill に使わない**

#### 判定タイミング（hydrate）

参加 ID の登録済み判定は、次をすべて満たしてから行う。

```text
発行元 scope の解決完了
AND 対象 spaceId の確定
AND space_nicknames の hydrate 完了
```

hydrate / scope 未完了のあいだは「未登録」とみなして NicknameModal を **表示しない**（`/s/[slug]` 経路・参加 ID ログイン pending 経路の双方）。

ゲスト参加時の Hossii キャラ・吹き出し UI は [16](./16_ゲスト入室フロー.md) / `GuestEntryScreen` のニックネームステップを **再利用** する。

### 4.4.1 不変条件（参加 ID 初回名前登録）

次を破る変更は、本節と必須回帰テストを **同時更新** しない限り禁止する。

1. 初回登録済み判定の正本は発行元 space の `space_nicknames` のみとする
2. `space_nicknames` hydrate 完了前に未登録判定（NicknameModal 表示）を行わない
3. `user_profiles.username` を登録済み判定に使わない
4. `profiles.default_nickname` を登録済み判定に使わない
5. `user_metadata.display_name` を登録済み判定および初回 NicknameModal の prefill に使わない
6. 「ユーザー」等の placeholder を正式名または登録済み判定に使わない
7. 発行元以外の space nickname を登録済み判定に使わない
8. URL 上の別 space へ nickname を保存しない（登録対象は常に発行元 space）
9. プロフィール・membership・ルーティング機能の変更で、参加 ID 初回登録をスキップさせない（hydrate 前の誤スキップ / 誤表示を含む）
10. 本不変条件を変更する場合は、仕様書 101（本節）と必須回帰テストを同時更新する

### 4.4.2 必須回帰テスト（参加 ID 初回名前登録）

最低限、次を自動化または手動受入で維持する。

| # | ケース | 期待 |
|---|--------|------|
| 1 | hydrate 前 | NicknameModal を表示しない |
| 2 | hydrate 後 + 発行元 nick なし | NicknameModal を表示 |
| 3 | hydrate 後 + 発行元 nick あり | NicknameModal を表示しない |
| 4 | 保存後の再ログイン | NicknameModal を表示しない |
| 5 | `/s/[slug]` 再読み込み（保存済み） | NicknameModal を表示しない |
| 6 | username=`ユーザー` + nick なし | NicknameModal を表示（初期値は空） |
| 7 | display_name あり + nick なし | NicknameModal を表示（初期値は空） |
| 8 | 別 space nick のみ | 発行元については NicknameModal を表示 |
| 9 | 保存先 | 発行元 space の `space_nicknames` |
| 10 | 正本保存失敗 | スペースへ進まない / modal を閉じない |
| 11 | 通常アカウント・ゲスト | 既存の判定・prefill・保存導線を維持 |

### 4.5 2 回目以降の入室

| 状況 | ふるまい |
|------|----------|
| **同じブラウザ** | Supabase がセッションを保持 → アプリ起動 / `/s/[slug]` アクセス時に自動ログイン → GuestEntryScreen スキップ → SpaceScreen |
| **別端末・別ブラウザ** | GuestEntryScreen →「アカウントでログイン」→ 同じ参加 ID / パスワード → 同一 `auth.uid()` → サーバー上のニックネーム・投稿履歴を復元 |
| **ゲスト入室中** | アカウントタブ →「アカウントでログイン」→ ParticipantLoginScreen → 認証後も当該スペースに留まる |
| **ログアウト後** | 再度 ID / パスワード入力が必要（セッション削除） |

---

## 5. 本人識別と投稿の紐づけ

### 5.1 問題（現状）

投稿の `authorId` は **端末ローカル** の `hossii.profile.id` を使用している（[02](./02_匿名利用機能.md)）。Auth でログインしても `authorId` が Auth UID と一致しない。

### 5.2 変更方針（最小差分）

| ユーザー状態 | `profile.id` / `authorId` |
|--------------|---------------------------|
| ゲスト | 従来どおり端末ローカル ID（変更なし） |
| **発行参加者（ログイン済み）** | **`auth.uid()` を使用** |
| ログアウト後ゲスト継続 | ローカル ID にフォールバック（ゲスト投稿と混在しないよう、ログイン中はローカル ID を上書き） |

**変更箇所（想定）:** `HossiiStoreProvider` で `currentUser` が存在し `isAdmin === false` のとき、初回プロフィール生成・投稿時の `authorId` に `currentUser.uid` を使う。

### 5.3 「私のログ」

[83](./83_ログ一覧の私のログ切替.md) の `authorId === profile.id` フィルタが、発行参加者では **端末横断で一致** するようになる。

---

## 6. データベース設計

### 6.1 新規テーブル `space_participant_accounts`

| カラム | 型 | 説明 |
|--------|-----|------|
| `id` | uuid | PK |
| `space_id` | uuid | FK → `spaces.id` ON DELETE CASCADE |
| `slot_number` | int | 1〜**50**（初版は 1〜20）。`(space_id, slot_number)` UNIQUE |
| `login_id` | text | スペース内表示用 ID。`(space_id, login_id)` UNIQUE |
| `auth_user_id` | uuid | FK → `auth.users.id`。発行時に作成 |
| `auth_email` | text | Auth 用のシステム生成内部識別子（UNIQUE）。本人メールではない。UI・通知には使わない |
| `status` | text | `'active'` / `'revoked'` |
| `first_login_at` | timestamptz | 初回ログイン日時（NULL = 未使用） |
| `issued_at` | timestamptz | 発行日時 |
| `issued_by` | uuid | 発行した管理者の `auth.uid()` |

**制約:**

```sql
CHECK (slot_number BETWEEN 1 AND 50)   -- migration 20260721130000（初版は 1 AND 20）
```

**revoked slot:** DB 行が残るため **occupied** 扱い。再利用はしない（空き slot 計算は active ではなく行の有無）。

### 6.2 既存テーブルの利用

| テーブル | 用途 |
|----------|------|
| `auth.users` | 参加者の認証主体。`app_metadata.participant = true`（任意） |
| `user_profiles` | 任意。初回ニックネームを `username` にもミラー可 |
| `space_nicknames` | スペース別表示名（`profile_id = auth.uid()`） |
| `hossiis.author_id` | 投稿本人識別（`auth.uid()` が入る） |

### 6.3 RLS

| テーブル | ポリシー |
|----------|----------|
| `space_participant_accounts` | **管理者**: 自コミュニティ配下スペースのみ SELECT。INSERT/UPDATE は Edge Function（service role）経由のみ |
| `space_participant_accounts` | **参加者**: SELECT 不可（ログイン ID 解決は Edge Function または security definer RPC） |
| `space_nicknames` / `hossiis` | 既存ポリシーを踏襲 |

**ログイン ID 解決:** 参加者が `login_id` だけ入力するため、**スペース ID + login_id → auth_email（Auth 内部識別子）** の lookup は RPC（`resolve_participant_login`）で行う。`auth_user_id` は返さない。未ログインでも解決可能とし、`can_access_space` でゲートしない。クライアントは返却値を Auth 署名込みにのみ使い、UI には表示しない。他スロットの情報を漏らさない。

### 6.4 Account 所属表示（参加 ID・発行元限定）

参加 ID アカウント（`app_metadata.participant === true`）の Account 画面における所属表示の正本は次とする。

```text
auth.uid()
→ get_my_issued_participant_scope()（SECURITY DEFINER）
→ space_participant_accounts（status = active・本人のみ）
→ 発行元 space / community（表示用最小列）
```

| 表示 | 内容 |
|------|------|
| 所属コミュニティ | 発行元スペースが属するコミュニティ **1 件のみ**（切替 UI なし） |
| 参加しているスペース | 発行元スペース **1 件のみ** |
| コミュニティ内のマイスペース | 発行元スペース **1 件のみ**（発行元 scope を正本とする） |

- 発行元 scope は本人限定の `SECURITY DEFINER` RPC（`get_my_issued_participant_scope`）で解決する。`auth.uid()` に対応する **active** な `space_participant_accounts` 行だけを対象とし、任意の `auth_user_id` 指定による越境取得はできない
- クライアントから `communities` を直接 SELECT しない（RLS 上、参加 ID 本人は読めない）。RPC が発行元 space / community の**表示用最小情報**のみ返す（内部認証メール・参加 ID・Auth UUID・パスワード関連は返さない）
- active 行が 0 件なら空 / not_found、2 件以上なら ambiguous（推測して 1 件を選ばない）。失敗時も **membership 一覧へ fallback しない**
- `community_memberships` / `space_memberships` に他の行が存在しても、**参加 ID の Account 所属表示（所属コミュニティ・参加スペース・コミュニティ内のマイスペース）には使用しない**
- 「コミュニティ内のマイスペース」も発行元 scope を正本とし、`list_my_community_personal_spaces` 等の membership 系一覧は参加 ID 表示に使わない
- 通常アカウントの複数コミュニティ・複数スペース所属とは **別扱い**（通常アカウントは既存の複数表示を維持）
- 既存の過剰 membership の整理、および public スペースへの越境自動 join 制限は **後続課題**（本節の表示限定とは分離）

> **他仕様との関係:** [109](./109_コミュニティ所属個人アカウント・個人スペース・招待制スペース.md) / [115](./docs/115_membership自動整合.md) は membership モデルを扱う。参加 ID の Account 所属**表示**については **本仕様（101）を正本**とする。109/115 の全面整理は後続。

---

## 7. 自己登録を閉じたままにする

| 画面 | 対応 |
|------|------|
| `GuestEntryScreen` | 新規登録リンク・Coming soon 表示を **削除または非表示**（参加者ログインのみ有効化） |
| `LoginScreen`（汎用） | 自己登録タブ・OAuth は **従来どおり非表示 or 未使用** |
| `AccountScreen` | **参加者ログイン**は有効。`handleParticipantLoginRequested` 経由で ParticipantLoginScreen を開く。**新規会員登録**のみ Coming soon で無効 |

---

## 8. 実装フェーズ（推奨順）

| フェーズ | 内容 | 差分目安 |
|----------|------|----------|
| **P1** | migration + Edge Function + RPC（発行・ログイン解決） | 小 |
| **P2** | SpaceSettings「参加者アカウント」タブ | 中 |
| **P3** | GuestEntryScreen ログイン有効化 + ParticipantLogin + 初回ニックネーム | 小〜中 |
| **P4** | `authorId = auth.uid()` 統合 + セッション時 GuestEntry スキップ | **小（重要）** |
| **P5** | 管理画面での無効化・パスワード再発行 | 小 |

P1〜P4 までで **MVP（発行 → ログイン → 投稿 → 別端末で同一人物）** が成立する。

---

## 8.5 現行仕様（50スロット・一括発行）`✅ Production反映済み`

| 項目 | 内容 |
|------|------|
| 上限 | 1 スペース最大 **50** 件 |
| 一括発行 | 発行件数入力 + `issue_bulk`（Edge Function） |
| コピー | 今回発行分を TSV で一括コピー |
| 待機 UX | 発行中メッセージ、5 秒後の長時間待機メッセージ |
| revoked slot | occupied 扱い。**再利用しない** |
| 既存操作 | 単件 `issue` / `regenerate` / `revoke` 維持 |

**Production 反映（2026-07-21）:**

| レイヤ | project ref / SHA | 状態 |
|--------|-------------------|------|
| migration | `wzyoddyvfjkagqpnjejo` / `20260721130000` | ✅ 適用済み |
| Edge Function | `issue-participant-account` | ✅ deploy 済み |
| frontend | PR #31 merge `dc863c1` | ✅ Vercel Production READY |

**未確認（⚠️ 保留）:**

* Production 管理画面の実画面 smoke（PC / 375px）
* Production 専用テストスペース不足のため、最小 1 件発行 smoke 未実施

---

## 9. 受け入れ条件（QA）

| # | シナリオ | 期待結果 |
|---|----------|----------|
| 1 | 管理者がスペースで 3 件発行 | 一覧に 3 件表示。**51 件目（50 超）はエラー**（初版 QA は 21 件目） |
| 2 | 参加者が初回ログイン | ID/パスワード → ニックネーム 1 回 → スペース入室 |
| 2b | GuestEntryScreen からログイン押下 | ParticipantLoginScreen が表示され参加 ID を入力できる |
| 2c | ゲスト入室中にアカウントタブからログイン | 同上 |
| 3 | 同ブラウザで再アクセス | GuestEntry を経ずにスペース表示 |
| 4 | 別端末で同 ID/パスワード | 同一ニックネーム・「私のログ」が一致 |
| 5 | ゲスト参加 | 従来どおり。発行アカウント不要 |
| 6 | 自己登録 UI | どこからも新規会員登録できない |
| 7 | 無効化した ID | ログイン不可。既存投稿は残る |
| 8 | 管理者 `/admin/login` | 従来どおり。発行参加者 ID では管理者画面に入れない |

---

## 10. 将来拡張（本仕様のスコープ外）

| 項目 | 備考 |
|------|------|
| 一般向け自己登録 | [41](./41_個人ユーザー登録.md) を別フェーズで開放 |
| ゲスト投稿の引き継ぎ | ログイン時に端末ローカル投稿を `auth.uid()` にマージ |
| 参加者によるパスワード変更 | ワークショップ用途では不要。必要なら再発行で代替 |
| スペース横断 1 アカウント | 本仕様は **スペース単位** 発行。横断は将来検討 |
| メールアドレス連携 | 不要（参加 ID のみ） |

---

## 11. セキュリティ・運用上の注意

- 初期パスワードは **HTTPS 前提** で管理者から参加者へ安全に共有する（口頭・対面推奨）
- 平文パスワードは **発行/再発行時のみ** UI 表示。DB には Auth 側のハッシュのみ
- ブルートフォース対策: Supabase Auth の rate limit + 必要なら Edge Function 側でも IP 制限
- 参加 ID はスペース URL を知っている人にのみログイン画面が見えるが、**URL 共有＝入室可能** である点はゲストと同様

---

## 12. 変更ファイル一覧（実装時の目安）

| 種別 | ファイル / リソース |
|------|---------------------|
| 新規 | `supabase/migrations/YYYYMMDD_space_participant_accounts.sql` |
| 新規 | `supabase/functions/issue-participant-account/index.ts` |
| 新規 | `src/core/utils/participantAccountsApi.ts` |
| 新規 | `src/components/SpaceSettingsScreen/ParticipantAccountsTab.tsx` |
| 新規 | `src/components/Auth/ParticipantLoginScreen.tsx`（または `LoginScreen` 拡張） |
| 新規 | `src/components/Auth/ParticipantNicknameModal.tsx` |
| 変更 | `src/components/Auth/GuestEntryScreen.tsx` |
| 変更 | `src/core/contexts/AuthContext.tsx` — `loginParticipant()` |
| 変更 | `src/core/hooks/HossiiStoreProvider.tsx` — `authorId` 統合 |
| 変更 | `src/App.tsx` — セッション時の入室分岐 |
| 変更 | `SpaceSettingsScreen` — タブ追加 |
