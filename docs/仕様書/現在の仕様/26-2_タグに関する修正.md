# Hossii タグ機能まわりの現状整理と修正方針仕様書

> **文書役割:** **補足（経緯・調査・フェーズメモ）**
> **現行仕様・実装ステータスの正本:** [26_タグとフィルタリング機能](./26_タグとフィルタリング機能.md)
> ステータス矛盾がある場合は **26 を優先**する。

> **最終更新:** 2026-07-24（Production `tags text[]` 統一・main 反映。詳細は 26 正本）

---

## 目的

当初の目的（スペースのタグ候補 / ワンタップ付与 / ログでのフィルタ）と、
途中で見つかった障害（preset_tags のサイレント失敗、`hossiis.tags` 未マイグレーション）を記録する。

現行の機能定義・受入結果・Production 注意は **26 を見ること**。

---

## 調査で判明した原因（履歴）

### 根本原因: RLS による UPDATE のサイレントブロック

`spaces` テーブルの UPDATE ポリシーは `auth.uid()` を使う条件だったが、
開発中は Supabase クライアントが未認証（`auth.uid() = NULL`）で動いていた。

結果として:

- PATCH リクエストは正しく飛んでいる（`preset_tags` も含まれている）
- Supabase が 204 を返す
- しかし 0 rows updated で DB は更新されない

| 項目 | 結果 |
|------|------|
| PATCH リクエストの送信 | ✅ 正常（DevTools で確認） |
| リクエストの保存先テーブル | ✅ `spaces` で正しい |
| WHERE 条件 (`id = '...'`) | ✅ 対象 row は存在する |
| `auth.uid()` | ❌ NULL（未認証） |
| RLS UPDATE ポリシー | ❌ `auth.uid()` 前提のため実質ブロック |

---

## 実施した修正（履歴）

### 1. 開発用 RLS ポリシー（一時）

```sql
-- 開発用: 本番前に必ず DROP すること
CREATE POLICY "dev_spaces_update_all" ON spaces
  FOR UPDATE USING (true);
```

### 2. `updateSpaceInDb` のデバッグ強化

認証ユーザーのログ、`.select()` による更新件数確認、0 件更新時の warn。

### 3. 型コメントでの責務整理

- `presetTags`: スペースのタグ候補
- `Hossii.tags`: 投稿に付いたプリセットタグ（DB 正本型は `text[]`）

### 4〜5. `hossiis.tags` 追加と永続化・表示（2026-07-24）

詳細・受入表・Production 差分は **26 正本**に集約した。要約のみ:

| 項目 | Development | Production |
|------|-------------|------------|
| カラム追加 | ✅ `text[]`（migration 適用済み） | ✅ `20260724200000`（既存列のため no-op） |
| jsonb→text[] | —（既に text[]） | ✅ `20260724210000` 適用・DB 受入済み |
| INSERT 永続化 | ✅ | ✅ アプリ main 反映（UI 人手は 26 で未確認） |
| 再読込復元 | ✅（コード + REST） | ✅ REST 読取（既存非空タグ） |
| 吹き出し表示 | ✅（コード） | ⚠️ ブラウザ未確認 |
| 星モード | ✅（コード + 自動テスト） | ⚠️ ブラウザ未確認 |
| ログ / モデレーション | ✅（コード） | ⚠️ ブラウザ未確認 |

---

## フェーズ対応表（26 へのポインタ）

| フェーズ | 内容 | 状態の正本 |
|--------|------|-----------|
| 1 | `spaces.preset_tags` 安定化 | 26 T01 |
| 2a | `hossiis.tags` カラム追加 | 26「T02 の分割ステータス」 |
| 2b | INSERT / 再読込 | 26「Development 受入結果」 |
| 2c | 吹き出し表示 | 26 bubble 節 |
| 3 | ログ / モデレーション | 26 T03 / T04 |

---

## Production 型統一（適用済み）

正本は 26「Production 反映結果」を参照。要約:

- 旧型（Production）: `jsonb DEFAULT '[]'::jsonb`
- 適用後: `text[] DEFAULT '{}'`
- 変換 migration: `20260724210000_convert_hossiis_tags_jsonb_to_text_array.sql`
- **2026-07-24 Production 適用済み**（DB 受入 OK）
- 別作業 `fix/post-tag-required-validation` は未取り込み

---

## 今はまだやらなくてよいこと

- Production ブラウザ人手の全画面 E2E・投稿操作確認
- 本番向けの厳密な Auth/RLS 設計の完成
- 複雑な共同編集権限設計
- メンバーシップテーブルを含む完全な権限制御
- `fix/post-tag-required-validation`（別作業）
