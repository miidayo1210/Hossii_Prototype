# 51 簡単投稿（クイック投稿パネル）

## 機能概要

スペース画面上の空きスペース（ボタンやバブル以外の場所）を **ダブルクリック / ダブルタップ** すると、スペース画面を表示したまま投稿フォームがパネル形式で出現し、その場で気持ちを置けるようにする。

投稿は **ダブルクリックした座標**、または **中央既定座標**（`positionX/Y` ともに `50`）に配置される（`isPositionFixed: true`）。

- **訪問モード**（隣人スペース閲覧中）は閲覧専用のため、クイック投稿パネルは無効（[44_隣人システム](./44_隣人システム.md)）。
- **右端キューブドック（`SpacePanelCubeDock`）は廃止**。代わりに TopRightMenu「投稿する」から開く（[06_ナビゲーション・UI構成](./06_ナビゲーション・UI構成.md)）。

---

## デバイス別レイアウト

### PC（768px より広い画面）

スペース画面の**右側 1/4** にサイドパネルとしてスライドインする。

```
┌────────────────────┬──────────┐
│                    │          │
│   スペース画面      │ 投稿     │
│   （そのまま表示）  │ パネル   │
│                    │          │
└────────────────────┴──────────┘
         75%                25%
```

### モバイル縦（768px 以下 + `orientation: portrait`）

投稿パネルを開くと **スペース画面の上に重なる固定ボトムシート** として表示する（LINE の入力欄展開に近い UX）。`FloatingPanelShell` は使わない。**ドラッグ・リサイズ不可**。

- **スペース:** レイアウトは変えず全画面表示のまま
- **投稿シート:** `position: fixed` の `mobilePostSheet`。下ナビ（72px + safe-area）の **直上** から下方向へスライドイン
- **高さ:** 58vh（最小 280px）。縦方向の伸縮は不可
- **内部:** `PostScreen` の `panelMain` を `-webkit-overflow-scrolling: touch` でスムーズスクロール

```
┌──────────────────────────┐
│   スペース（そのまま）      │
│                          │
│   ┌──────────────────┐   │  ← 固定ボトムシート（重ね表示）
│   │   投稿フォーム      │   │
│   └──────────────────┘   │
├──────────────────────────┤
│  BottomNav（常に見える）   │
└──────────────────────────┘
```

### モバイル横（`max-height: 600px` + `orientation: landscape`）

iPhone 等は横持ちで論理幅が 768px を超えるため、**幅ではなく高さ**で横持ちモバイルを判定する（[75](./75_スマホ横持ちレイアウト.md)）。

投稿パネルを開くと **左右スプリット固定レイアウト** になる（`FloatingPanelShell` は使わない）。**ドラッグ・リサイズ不可**。

- **左:** スペース（残り幅、`flex: 1`）
- **右:** 固定投稿ドック（`mobilePostSideDock`、幅 `clamp(240px, 36vw, 320px)`）
- **`panelMode="side"`**（PC サイドと同系の縦長フォーム）

```
┌──────────────────┬─────────┐
│                  │         │
│   スペース        │  投稿   │  ← 右ドック固定
│                  │  フォーム│
│                  │         │
├──────────────────┴─────────┤
│  BottomNav（固定）          │
└────────────────────────────┘
```

詳細: [75_スマホ横持ちレイアウト](./75_スマホ横持ちレイアウト.md) / [76_スマホ投稿ナビとクイック投稿](./76_スマホ投稿ナビとクイック投稿.md)

---

## 投稿位置の決定

### ダブルクリックで開いた場合

ダブルクリック / ダブルタップした座標を、スペースコンテナに対する **パーセント値（0〜100）** に変換して `positionX` / `positionY` として使用する。

```
positionX = (clientX - containerRect.left) / containerRect.width  * 100
positionY = (clientY - containerRect.top)  / containerRect.height * 100
```

### TopRightMenu / ナビゲーションから開いた場合

座標指定がないため、スペース中央付きの既定値として **`{ x: 50, y: 50 }`**（パーセント）を渡す。

投稿はいずれの経路も `isPositionFixed: true` で保存され、スペース画面上の指定座標に表示される。

---

## トリガー条件（ダブルクリック）

- **対象**: スペース画面のコンテナ要素への `onDoubleClick`
- **除外**: バブル・ボタン・コントロールバー・装飾要素など、インタラクティブな要素上でのダブルクリックは除外する（各要素で `e.stopPropagation()` により自然にキャンセルされる）
- **開閉のトグル**: パネルが**既に開いている**ときは、再度ダブルクリックで **閉じる**（`quickPostPos` を `null`）。閉じたあとに再度ダブルクリックした位置で開く。
- **ログパネル（#57）との競合回避**: ダブルクリック直後すぐには開かず、**約 320ms 遅延**してから開く。同じ間に **第3クリック（トリプルクリック）** が発生すると遅延タイマーをキャンセルし、投稿パネルは開かない（トリプルはログパネル用。`SpaceScreen.tsx` 内の `pendingQuickPostOpenRef`）。

---

## トリガー条件（ナビゲーション「投稿」・全デバイス共通）

> **詳細（モバイル）:** [76_スマホ投稿ナビとクイック投稿](./76_スマホ投稿ナビとクイック投稿.md)  
> **詳細（PC）:** [06_ナビゲーション・UI構成](./06_ナビゲーション・UI構成.md)  
> **ステータス:** ✅ 実装済み（全デバイス共通）

| 条件 | 挙動 |
|------|------|
| **`#screen`** + パネル未表示 | クイック投稿パネルを **開く**（位置 `{ x:50, y:50 }`） |
| **`#screen`** + パネル表示中 | **閉じる**（トグル） |
| **他画面** | `#screen` へ遷移後、パネルを開く |
| 訪問モード | 開かない |

- PC・モバイル**どちらも同じ動作**（`#post` フルページへの遷移はしない）
- ナビからの開閉は **即時**（320ms 遅延は **ダブルタップ経路のみ**）
- 同じ `quickPostPos` / `PostScreen` パネルを使う

---

## ❌ キューブドック（廃止）

> **廃止（2026-06-02）:** 右端 `SpacePanelCubeDock` は削除。

以前は PC 右端に「投稿」キューブを置いていたが、UI の存在がわかりにくいため廃止。  
代替として TopRightMenu「投稿する」・背景ダブルクリックで開ける。  
`SpacePanelCubeDock.tsx` / `SpacePanelCubeDock.module.css` は削除対象。

---

## UI 仕様

### パネルの表示内容

既存の `PostScreen` をパネルモードで流用する。パネルモードでは以下を **非表示** にする：

- `TopRightMenu`（右上メニュー）
- `HossiiMini` ヘッダー（Hossii キャラクターとセリフ）
- タイトル「気持ちを置く 🌸」（任意）

投稿フォーム本体（感情ボタン・メッセージ・送信ボタン等）はそのまま表示する。

### 閉じるボタン

パネル右上（または上端中央）に **「✕ 閉じる」** ボタンを配置する。

### 投稿後の挙動

パネルモードでは、投稿完了後に `navigate('screen')` は呼ばない。

- **連続投稿（#46）が OFF** のとき: トーストを見せたあと短い遅延で `onClose()` を呼び、パネルを閉じる。
- **連続投稿が ON** のとき: `onClose()` は呼ばずパネルを開いたままにする。コメント投稿が有効なスペースでは、本文入力へフォーカスを戻す。

送信完了時点のチェック状態で上記を判定する（画像アップロード待ちのあいだにチェックを変えた場合も、完了時の意図が反映されるようにしている）。

---

## 実装仕様

### 変更・追加ファイル

| ファイル | 変更種別 | 内容 |
|---------|---------|------|
| `src/components/SpaceScreen/SpaceScreen.tsx` | 変更 | ダブルクリック・トリプルクリック連携・`quickPostPos`・モバイル固定ドック / PC `FloatingPanelShell` |
| ~~`src/components/SpaceScreen/SpacePanelCubeDock.tsx`~~ | **廃止** | |
| ~~`src/components/SpaceScreen/SpacePanelCubeDock.module.css`~~ | **廃止** | |
| `src/components/SpaceScreen/SpaceScreen.module.css` | 変更 | `mobilePostSheet` / `containerMobilePostLandscapeSplit` / `mobilePostSideDock`、PC 用 chrome クラス |
| `src/components/PostScreen/PostScreen.tsx` | 変更 | `panelMode` / `initialPosition` / `onClose` 等 |
| `src/components/PostScreen/PostScreen.module.css` | 変更 | パネルモード用スタイル追加 |

### `PostScreen` の props 追加

```tsx
type Props = {
  panelMode?: 'side' | 'bottom';
  initialPosition?: { x: number; y: number };
  onClose?: () => void;
};

export const PostScreen = ({ panelMode, initialPosition, onClose }: Props) => { ... }
```

| prop | 型 | 説明 |
|------|---|------|
| `panelMode` | `'side' \| 'bottom' \| undefined` | `'side'`=PC サイドパネル、`'bottom'`=モバイル ボトムシート、`undefined`=従来のフルスクリーン |
| `initialPosition` | `{ x: number; y: number } \| undefined` | ダブルクリック座標（0〜100 のパーセント値）。指定時は `positionX`/`positionY` の初期値として使用し `isPositionFixed: true` で投稿 |
| `onClose` | `() => void \| undefined` | パネルを閉じるコールバック。`panelMode` 時に「閉じる」ボタンと投稿完了後に呼ぶ |

### `handleSubmit` の分岐（要点）

```ts
// フルスクリーン: OFF のときだけスペースへ戻る
if (!panelMode && !continuousPost) {
  setTimeout(() => navigate('screen'), 800);
}

// パネル: OFF のときだけトースト後に閉じる。ON のときは閉じない
if (panelMode) {
  if (!continuousPost) {
    setTimeout(() => onClose?.(), 700);
  } else if (commentPost 有効) {
    requestAnimationFrame(() => messageTextareaRef.current?.focus());
  }
}
```

※ `continuousPost` は非同期のあとでも最新のチェック状態を参照する必要があるため、実装では ref とチェック時の同期を併用している。

### `SpaceScreen` でのパネル管理（要点）

```tsx
const mobilePostSplit = useMobileBottomSheet && quickPostPos != null; // 縦
const mobilePostLandscapeSplit = isMobileLandscape && quickPostPos != null; // 横

{quickPostPos &&
  (mobilePostSplit ? (
    <div className={styles.mobilePostSheet}>
      <PostScreen panelMode="bottom" ... />
    </div>
  ) : mobilePostLandscapeSplit ? (
    <div className={styles.mobilePostSideDock}>
      <PostScreen panelMode="side" ... />
    </div>
  ) : (
    <FloatingPanelShell storageKey="quickPost.desktop" zIndex={310} ...>
      <PostScreen panelMode="side" ... />
    </FloatingPanelShell>
  ))}
```

- **モバイル:** `FloatingPanelShell` 不使用。位置・サイズの `localStorage` 永続化もしない
- **PC:** 従来どおり `FloatingPanelShell`（`storageKey="quickPost.desktop"`）でドラッグ・リサイズ可

### CSS 概要

**PC サイドパネル（`.sidePanelWrapper`）**

```css
.sidePanelWrapper {
  position: absolute;
  top: 0;
  right: 0;
  width: 25%;
  min-width: 320px;
  height: 100%;
  overflow-y: auto;
  background: white;
  box-shadow: -4px 0 24px rgba(0, 0, 0, 0.12);
  animation: slideInRight 0.2s ease-out;
  z-index: 100;
}
```

**モバイル縦 — 固定ボトムシート（`.mobilePostSheet`）**

`position: fixed`。下ナビ直上・高さ 58vh。`translateY` スライドインアニメーション。内部は `PostScreen` スクロール。

**モバイル横 — 右固定ドック（`.mobilePostSideDock`）**

`containerMobilePostLandscapeSplit` が flex 横。左スペース + 右 `mobilePostSideDock`（幅 `clamp(240px, 36vw, 320px)`）。

---

## 既存機能との組み合わせ

| 機能 | 挙動 |
|------|------|
| **連続投稿モード（#46）** | パネルモードでは **OFF** のときだけ投稿後に `onClose()` で閉じる。**ON** のときはパネルを開いたまま（#46 参照） |
| **位置選択グリッド（#45）** | パネルモードでは `position_selector` フラグの ON/OFF に関わらず、位置グリッドを非表示にする。ダブルクリック座標がそのまま位置として使われるため不要 |
| **お絵描き（DrawingModal）** | パネルモードでもそのまま動作する（モーダルがパネルの上に重なる） |

---

## 実装状況

**実装済み（主要機能）**

| 項目 | 状態 |
|------|------|
| ダブルクリックでパネル表示（遅延＋トリプルでキャンセル） | ✅ 実装済み |
| ダブルクリックでパネルが開いているときは **閉じる**（トグル） | ✅ 実装済み |
| PC: `FloatingPanelShell` で **ドラッグ・リサイズ・永続化**（`z-index: 310`） | ✅ 実装済み（[56](./56_クリックフィードバックとフローティングパネル.md)） |
| モバイル縦: **固定ボトムシート**（下からスライドイン・ドラッグ不可） | ✅ 実装済み |
| モバイル横: **左スペース + 右固定ドック**（ドラッグ不可） | ✅ 実装済み |
| ダブルクリック座標またはナビ既定（50,50）を `positionX/Y` として投稿 | ✅ 実装済み |
| TopRightMenu「投稿する」→ クイック投稿（全デバイス共通） | ✅ 実装済み |
| ~~右側キューブドック~~ | ❌ 廃止 |
| TopRightMenu / HossiiMini ヘッダー / 位置グリッド をパネル時に非表示 | ✅ 実装済み |
| 「✕ 閉じる」ボタン | ✅ 実装済み |
| 連続投稿 OFF のときのみ、トースト後に `onClose()` でパネルを閉じる。ON のときは閉じない | ✅ 実装済み |

**主な関連ファイル**

| ファイル | 変更内容 |
|---------|---------|
| `src/components/SpaceScreen/SpaceScreen.tsx` | `quickPostPos`・`mobilePostSplit` / `mobilePostLandscapeSplit`・`mobilePostSheet`・PC のみ `FloatingPanelShell` |
| ~~`src/components/SpaceScreen/SpacePanelCubeDock.tsx`~~ | **廃止（削除対象）** |
| `src/components/SpaceScreen/SpaceScreen.module.css` | `.quickPostSideChrome` / `.quickPostBottomChrome` 等 |
| `src/components/FloatingPanelShell/*` / `src/core/utils/floatingPanelStorage.ts` | パネルシェル・既定矩形（[56](./56_クリックフィードバックとフローティングパネル.md)） |
| `src/components/PostScreen/PostScreen.tsx` | パネルモード・`handleSubmit` 分岐 |
| `src/components/PostScreen/PostScreen.module.css` | `.panelContainer` / `.panelCloseBar` 等 |

---

## 未決事項

- [ ] パネルが開いているときにスペース画面の Hossii（HossiiLive）の動作をどうするか（そのまま動かすか・一時停止するか）
- [ ] PC で 25% 幅が狭すぎる場合のレスポンシブ対応（min-width を設けるか、`FloatingPanelShell` の既定サイズを調整するか）
- [ ] Feature Flag で管理者がこの機能を有効/無効にできるようにするか
- [ ] ESC キーでパネルを閉じるキーボードショートカット対応

---

## 追加仕様（改善要件・未実装／将来）

### A. ダブルクリックによるパネルのトグル開閉

**実装済み（2026-04）** — 上記「トリガー条件」「キューブドック」を参照。開いているときはダブルクリックで閉じ、閉じたあとは新しいクリック位置で開く。開くまでに **約 320ms の遅延**があり、**トリプルクリック**でログパネル（#57）を開く場合は投稿の遅延オープンをキャンセルする。

#### 補足（未対応のまま残るもの）

- 閉じる方向のスライドアウト専用アニメーションは未実装（パネルは即非表示）。
- 「✕ 閉じる」・キューブ・ダブルクリックの閉じる挙動はいずれも `quickPostPos` を `null` にする点で一致。

---

### B. サイドパネルのデザイン改善（立体感・浮遊感・ヘッダー非重複）

#### B-1. 立体感・浮遊感

「白い紙がスペース画面の右側に浮いている」ような見た目にする。

| 属性 | 値 | 意図 |
|------|----|------|
| `border-radius` | `20px 0 0 20px` | 左側（接続面の反対側）のみ丸めて、浮いている感を出す |
| `background` | `linear-gradient(160deg, #fff 0%, #fdf6ff 100%)` | わずかに紫寄りのホワイト。Hossii の世界観に合わせる |
| `box-shadow` | `−10px 4px 40px rgba(139,92,246,0.18), −2px 0 12px rgba(0,0,0,0.06)` | 紫がかった影を左下方向に落とすことで「浮いている」奥行きを出す |
| `border-left` | `1px solid rgba(255,255,255,0.9)` | 光のハイライト（白線）で手前に出ている感を強調 |

```css
.sidePanelWrapper {
  position: absolute;
  top: 4rem;                        /* TopBar の高さ分下げる */
  right: 0;
  width: 25%;
  min-width: 300px;
  height: calc(100% - 4rem);        /* TopBar 分の高さを除く */
  overflow-y: auto;
  background: linear-gradient(160deg, #fff 0%, #fdf6ff 100%);
  border-radius: 20px 0 0 20px;
  border-left: 1px solid rgba(255, 255, 255, 0.9);
  box-shadow:
    -10px 4px 40px rgba(139, 92, 246, 0.18),
    -2px 0 12px rgba(0, 0, 0, 0.06);
  animation: slideInRight 0.25s cubic-bezier(0.34, 1.56, 0.64, 1);
  z-index: 100;
}
```

> `cubic-bezier(0.34, 1.56, 0.64, 1)` は軽くオーバーシュート（ぷっくり感）するイージング。

#### B-2. ナビゲーション・ヘッダーへの重複解消

現状の `.sidePanelWrapper` は `top: 0; height: 100%` で画面最上部から始まるため、PC の TopBar（`4rem`）に被る。

**修正:** `top: 4rem; height: calc(100% - 4rem)` にすることで TopBar の下端から始める。

モバイルの `.bottomSheetWrapper` は `position: fixed` かつ下から出るため、TopBar との重複は発生しない（対応不要）。

#### B-3. 縦方向のコンパクト化（パネルモード専用スタイル）

`PostScreen` の `panelMode` 時は以下を縮小する。

| 要素 | 通常モード | パネルモード |
|------|-----------|------------|
| `main` の padding | `1.5rem 1rem` | `0.75rem 0.75rem` |
| セクション間 margin | `1.5rem` | `0.75rem` |
| タイトル「気持ちを置く 🌸」 | 表示 | **非表示**（すでに実装済み対象外だが追加で非表示推奨） |
| `panelCloseBar` の padding | `0.5rem 0.75rem` | そのまま |

```css
/* PostScreen.module.css へ追加 */
.panelMain {
  padding: 0.75rem 0.75rem;
}

.panelSection {
  margin-bottom: 0.75rem;
}
```

`panelMode` 時は `<main className={panelMode ? styles.panelMain : styles.main}>` に切り替える。

---

### C. 「気持ちを置く」ボタンの立体感・ポヨンアニメーション

#### C-1. 立体感（ネオモーフィズム風 3D ボタン）

```css
.submitButton {
  /* 既存スタイルに追加 */
  position: relative;
  top: 0;
  box-shadow:
    0 5px 0 #7c3aed,           /* 下面の影（ボタン本体の厚み） */
    0 8px 16px rgba(124, 58, 237, 0.35); /* 浮遊感のある落ち影 */
  border-bottom: none;
  transition: top 0.08s ease, box-shadow 0.08s ease;
}
```

| 状態 | `top` | `box-shadow` |
|------|-------|-------------|
| 通常 | `0` | 5px の下面影 + 浮遊影 |
| ホバー | `0` | 影をやや大きく（`0 7px 20px`） |
| 押下中（`:active`） | `3px` | 下面影を `2px` に縮小 → 「沈んだ」感 |

#### C-2. ポヨンアニメーション

ボタンを離した瞬間（`:active` が外れた後）にバネで戻るような弾みをつける。React の `onClick` ハンドラで CSS クラスを付け外しする方法が安定している。

```css
@keyframes poyon {
  0%   { transform: scale(1); }
  25%  { transform: scale(0.91); }   /* 押し込まれる */
  55%  { transform: scale(1.07); }   /* 戻りすぎ（バネ） */
  75%  { transform: scale(0.97); }   /* 微調整 */
  100% { transform: scale(1); }      /* 定位置 */
}

.submitButtonPoyon {
  animation: poyon 0.35s cubic-bezier(0.36, 0.07, 0.19, 0.97);
}
```

**実装方法（PostScreen.tsx）:**

```tsx
const [poyonActive, setPoyonActive] = useState(false);

const handleSubmitClick = () => {
  setPoyonActive(true);
  handleSubmit();
};

const handleAnimationEnd = () => setPoyonActive(false);

// JSX
<button
  onClick={handleSubmitClick}
  onAnimationEnd={handleAnimationEnd}
  className={`${styles.submitButton} ${poyonActive ? styles.submitButtonPoyon : ''}`}
  ...
>
```

#### C-3. 閉じるボタンへの適用

`panelCloseButton` にも同様の立体感・ポヨンを適用する（スケールは控えめに `scale(0.95)` → `scale(1.03)` 程度）。

---

## 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-04-05 | 初版相当（クイック投稿・FloatingPanelShell） |
| 2026-04-06 | トリプルクリック連携・遅延オープン・ダブルクリックトグル・右側キューブドック（`SpacePanelCubeDock`）・訪問モード無効を明文化 |
| 2026-04-06 | モバイル: キューブドックは PC のみ・ボトムシートと BottomNavBar の重なり防止（#56 の `bottomInset`） |
| 2026-04-06 | 連続投稿 ON 時はパネルを閉じない・OFF のときのみトースト後 `onClose`（#46 補足と整合）。`handleSubmit` の記述を更新 |
| 2026-06-02 | モバイル: **固定スプリットドック**（縦=16:9+下 / 横=左+右）。`FloatingPanelShell` は PC のみ |
| 2026-06-02 | モバイル縦: スプリット廃止→**固定ボトムシート**（`mobilePostSheet`）。横持ち判定を `max-height: 600px` に変更 |