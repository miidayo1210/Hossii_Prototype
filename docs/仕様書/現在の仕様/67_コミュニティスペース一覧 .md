# 67 コミュニティスペース一覧（スーパー管理者の表示）

## この文書の目的

**スーパー管理者**が [21_コミュニティごとのスペース管理画面](./21_コミュニティごとのスペース管理画面.md) の導線から **特定コミュニティのスペース一覧**を開いたとき、**他コミュニティのスペースが一瞬でも一覧に現れない**こと、およびそのために必要な実装方針を仕様としてまとめる。

一般管理者（自コミュニティのみ）の挙動は従来どおりを前提とする。

---

## 用語

| 用語 | 意味 |
|------|------|
| スーパー管理者 | `AppUser.isSuperAdmin === true`。全コミュニティを横断して管理する。 |
| override | `AdminNavigationContext` の `overrideCommunityId` / `overrideCommunityName` / `overrideCommunitySlug`。スーパー管理者が「どのコミュニティの管理画面を見ているか」を表す。`sessionStorage` に `hossii_adminNav` として永続化される。 |
| スペース一覧画面 | 実装上は `SpacesScreen`（ハッシュ `#spaces` または `#spaces/{communitySlug}`）。 |

---

## 現状の挙動と課題

### 期待する UX

- コミュニティ管理一覧から **あるコミュニティの「スペースを管理」** を押した直後から、**そのコミュニティに属するスペースだけ**が一覧に表示される。
- 全コミュニティ分のスペースが一度に並び、その後に絞り込まれるような **フラッシュは許容しない**。

### 技術的な原因（現行コード）

1. **`fetchSpaces` の仕様**  
   `src/core/utils/spacesApi.ts` の `fetchSpaces(communityId?: string)` は、`communityId` が **渡されない**とき `community_id` 列で **フィルタせず** `spaces` テーブルを **全件**取得する。

2. **`HossiiStoreProvider` の `communityId` の決め方**  
   `communityId = overrideCommunityId ?? currentUser?.communityId`  
   スーパー管理者は **`currentUser.communityId` を持たない**ことが多く、`overrideCommunityId` が **まだ無い**フレームでは `communityId` が **undefined** になり、`fetchSpaces(undefined)` が **全件取得**につながる。

3. **状態の持ち越し**  
   一度全件が `SET_SPACES` されると、`override` が確定してから **別の `communityId` で再 fetch** するまで、画面上は **全件のまま**残る。非同期の完了タイミングによっては **一瞬すべて表示**に見える。

4. **直接 URL・復元の遅延**  
   `SpacesScreen` は `overrideCommunityId` が無いとき `fetchCommunityBySlug(screenParam)` で override を **非同期**で復元する。歴史的にはこの間に上記 2 と組み合わさり **`fetchSpaces(undefined)` による全件取得**が走り得た。**案 A 適用後**は、スーパー管理者で `override` も `currentUser.communityId` も無い間は **`fetchSpaces` 自体を起動しない**ため、典型フローでは抑止される。ただし **原因 6・7** は別問題として残り得る。

5. **一覧に戻ったあとの ref リセット（案 B 単体での抜け）**  
   案 B で `lastScopedCommunityFetchKeyRef` を「別コミュニティへ切り替わったか」判定に使うとき、コミュニティ一覧 `#communities` にいる間の `skipAllTenantFetch` 分岐で **ref を `undefined` に戻す**と、次に別コミュニティ B を開いた直後は `prevKey` が未定義のままになり **`shouldClearBeforeFetch` が false** になり得る。その結果、**直前に開いていたコミュニティ A の `state.spaces` が B の fetch 完了まで画面上に残る**（ブラウザキャッシュではなく React state の持ち越し）。  
   ※現行コードでは `skipAllTenantFetch` 分岐で ref を消さない **案 B 補足（案1）** により原因 5 は緩和済み。ただし **`effectiveId == null` 分岐**（ログアウト等）では ref が `undefined` に戻るため、その直後の **最初のスコープ付き fetch** では再び `prevKey === undefined` となり、**fetch 前クリアがスキップされ得る**（下記 7 と合わせて対策する）。

6. **非同期レスポンスの競合（順序逆転）**  
   `fetchSpaces(effectiveId).then(applySpaces)` に **「完了時点でまだその `effectiveId` が有効か」** の検証がない場合、**遅い応答が後から届き**、すでに別コミュニティへ切り替えたあとに **古い一覧で `SET_SPACES` する**ことがある。見た目では「一瞬直ったあと、また別コミュニティの一覧に戻る」などになる。

7. **`shouldClearBeforeFetch` が ref のみに依存する抜け**  
   `prevKey !== undefined && prevKey !== effectiveId` のときだけ fetch 前に一覧を空にする実装では、`lastScopedCommunityFetchKeyRef` が **`undefined` のまま**のケース（初回のスコープ付き fetch、上記 5 の注記のとおり ref リセット直後など）では **クリアが走らず**、その間 **`state.spaces` は前回の React state または `initializeSpaces()` が読み込んだ localStorage** のままになる。前者は「直前コミュニティの一覧が残る」、後者は「意図しない件数が一瞬並ぶ」につながり得る。

---

## 仕様上のゴール

| # | 要件 |
|---|------|
| G1 | スーパー管理者で **`overrideCommunityId` が未確定**のあいだ、`fetchSpaces` によって **テナント横断の全スペース一覧を state に載せない**。 |
| G2 | `overrideCommunityId` が **確定した時点**で、その **1 コミュニティ分**だけを取得して一覧に反映する。 |
| G3 | コミュニティ A から B に切り替えるとき、**A の一覧が B の読み込み完了まで残らない**（空・スケルトン・ローディングのいずれかでよいが、**他コミュニティのスペースを誤って表示しない**）。 |
| G4 | 一般管理者（`overrideCommunityId` が null で `currentUser.communityId` がある）は **現状と同様**、自コミュニティのスペースのみ取得する。 |
| G5 | **同一画面内でコミュニティやスコープが変わったあと**、より古い `fetchSpaces` の結果が state を **上書きしない**（非同期の順序逆転に負けない）。 |

---

## 実装案（具体的）

以下は **いずれかまたは組み合わせ**で **G1〜G5** を満たす想定である（**G5** は主に案 E）。実装時は `npm run lint` / `npm run build` と、スーパー管理者での画面遷移を確認する。

### 案 A — ストア層で「スーパー管理者かつ community 未確定」は fetch しない

**場所**: `src/core/hooks/HossiiStoreProvider.tsx`

- `useAuth` から `currentUser?.isSuperAdmin` を参照する。
- 条件例: `isSupabaseConfigured && currentUser?.isSuperAdmin && !overrideCommunityId && !currentUser?.communityId` のときは **`fetchSpaces` を呼ばない**。
- この間は `spacesLoadedFromSupabase` の扱いを決める必要がある。例: **未ロード扱い**のままにする／**空配列を dispatch** して一覧を空にする。

**注意**: スーパー管理者が **コミュニティ一覧 `#communities` にいるだけ**のときも override は無いので、**誤って全件 fetch しない**ことと整合させる。

### 案 B — `communityId` 変更直前に一覧をクリア

**場所**: `HossiiStoreProvider` の `useEffect`（`communityId` 依存）の先頭、または reducer に `CLEAR_SPACES` のようなアクションを追加。

- `overrideCommunityId` が **null → 非 null** になったタイミング、または **UUID から別 UUID** に変わったタイミングで、**先に** `dispatch({ type: 'SET_SPACES', payload: [], ... })` 等し、**古い全件一覧を消す**。
- その後 `fetchSpaces(communityId)` で **正しい一覧だけ**を入れる。

**メリット**: fetch の有無と独立に **見た目のフラッシュ**を抑えやすい。

#### 案 B の補足 — **案1**: コミュニティ一覧では `lastScopedCommunityFetchKeyRef` を消さない

**場所**: `HossiiStoreProvider` の `skipAllTenantFetch` 分岐（および必要ならログアウト等でのリセット）

- **方針**: スーパー管理者が **コミュニティ一覧**にいて `fetchSpaces` をスキップしている間も、**直近スコープ付き fetch に成功したコミュニティ ID** は ref に **保持**する（`lastScopedCommunityFetchKeyRef.current = undefined` にしない）。
- **狙い**: 一覧からコミュニティ B の「スペースを管理」へ入ったとき、`prevKey` が直前の A のまま残り、`effectiveId === B` となり、**案 B の「別コミュニティへ切り替わる → fetch 前に一覧を空にする」**が確実に効くようにする（上記「技術的な原因 5」の対策）。
- **注意**: **ログアウト**・**別ユーザーでログイン**・文脈を本当にリセットすべきタイミングでは、ref を **別途クリア**し、他画面との整合を取る。

### 案 C — `fetchSpaces` の契約を厳格化する

**場所**: `src/core/utils/spacesApi.ts`

- 呼び出し側で **常に `communityId` を渡す**設計に寄せ、`undefined` のときは **空配列を返す**または **fetch 自体を行わない**（破壊的変更になり得るため、呼び出し箇所の洗い出しが必要）。

### 案 D — `SpacesScreen` の表示ガード

**場所**: `src/components/SpacesScreen/SpacesScreen.tsx`

- スーパー管理者かつ `overrideCommunityId` がまだ無い（`fetchCommunityBySlug` 待ち）の間は、**一覧 UI を出さず**ローディングのみ表示する。
- ストアに全件が入っていても **フィルタして表示しない**のは二重対策として有効だが、**データがストアに載らない**案 A・B の方が安全。

### 案 E — 取得結果の適用ガード（リクエスト世代・キー一致）

**場所**: `src/core/hooks/HossiiStoreProvider.tsx`（`fetchSpaces` を起動する `useEffect` 内）

- `fetchSpaces` を投げるたびに **モノトニックな世代番号**（または `AbortController`）を持つ。
- Promise が解決したとき、**その起動時の `effectiveId`（または世代）が、いま effect が想定している値と一致する場合だけ** `SET_SPACES` する。一致しない場合は **結果を破棄**し、`spacesLoadedFromSupabase` の扱いは「その後の有効な fetch に任せる」など一貫させる。
- **狙い**: 上記「技術的な原因 6」を潰し、**G5** を満たす。

### 案 B の補足 — **案2**: `effectiveId` の変更は ref だけに頼らず検知する

**場所**: `HossiiStoreProvider` の `fetchSpaces` 用 `useEffect`

- **`lastScopedCommunityFetchKeyRef` が `undefined` でも**、「**直前に fetch を走らせた `effectiveId`**」と今回の `effectiveId` を別 ref で保持し、**変わったら必ず**（pending スペース INSERT の保持方針と整合する範囲で）fetch 前に一覧を空にする、または **案 E と組み合わせ**て少なくとも **誤表示の持ち越しを許さない**。
- **狙い**: 上記「技術的な原因 7」および **ref リセット直後**の抜けを塞ぐ。

---

## 推奨する組み合わせ

| 優先 | 内容 |
|------|------|
| 1 | **案 A** でスーパー管理者の **community 未確定時に全件 fetch を発生させない**。 |
| 2 | **案 B** で **community 切替時に旧一覧を即クリア**し、取得完了まで空またはスケルトンにする。 |
| 2' | **案 B 補足（案1）** で **コミュニティ一覧にいる間も** `lastScopedCommunityFetchKeyRef` を維持し、一覧から別コミュニティを開いたときも **旧コミュニティ一覧のちらつき**を防ぐ。 |
| 2'' | **案 B 補足（案2）** および／または **案 E** で、**ref が未定義のフレーム**や **遅延した古い fetch** でも **他コミュニティ一覧が残ったり戻ったりしない**ようにする（**G5**）。 |
| 3 | 必要に応じ **案 D** で UI を補強。 |
| 4（任意） | **案 C** で `fetchSpaces` の **`communityId` 必須**化により、誤った全件取得を API 層でも防ぐ。 |

---

## 関連ファイル

| ファイル | 役割 |
|----------|------|
| `src/core/utils/spacesApi.ts` | `fetchSpaces` のクエリ条件 |
| `src/core/hooks/HossiiStoreProvider.tsx` | `communityId` と `fetchSpaces` の `useEffect` |
| `src/core/contexts/AdminNavigationContext.tsx` | override の state と `sessionStorage` |
| `src/components/CommunitiesScreen/CommunitiesScreen.tsx` | `setOverrideCommunity` と `navigate('spaces', slug)` |
| `src/components/SpacesScreen/SpacesScreen.tsx` | スーパー管理者の override 復元 `useEffect` |

---

## 実装ステータス

| 項目 | 状態 |
|------|------|
| 本仕様書の記載 | 文書化済み |
| 案 A・B・D のコード反映 | **反映済み**（案 C の API 契約変更のみは未実施） |
| 案 B 補足（**案1**：一覧で ref を維持） | **反映済み**（`skipAllTenantFetch` で ref を消さない） |
| 案 B 補足（**案2**）・**案 E**（**G5**：古い fetch の破棄） | **反映済み**（`HossiiStoreProvider`：`lastSpacesFetchTargetRef` / `spacesFetchRequestIdRef`） |
| 案 C（`fetchSpaces` の `communityId` 必須化） | **未実施**（任意） |
| 受け入れ条件 2・6 | **2026-04-09** 案1後に 2 を確認。**2026-04-16** 案2・案 E 実装後は 6 含め再確認推奨（`npm run build` 通過済み） |

---

## 実装内容（反映済み）

| 区分 | 内容 |
|------|------|
| 案 A | `HossiiStoreProvider` の `fetchSpaces` 用 `useEffect` で、スーパー管理者かつ `overrideCommunityId` も `currentUser.communityId` も無いとき **`fetchSpaces` を呼ばない**。`communityId` が空のときも全件取得を避ける。スキップ時は `spacesLoadedFromSupabase` を true にし、**`spacesFetchRequestIdRef` を進めて**進行中の fetch 完了を無効化（案 E との整合）、`App.tsx` の `/s/` 解決と整合。 |
| 案 B | 取得成功時に `lastScopedCommunityFetchKeyRef` を更新。**取得後**に `activeSpaceId` が一覧に無ければ先頭スペースへ `SET_ACTIVE_SPACE`。 |
| 案 B 補足（案1） | `skipAllTenantFetch`（コミュニティ一覧など）の分岐では **`lastScopedCommunityFetchKeyRef` を `undefined` に戻さない**。進行中の `fetchSpaces` を無効化するため **`spacesFetchRequestIdRef` をインクリメント**する。 |
| 案 B 補足（案2） | `lastSpacesFetchTargetRef` に「直近で `fetchSpaces` を **開始した** `effectiveId`」を保持し、`prevTarget !== effectiveId` のとき fetch 前に `SET_SPACES([])` と `SYNC_HOSSIIS([])`（初回・ログアウト直後の `undefined !== uuid` でもクリア）。`effectiveId == null` でターゲット ref をリセット。 |
| 案 E | `spacesFetchRequestIdRef` を fetch 開始ごとにインクリメントし、完了コールバックで **世代が一致するときだけ** `SET_SPACES` / `setSpacesLoadedFromSupabase(true)` を実行。`skipAllTenantFetch` / `effectiveId == null` でも世代を進め、一覧画面やログアウト後に遅延応答が state を汚さないようにする。 |
| 補助 | アクティブスペースの hossii 同期 `useEffect` で、`activeSpaceId` が現在の `spaces` に存在しないときは **fetchHossiis を呼ばず** hossii を空にする（一覧クリア直後の不整合防止）。依存に `state.spaces` を追加。 |
| 案 D（最小） | `SpacesScreen` で、スーパー管理者かつ `overrideCommunityId` があり、一覧が空で **`spacesLoadedFromSupabase` が false** のとき「スペース一覧を読み込み中です…」を表示。 |

### 実装内容（未反映・任意）

| 区分 | 内容 |
|------|------|
| 案 C（任意） | `fetchSpaces(undefined)` で全件取得されないよう **API 契約を厳格化**（呼び出し側の洗い出し必須）。 |

---

## 受け入れ条件（実装後の確認）

1. スーパー管理者でコミュニティ一覧から任意コミュニティのスペース管理へ遷移したとき、**最初のフレームから**当該コミュニティのスペースのみが表示される、または **意図したローディング**のみが見える（**全コミュニティ分が一瞬並ばない**）。
2. コミュニティ A のスペース管理を開いたあと **コミュニティ一覧に戻り**、別コミュニティ B のスペース管理を開いたとき、**A のスペース一覧が一瞬も表示されない**（空・ローディング・B の一覧のいずれか）。
3. ブラウザ更新で `#spaces/{slug}` を直打ちした場合も、復元後は **そのコミュニティのスペースのみ**（全件フラッシュなし）。
4. 一般管理者のスペース管理は **従来どおり**動作する。
5. `npm run build` が通る。
6. コミュニティを **短時間に複数回切り替えた**あとでも、**遅い応答がより新しい選択を上書きしない**（最後に選んだコミュニティの一覧のみが安定して表示される）。案 E の世代ガードで担保。

---

## 関連仕様

- [21_コミュニティごとのスペース管理画面](./21_コミュニティごとのスペース管理画面.md)
- [05_管理者ログイン画面](./05_管理者ログイン画面.md) — 管理者・スーパー管理者の役割
- [10_ログインユーザー管理設計](./10_ログインユーザー管理設計.md)

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-04-09 | 初版。スーパー管理者のスペース一覧フラッシュ問題と実装案を整理 |
| 2026-04-09 | 案 A・B・D を `HossiiStoreProvider` / `SpacesScreen` に反映。実装内容セクションを追加 |
| 2026-04-09 | 案 B 補足（**案1**：コミュニティ一覧で `lastScopedCommunityFetchKeyRef` を維持）を仕様に追記。技術的原因 5・推奨組み合わせ 2'・受け入れ 2・実装ステータスを更新 |
| 2026-04-09 | 案1 を `HossiiStoreProvider` に実装（`skipAllTenantFetch` で ref クリアを削除）。実装ステータス・実装内容・受け入れ 2 を更新 |
| 2026-04-09 | 受け入れ条件 2 をローカルで確認。案1 適用後、直前コミュニティのスペース一覧が一瞬表示される問題は解消。**実装ステータス**に確認済み行を追加 |
| 2026-04-16 | 技術的原因 **6・7**、**G5**、案 **E**・案 B 補足 **案2** を文書化。続けて **案2・案 E を `HossiiStoreProvider` に実装**（`lastSpacesFetchTargetRef` / `spacesFetchRequestIdRef`、一覧 `skipAllTenantFetch` 時は世代のみ進行）。推奨 **2''**・案 C 任意、実装ステータス・実装内容・案1記述・受け入れ **6** を更新。原因 4・5 の注記は前項 |
