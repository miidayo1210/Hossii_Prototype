# 76 スマホ投稿ナビとクイック投稿

> **分類:** `[Core / UX]` モバイル下部ナビ「投稿」とクイック投稿パネルの連携  
> **関連:** [06_ナビゲーション・UI構成](./06_ナビゲーション・UI構成.md) / [51_簡単投稿](./51_簡単投稿.md) / [62_スマホのスペース画面](./62_スマホのスペース画面.md) / [56_クリックフィードバックとフローティングパネル](./56_クリックフィードバックとフローティングパネル.md)

> 最終更新: 2026-06-23  
> **ステータス:** ✅ 実装済み

---

## 1. 目的

スマートフォンで下部ナビの **「投稿」** を押したとき、別ページ（`#post`）へ遷移せず、スペース画面（`#screen`）のまま **固定 UI** でクイック投稿パネルを表示する UX とする。

- **縦:** スペースの上に重なる **固定ボトムシート**（下からスライドイン。下ナビは常に見える）
- **横:** 左スペース + 右固定投稿ドック

いずれも **ドラッグ・リサイズ不可**（`FloatingPanelShell` 不使用）。

**意図:** 背景の **ダブルタップ** と同系統の「その場で置く」体験を、ナビからも一発で呼び出せるようにする（[51](./51_簡単投稿.md)）。

---

## 2. 適用範囲

| 条件 | 挙動 |
|------|------|
| **PC（769px 以上）** | 右上メニュー「投稿する」→ クイック投稿パネル（`#post` へは遷移しない） |
| **モバイル + `#screen` 表示中** | 本仕様（クイック投稿パネル） |
| **モバイル + 他画面**（`#comments` / `#account` 等） | 先に `#screen` へ遷移し、続けてパネルを **開く** |
| **訪問モード**（隣人スペース閲覧中） | **開かない**（[44_隣人システム](./44_隣人システム.md)・[51](./51_簡単投稿.md) と同一） |
| **スライドショー表示中** | 開かない |

### 2.1 パネル形状（向き別）

| 条件 | レイアウト | `panelMode` | シェル | 操作 |
|------|------------|-------------|--------|------|
| PC | 右フローティング（幅 25% 目安） | `side` | `FloatingPanelShell`（`storageKey="quickPost.desktop"`） | ドラッグ・リサイズ可 |
| モバイル **縦** | **固定ボトムシート**（高さ 58vh・下ナビ直上） | `bottom` | `mobilePostSheet`（CSS 固定） | **固定**（動かせない） |
| モバイル **横** | **左スペース + 右固定ドック**（幅 `clamp(240px, 36vw, 320px)`） | `side` | `mobilePostSideDock`（CSS 固定） | **固定**（動かせない） |

判定: `mobilePostSplit = isMobile && portrait && quickPostOpen`、`mobilePostLandscapeSplit = max-height: 600px && landscape && quickPostOpen`（`SpaceScreen.tsx`）。

---

## 3. タブ操作の詳細

### 3.1 `#screen` 表示中に「投稿」タブを押す

| 状態 | 結果 |
|------|------|
| クイック投稿パネル **未表示** | URL は `#screen` のまま。パネルを **開く** |
| クイック投稿パネル **表示中** | パネルを **閉じる**（トグル） |
| 下ナビのアクティブ表示 | `#screen` のままのときは **「スペース」タブをアクティブ**とする（「投稿」タブを `#post` 専用アクティブにしない） |

### 3.2 投稿位置

下ナビ・TopRightMenu から開く場合は **座標を渡さない**（[85](./85_スマホ吹き出し配置の中央寄せ調査.md) §4.6）。投稿は `isPositionFixed: false` となり、通常のランダム配置と同じ。

**旧仕様（廃止）:** 中央既定 `{ x: 50, y: 50 }` — モバイルで X 軸中央に帯状集中する原因だった。

### 3.3 他画面から「投稿」タブを押す

1. `navigate('screen')`  
2. マウント完了後（または同一 tick で state 連携後）クイック投稿パネルを **開く**  
3. 訪問モード等で開けない条件のときは `#screen` への遷移のみ

### 3.4 ダブルタップとの関係

- 背景ダブルタップ: 従来どおり **タップ位置** で開く（320ms 遅延・トリプル競合回避は [51](./51_簡単投稿.md)・[63_ダブルクリック感知](./63_ダブルクリック感知.md) のまま）。このときのみ `quickPostPos` を設定し `initialPosition` に渡す。
- 下ナビとダブルタップは **`quickPostOpen` を共有**（排他ではない。両方トグル）。座標 state は **`quickPostPos`**（ダブルタップ専用）。

### 3.5 `#post` フルページへの直接アクセス

- ブックマーク・外部リンクで `#post` に来た場合: **現状どおりフルページ**（本仕様では変更しない）
- 将来、モバイルでは `#post` を `#screen` + パネルへリダイレクトする案は **別チケット**

---

## 4. パネル UI

[51_簡単投稿](./51_簡単投稿.md) の **モバイル固定 UI** 定義に従う。

- モバイル縦: `PostScreen` を `mobilePostSheet` に直接配置（下からスライドイン）
- モバイル横: `mobilePostSideDock` に直接配置
- PC: `FloatingPanelShell` + `PostScreen`（`panelMode="side"`）
- 縦シート下端: 下ナビ 72px + `safe-area-inset-bottom` の直上
- 閉じる: パネル内 ✕、再度「投稿」タブ、背景ダブルタップ（開いているとき）

---

## 5. 実装アーキテクチャ

`quickPostOpen`（表示）と `quickPostPos`（ダブルタップ座標）を `SpaceScreen` 内 state として分離。下ナビ・PC 右上メニューから操作するため **App ↔ SpaceScreen の連携** を使う。

| 層 | 役割 |
|----|------|
| `SpaceScreen` | `forwardRef` + `useImperativeHandle` で `openQuickPost()` / `toggleQuickPost()` / `closeQuickPost()` を公開 |
| `App.tsx` | `screen === 'screen'` 時に ref を保持。`BottomNavBar` に `onPostPress` を渡す。訪問中は Toast |
| `BottomNavBar.tsx` | モバイルかつ `onPostPress` あり → 遷移せずコールバック |
| `TopRightMenu.tsx` | `onPostClick` prop で PC「投稿する」→ クイック投稿 |

---

## 6. 関連ファイル

| ファイル | 変更 |
|----------|------|
| `BottomNavBar.tsx` | モバイル「投稿」の分岐 |
| `App.tsx` | ref / callback 配線 |
| `SpaceScreen.tsx` | imperative API、他画面からの open 要求を受ける |
| `useRouter.ts` | 変更なし（hash は `#screen` のまま） |

---

## 7. 受け入れ条件

- [x] モバイル・`#screen` 中に「投稿」タブ → **ページ遷移なし**で固定 UI に投稿フォーム（縦=ボトムシート / 横=右ドック）
- [x] もう一度「投稿」タブ → パネルが **閉じる**
- [x] `#comments` 等から「投稿」→ **`#screen` + パネル開**
- [x] 訪問モードではパネルが **開かない**（Toast 表示）
- [x] PC でも `#post` へ遷移せずクイック投稿パネルを開く
- [x] モバイル投稿パネルは **ドラッグ・リサイズ不可**（固定ドック）
- [x] パネル下端が BottomNav に **隠れない**
- [x] 下ナビ経路の投稿が **中央 (50,50) に固定されない**（[85](./85_スマホ吹き出し配置の中央寄せ調査.md) §4.6）

---

## 8. 改訂履歴

| 日付 | 内容 |
|------|------|
| 2026-06-02 | 初版: 下ナビ「投稿」→ クイック投稿パネル。App–SpaceScreen 連携方針を記載 |
| 2026-06-02 | 実装完了。PC 右上メニューも統合。訪問中 Toast 追加 |
| 2026-06-02 | 向き別パネル形状（縦=ボトムシート / 横=右サイド）を追記 |
| 2026-06-02 | モバイルを **固定スプリットドック** に変更（`FloatingPanelShell` 廃止・縦=16:9+下 / 横=左+右） |
| 2026-06-02 | モバイル縦を **固定ボトムシート** に変更。横持ち判定を `max-height: 600px` に修正 |
| 2026-06-23 | ナビ経路の中央固定 (50,50) を廃止。`quickPostOpen` / `quickPostPos` 分離（[85](./85_スマホ吹き出し配置の中央寄せ調査.md) §4.6） |
