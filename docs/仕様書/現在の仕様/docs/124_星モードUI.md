# 星モードUI・スペース表示・リロード・参加者アカウント改善

> **分類:** `[Core / Space / StarView / Admin / Routing]`
> **機能名:** 星モード表示改善・いいね同期・参加者アカウント一括発行・hash route reload
> **ステータス:** ✅ 主要項目実装済み / ⚠️ スマホ横向きカスタムモードのみ未対応
> **最終更新:** 2026-07-21
> **対象 main:** `e963ed1`（`origin/main` @ 作業時点）
> **正本の範囲:** スペース画面（星モード・カスタムモード）の表示改善、いいね数同期、管理者による参加者アカウント一括発行、hash 付き URL の reload 保持

---

## 実装ステータス

### 星モード

| 項目 | 状態 | 備考 |
|------|------|------|
| 投稿日表示 | ✅ 実装済み | PR #30 |
| 上側の星の吹き出しを下へ配置 | ✅ 実装済み | y 座標に応じて above / below を切り替え |
| 左右端の配置調整 | ✅ 実装済み | PR #30 |
| PC で星・画像・本文を一回り大きく | ✅ 実装済み | PR #30 |
| スマホ横向きで全星の吹き出しを常時表示 | ✅ 実装済み | タップ不要・コンパクト表示（PR #30） |
| スマホ縦の既存サイズ | ✅ 維持 | PR #30 で縦向きサイズは変更しない |

### カスタムモード

| 項目 | 状態 | 備考 |
|------|------|------|
| スマホ横向きで吹き出しが画面外へはみ出す | ❌ 未対応 | 次の独立タスクとして残す（§残課題） |

### いいね

| 項目 | 状態 | 備考 |
|------|------|------|
| ログ一覧で数が即時更新されない | ✅ 解消 | PR #33 |
| 投稿詳細・星モード・著者タイムラインとの同期 | ✅ 実装済み | PR #33 |
| reload 後に DB の `like_count` と一致 | ✅ 実装済み | PR #33 |
| server 確定値で Store を更新 | ✅ 確定仕様 | optimistic UI 後に server 値で上書き |

### 参加者アカウント

| 項目 | 状態 | 備考 |
|------|------|------|
| 最大 20 件 → 最大 50 件 | ✅ 実装済み | PR #31 |
| 件数を入力して一括発行 | ✅ 実装済み | PR #31 |
| 今回発行分を TSV で一括コピー | ✅ 実装済み | PR #31 |
| revoked slot は再利用しない | ✅ 確定仕様 | PR #31 |
| 長時間時の待機メッセージ | ✅ 実装済み | PR #31 |
| Production migration | ✅ 適用済み | |
| Production Edge Function | ✅ deploy 済み | |
| Production 管理画面での実発行 smoke | ⚠️ 未実施 | 専用テストスペース不足 |

### routing / reload

| 項目 | 状態 | 備考 |
|------|------|------|
| `#comments` reload | ✅ 同じ画面を維持 | PR #32 |
| `#account` reload | ✅ 同じ画面を維持 | PR #32 |
| `#post` reload | ✅ 同じ画面を維持 | PR #32 |
| hash route を保持 | ✅ 実装済み | PR #32 |
| 「スペースが見つかりません」の一瞬表示 | ✅ 解消済み | slug 解決中の not-found フラッシュを抑制 |
| private window での reload | ✅ 確認済み | 同一セッション内で確認 |
| quickLog / quickPost overlay | — 対象外 | URL state ではないため別課題 |

---

## 関連 PR

| PR | 概要 | merge SHA |
|----|------|-----------|
| [#30](https://github.com/miidayo1210/Hossii_Prototype/pull/30) | 星モード表示改善 | `2b46c65` |
| [#31](https://github.com/miidayo1210/Hossii_Prototype/pull/31) | 参加者アカウント 50 件・一括発行 | `dc863c1` |
| [#32](https://github.com/miidayo1210/Hossii_Prototype/pull/32) | space route reload | `a34cba1` |
| [#33](https://github.com/miidayo1210/Hossii_Prototype/pull/33) | like-count 同期 | `b896227` |

---

## 1. 星モード表示改善

### 1.1 投稿日表示

| | |
|---|---|
| **課題（実装前）** | 星モードで投稿した日付が表示されず、いつ投稿されたか分かりにくかった。 |
| **対応仕様** | 星モードの吹き出し内に投稿日を表示する。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #30（`2b46c65`） |
| **確認結果** | 星モード吹き出しに日付が表示される。 |

### 1.2 上側の星の吹き出し配置（above / below 切り替え）

| | |
|---|---|
| **課題（実装前）** | 画面上部に配置された星の吹き出し・画像が上方向にはみ出し、見切れていた。 |
| **対応仕様** | 星の y 座標に応じて吹き出しを `above` / `below` に切り替える。上部付近の星は星の下側に吹き出しを表示する。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #30（`2b46c65`） |
| **確認結果** | 上部の星でも吹き出し・画像が画面内に収まる。 |

### 1.3 左右端の配置調整

| | |
|---|---|
| **課題（実装前）** | 左右端の星の吹き出しが画面外にはみ出すことがあった。 |
| **対応仕様** | 吹き出しの水平位置を viewport 内に収める。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #30（`2b46c65`） |
| **確認結果** | 左右端でも吹き出しが見切れにくい。 |

### 1.4 PC 表示サイズ拡大

| | |
|---|---|
| **課題（実装前）** | PC 画面で星モード表示時、星・画像・本文が小さく 6 件表示だとスカスカに見えた。 |
| **対応仕様** | PC viewport では星・画像・本文フォントを一回り大きくする。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #30（`2b46c65`） |
| **確認結果** | PC で視認性が向上。 |

### 1.5 スマホ横向き：全星の吹き出し常時表示

| | |
|---|---|
| **課題（実装前）** | スマホ横向きの星モードで、星からランダムに吹き出しが出ず星しか見えない状態だった。 |
| **対応仕様** | スマホ横向き（`max-height: 600px` + `orientation: landscape`）では全星の吹き出しを常時表示する。タップ不要。コンパクト表示とする。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #30（`2b46c65`） |
| **確認結果** | 横向きで星と吹き出しが同時に見える。 |

### 1.6 スマホ縦向きサイズ維持

| | |
|---|---|
| **課題（実装前）** | 星モード改善でスマホ縦の既存 UX が変わらないこと。 |
| **対応仕様** | スマホ縦向きの星・吹き出しサイズは従来どおり維持する。 |
| **実装状態** | ✅ 維持 |
| **関連 PR** | #30（`2b46c65`） |
| **確認結果** | 縦向きの既存表示サイズは変更なし。 |

---

## 2. カスタムモード（スマホ横向き）

### 2.1 吹き出しの画面外はみ出し

| | |
|---|---|
| **課題（実装前）** | スマホ横向きのカスタムモードで、吹き出しが画面上下左右からはみ出す。 |
| **対応仕様** | viewport 内に吹き出しを clamp し、はみ出しを防ぐ（§残課題参照）。 |
| **実装状態** | ❌ 未対応 |
| **関連 PR** | —（次の独立タスク） |
| **確認結果** | 星モード（§1）の対応とは別。現状は未修正。 |

**関連コード（調査起点）:** `Tree.tsx` / `SpaceScreen.tsx` / `HossiiFullTextPopover.tsx` / bubble position 関連

---

## 3. いいね数同期

### 3.1 ログ一覧での即時更新

| | |
|---|---|
| **課題（実装前）** | ログ一覧で「いいね」を押しても個数が増えず、reload するとリセットされるように見えた。 |
| **対応仕様** | いいね操作後、ログ一覧の表示数を即時更新する。server から返った確定値で Store を更新する。 |
| **実装状態** | ✅ 解消済み |
| **関連 PR** | #33（`b896227`） |
| **確認結果** | ログ一覧で押下直後に数が変わる。 |

### 3.2 複数ビュー間の同期

| | |
|---|---|
| **課題（実装前）** | 投稿詳細・星モード・著者タイムライン間でいいね数が一致しない。 |
| **対応仕様** | 同一投稿の `likeCount` を Store 経由で各ビューに反映する。外部更新（初回フェッチ等）でも local state を同期する。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #33（`b896227`） |
| **確認結果** | ログ一覧・投稿詳細・星モード・著者タイムラインで数が揃う。 |

### 3.3 reload 後の DB 一致

| | |
|---|---|
| **課題（実装前）** | reload 後に UI のいいね数が DB の `like_count` と一致しない。 |
| **対応仕様** | reload 後は server から取得した `like_count` を正とする。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #33（`b896227`） |
| **確認結果** | reload 後も DB 値と一致。 |

### 3.4 server 確定値による Store 更新（確定仕様）

| | |
|---|---|
| **課題** | optimistic UI と server 値の競合。 |
| **対応仕様** | いいね RPC の応答（server 確定値）で Hossii Store を更新し、全購読ビューに伝播する。 |
| **実装状態** | ✅ 確定仕様 |
| **関連 PR** | #33（`b896227`） |
| **確認結果** | server 値が最終的な表示源。 |

---

## 4. 参加者アカウント一括発行

### 4.1 発行上限 50 件

| | |
|---|---|
| **課題（実装前）** | 参加者アカウントの一括発行上限が 20 件で足りない場面があった。 |
| **対応仕様** | 1 回の発行上限を最大 50 件とする。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #31（`dc863c1`） |
| **確認結果** | 管理画面で 50 件まで指定可能。 |

### 4.2 件数入力による一括発行

| | |
|---|---|
| **課題（実装前）** | 何件発行するか入力して、その数だけ一括発行したい。 |
| **対応仕様** | 発行件数を入力し、指定件数分を一括発行する。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #31（`dc863c1`） |
| **確認結果** | 入力件数どおり発行される。 |

### 4.3 今回発行分の TSV 一括コピー

| | |
|---|---|
| **課題（実装前）** | 発行したアカウント情報をまとめてコピーしたい。 |
| **対応仕様** | 今回発行分を TSV 形式で一括コピーできる。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #31（`dc863c1`） |
| **確認結果** | 「今回発行したアカウントをコピーしました」等の UI で TSV コピー可能。 |

### 4.4 revoked slot 非再利用（確定仕様）

| | |
|---|---|
| **課題** | 失効済み slot の再利用による ID 衝突・混乱。 |
| **対応仕様** | revoked された slot は再利用しない。新規 slot を割り当てる。 |
| **実装状態** | ✅ 確定仕様 |
| **関連 PR** | #31（`dc863c1`） |
| **確認結果** | 仕様どおり revoked slot は再割当しない。 |

### 4.5 長時間発行時の待機メッセージ

| | |
|---|---|
| **課題（実装前）** | 大量発行時に処理中であることが分かりにくい。 |
| **対応仕様** | 長時間かかる場合に待機メッセージを表示する。 |
| **実装状態** | ✅ 実装済み |
| **関連 PR** | #31（`dc863c1`） |
| **確認結果** | 待機中フィードバックが表示される。 |

### 4.6 Production 反映

| | |
|---|---|
| **課題** | migration・Edge Function の Production 適用。 |
| **対応仕様** | Production DB migration 適用、Edge Function deploy。 |
| **実装状態** | ✅ migration 適用済み / ✅ Edge Function deploy 済み |
| **関連 PR** | #31（`dc863c1`） |
| **確認結果** | Production 反映済み。管理画面での実発行 smoke は ⚠️ 専用テストスペース不足のため未実施。 |

---

## 5. routing / reload（hash route 保持）

### 5.1 hash 付き URL の reload 保持

| | |
|---|---|
| **課題（実装前）** | ログ一覧（`#comments`）・投稿（`#post`）・アカウント（`#account`）から reload するとスペース画面（`#screen`）に戻ってしまう。private window では毎回「おかえり」画面になることもあった。 |
| **対応仕様** | slug URL 解決後も hash route（`#comments` / `#account` / `#post` 等）を保持する。reload しても同じ画面を維持する。 |
| **実装状態** | ✅ 解消済み |
| **関連 PR** | #32（`a34cba1`） |
| **確認結果** | Production `/s/machizukuri` で `#comments` / `#account` / `#post` の reload 確認済み。 |

### 5.2 「スペースが見つかりません」の一瞬表示

| | |
|---|---|
| **課題（実装前）** | reload 中に slug 解決が完了する前に not-found 画面が一瞬表示される（過去にも修正したが再発）。 |
| **対応仕様** | slug 解決・アクセスチェック中は not-found を表示しない（`isSlugUrlStillResolving` 等）。 |
| **実装状態** | ✅ 解消済み |
| **関連 PR** | #32（`a34cba1`） |
| **確認結果** | reload 時に not-found フラッシュなし。 |

### 5.3 private window での reload

| | |
|---|---|
| **課題（実装前）** | private window で reload すると毎回「おかえり」画面になる。 |
| **対応仕様** | 同一セッション内の reload では参加済み状態を維持し、hash route も保持する。 |
| **実装状態** | ✅ 確認済み |
| **関連 PR** | #32（`a34cba1`） |
| **確認結果** | 同一セッション内 reload で確認済み。 |

### 5.4 quickLog / quickPost overlay（対象外）

| | |
|---|---|
| **課題** | overlay 状態は URL に載らない。 |
| **対応仕様** | 本仕様（124）の対象外。URL state ではないため別課題とする。 |
| **実装状態** | — 対象外 |
| **関連 PR** | — |
| **確認結果** | 今回の PR #32 スコープ外。 |

---

## 6. 残課題

### 6.1 スマホ横向き・カスタムモードの吹き出し配置

**現状:**

- 吹き出しが画面上下左右からはみ出す可能性がある
- 関連: `Tree.tsx` / `SpaceScreen.tsx` / `HossiiFullTextPopover.tsx` / bubble position 関連
- 星モード（§1）の対応とは別タスク

**次回検討:**

- viewport 内への clamp
- 上下左右の表示方向
- 吹き出し最大幅・最大高
- 画像サイズ
- 本文行数 clamp
- 横スクロール防止
- 複数吹き出しの重なり

**今回のスコープ:** 実装しない（仕様整理のみ）。

### 6.2 Production 参加者アカウント実発行 smoke

- migration / Edge Function は Production 反映済み
- 管理画面からの実発行 end-to-end smoke は、専用テストスペース不足のため ⚠️ 未実施

---

## 7. 主な変更ファイル（参照）

| 区分 | ファイル |
|------|----------|
| 星モード | `StarView.tsx`, `StarHoverPreview.tsx`, `Tree.tsx`, `HossiiFullTextPopover.tsx` |
| いいね | `HossiiStoreProvider.tsx`, `LogListBody.tsx`, `PostDetailModal.tsx`, `Tree.tsx`, `AuthorTimelineModal.tsx` |
| 参加者アカウント | `ParticipantAccountsTab.tsx`, bulk issue RPC / Edge Function |
| routing | `App.tsx`, `useRouter.ts`, `slugUrlResolution.ts`, `spaceScreenRoute.ts` |

---

## 改訂履歴

| 日付 | 内容 |
|------|------|
| 2026-07-21 | 要望列挙から実装ステータス・確定仕様・残課題形式へ全面整理。PR #30–#33 の実装反映。 |
