カスタムモード｜吹き出し同士を「糸」でつなぐ機能

分類: [Core / Space / Custom Mode / Connection UI]
機能名: 吹き出し（投稿）の関連づけ・糸のつながり
調査基準: main HEAD `12cab9a`（PR #93 マージ時点）  
Production DB 正本: PR #53〜#60 + Quest 5 migration（reason / Type A RLS）— §B.1。**Type B migration のみ未適用**（§B.4）
関連仕様: [84_スペース表示モード拡張](../84_スペース表示モード拡張.md)

**ラベル**


| 記号  | 意味                           |
| --- | ---------------------------- |
| ✅   | 現在実装済み（Production 正本 = §B.1） |
| 🎯  | リリース時目標（未実装）                 |
| 📋  | 追加予定・未実装                     |
| 🔍  | 調査が必要                        |
| ⚠️  | 未確定                          |


---

## 仕様書構成（A〜H）


| ブロック  | 内容         | ラベル | 本文                  |
| ----- | ---------- | --- | ------------------- |
| **A** | 用語定義       | —   | [§A](#a-用語定義)       |
| **B** | 現在実装済みの仕様  | ✅   | [§B](#b-現在実装済みの仕様)  |
| **C** | 現状 UI の課題  | 事実  | [§C](#c-現状-ui-の課題)  |
| **D** | リリース時の目標仕様 | 🎯 / ✅ | [§D](#d-リリース時の目標仕様) — Type A/reason ✅ / Type B v1 main ✅ |
| **E** | 追加予定仕様     | 📋  | [§E](#e-追加予定仕様)     |
| **F** | 未確定事項      | ⚠️  | [§F](#f-未確定事項)      |
| **G** | 実装前調査      | 🔍  | [§G](#g-実装前調査)      |
| **H** | Phase 案    | 計画  | [§H](#h-phase-案)    |


**読み方**

- **§B.1 のみ**が Production 上の現行仕様。**D / E で上書きしない。**
- **§D** = リリースで目指す connection 体験（Type A / B、権限、reason 等）。
- **§E** = リリース後〜拡張の UX（Hossii ハンドル、Star 星座線等）。
- 旧 §1〜§33 は §B.2 参照用。B.1 と矛盾する場合 **B.1 を優先**。

**リリース時目標 vs 追加予定（原則）**

| ラベル | 含む機能 |
|--------|----------|
| 🎯 §D | guest Type B（**v2**）、Type B Production 公開 |
| ✅ §B | Type A / reason / connection UX 強化 / Type B v1（authenticated）— **main ✅** |
| 📋 §E | Hossii ハンドル drag 移設、接続成立時の一時引力、connection 数 Bubble 倍率、Star 星座線、関係から並べ直す |
| ⚠️ | Space Hossii handle drag 移設、guest Type B identity — 比較案 §F.2 / §F.13 |

**実装・デプロイの区別（4 層）**

| 層 | 状態 |
|----|------|
| **main コード** | Quest 5（Type A / reason）+ connection UX 強化 + **Type B v1** — **merge 済み**（§B.1 / §B.3） |
| **dev DB** | `20260722100000` + `20260723140000` + `20260723153000` + `20260724120000` — **適用済み** |
| **Production DB** | base + reason + Type A RLS — **適用済み**。`20260724120000`（Type B RPC）— **未適用** |
| **Production frontend deploy** | PR #53〜#60 基準の deploy は §B.1 に記載。**Quest 5 以降の frontend deploy 状態は本書更新時点で未確認**（断定しない） |

**Production 公開 — 残タスク（§B.4）**: Type B migration 適用 → frontend deploy → smoke QA |

---



## A. 用語定義



### A.1 体験・UI 用語


| 用語                 | 定義                                         | コード / DOM                                                                |
| ------------------ | ------------------------------------------ | ------------------------------------------------------------------------ |
| **Hossii（キャラクター）** | スペース内を浮遊するキャラクター。**投稿そのものではない**            | `HossiiLive`（Space Hossii）、`MyHossiiLayer` / `MyHossiiAvatar`（My Hossii） |
| **吹き出し**           | ユーザー投稿を表示する Bubble UI。**connection の操作対象** | `Tree.tsx` `Bubble`、`data-hossii-id`                                     |
| **星**              | Star モードにおける投稿の表示表現                        | `StarView` 等。同一 `hossiis` レコード                                           |
| **糸**              | `hossii_connections` に保存された connection の視覚表現（Bubble モード） | `ConnectionOverlay`（SVG）                                                 |
| **仮糸**             | Type B compose 中、起点吹き出しから新規吹き出し予定位置への**破線 preview**。`hossii_connections` 確定前 | `TypeBProvisionalThread`（SVG） |
| **星座線**            | Star モードにおける connection の視覚表現              | 📋 未実装（§E.4）                                                             |


**同一投稿レコード**はモードにより表示が変わる:

- `PresentationMode === 'custom'` → **吹き出し**
- Star モード（`renderAsStar`）→ **星**



### A.2 データ・コード名称（改名しない）


| 名称                   | 意味                              |
| -------------------- | ------------------------------- |
| `hossiis`            | 投稿エンティティ（DB テーブル）               |
| `hossii_connections` | 2 つの `hossiis.id` 間の connection |
| `selectedBubbleId`   | 選択中吹き出しの `hossiis.id`           |
| `Hossii` 型 / hook 名  | 歴史的命名。**キャラクターと混同しない**          |


**表記例**

- UI: 吹き出し同士を糸でつなぐ
- データ: `hossiis` レコード間を `hossii_connections` で接続する



### A.3 修正した混同表現


| 避ける             | 使う                         |
| --------------- | -------------------------- |
| 投稿された Hossii    | 吹き出し / 投稿                  |
| Hossii 同士をつなぐ   | 吹き出し同士をつなぐ                 |
| Hossii を選んで糸を作る | 吹き出しを選んで糸を作る               |
| Hossii 選択       | 吹き出し選択（`selectedBubbleId`） |


泳ぐキャラクターは **キャラクター Hossii** / **Space Hossii** / **My Hossii** と明示。

### A.4 その他（§B.2 §3 参照）

- `PresentationMode 'custom'` ≠ `SpaceModeId 'custom'`
- strength: ほのか / やわらか / しっかり（`soft` / `medium` / `strong`）

---



## B. 現在実装済みの仕様

> **§B.1** = Production DB 正本（base + reason + Type A RLS）+ Production frontend deploy 基準（PR #53〜#60）。  
> **§B.3** = main コード追加分（connection UX 強化 + Type B v1）。Production frontend / Type B migration 未反映。  
> **§B.4** = Type B Production 未反映の公開手順。  
> **§D / §E** は目標・拡張枠（B.1 で上書きしない）。



### B.1 Production 正本（PR #53〜#60）



#### Production 反映


| 項目              | 事実                                                                                            |
| --------------- | --------------------------------------------------------------------------------------------- |
| Production コード deploy 基準 | PR #53〜#60 まで（HEAD `a929fa4` 相当）。**以降の deploy 状態は未確認** |
| main HEAD | `12cab9a` — Quest 5 + connection UX + Type B v1 を含む |
| 最終 merge（B.1 範囲） | **PR #60** `feat/connection-pull-integration` |
| DB migration（Production 適用済み） | `20260722100000`（base）+ `20260723140000`（reason）+ `20260723153000`（Type A RLS） |
| DB migration（Production 未適用） | `20260724120000_add_type_b_atomic_create.sql`（Type B RPC）— §B.4 |




#### PR 対応表


| PR  | 内容                                                          | 状態  |
| --- | ----------------------------------------------------------- | --- |
| #53 | `hossii_connections` テーブル + RLS + trigger                   | ✅   |
| #54 | SVG overlay 描画基盤 + fetch hook                               | ✅   |
| #55 | コンパクト操作メニュー + 作成/編集/解除 + つながり一覧                             | ✅   |
| #56 | 本仕様書（126）初版                                                 | ✅   |
| #57 | RLS coverage 検証 script                                      | ✅   |
| #58 | pull-to-reveal foundation（`useConnectionPullInteraction` 等） | ✅   |
| #59 | `useSpaceConnectionIntegration` テスト                         | ✅   |
| #60 | pull を `SpaceScreen` / `BubbleActionMenu` / `Tree` へ統合      | ✅   |




#### DB / RLS / API（実装済み）


| 層          | 実装                                                                                                                                |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------- |
| テーブル       | `public.hossii_connections` — `space_id`, `pane_id`, `source_hossii_id`, `target_hossii_id`, `strength`, `created_by`, timestamps |
| 制約         | 自己接続禁止、`UNIQUE (space_id, pane_id, source, target)`、FK cascade                                                                    |
| trigger    | `guard_hossii_connection_row` — ID 正規化（lex sort）、endpoint 検証、archived write 拒否、`created_by := auth.uid()`                         |
| RLS SELECT | `can_access_space` + 両端 `hossii_connection_endpoint_readable`                                                                     |
| RLS write  | INSERT: `can_create_hossii_connection`（active member / personal owner / admin）。UPDATE/DELETE: `created_by = auth.uid() OR admin`（#65） |
| reason 列  | `reason_text` / `reason_emoji`（#64）— Production 適用済み |
| クライアント API | `hossiiConnectionsApi.ts` — `fetchConnections` / `createConnection` / `updateConnectionStrength` / `updateConnectionReason` / `deleteConnection` |
| fetch gate | `shouldFetchHossiiConnections` — gate 成立 + `spaceId` / `paneId` 非空時のみ fetch                                                       |
| 状態         | `useHossiiConnections` hook → `SpaceScreen` local。`HossiiStore` **不使用**                                                           |




#### 吹き出し間 connection（実装済み）

- 対象: 同一 `space_id` + 同一 `pane_id` の 2 つの `hossiis.id`
- 方向: なし（INSERT 前に lex sort 正規化）
- 表示中のみ: `filteredHossiis` / `visibleHossiiIds` に含まれる端点のみ描画・操作



#### strength 3 段階（実装済み）


| DB / TS 値 | UI 見出し | 説明         |
| --------- | ------ | ---------- |
| `soft`    | ほのか    | なんとなく近いかも  |
| `medium`  | やわらか   | 関係がありそう    |
| `strong`  | しっかり   | かなりつながっている |


- 作成時デフォルト: `medium`
- 描画: `getConnectionStrokeStyle(strength)` — 太さ・opacity の差（色だけでは区別しない）



#### ✦N（実装済み）

- 非選択 Bubble 右上 `✦{N}`（`.connectionBadge`、`z-index: 22`、`pointer-events: none`）
- N = `buildConnectionBadgeCounts` の **1-hop 件数**（active Pane + visible のみ）
- **選択中 Bubble には ✦N を出さない**（overlay 1-hop 表示に任せる）



#### 1-hop 表示（実装済み）

- `shouldShowConnectionOverlay`: gate 成立 **かつ** `selectedBubbleId != null`
- `filterVisibleConnections`: 選択 root から **1 階層のみ**、同一 Pane、visible 端点のみ
- SVG: `ConnectionOverlay` + `useConnectionOverlayGeometry`（rAF + `getBoundingClientRect`）



#### つながり一覧（実装済み）

- メニュー **「つながり N」** → `ConnectionListPopover`
- データ: `overlayInputs.connections` 正本（**再 fetch なし**）
- 各行: 接続先本文プレビュー + strength ラベル
- N=0 のときメニュー項目非表示



#### connection CRUD（実装済み）


| 操作   | UI                              | API                        | 権限（Production DB + main UI）       |
| ---- | ------------------------------- | -------------------------- | -------- |
| 作成   | メニュー「つないでみる」→ 相手選択 → 強さ → 「つなぐ」 | `createConnection`         | active member / personal owner / admin（#65 RLS + #68 UI gate） |
| 強さ変更 | 糸 click → 「強さを変える」              | `updateConnectionStrength` | 本人 or admin |
| reason 変更 | 糸 edit → reason 折りたたみ           | `updateConnectionReason`   | 本人 or admin |
| 解除   | 糸 click → 「糸をほどく」→ 確認           | `deleteConnection`         | 本人 or admin |


- 状態機械: `useConnectionEditor`（`idle` / `pickingTarget` / `pickingStrength` / `saving` / `editing` / `deleting` / `error`）
- 成功時のみ `refetch()`。失敗時 UI rollback（§24）



#### pull-to-reveal（実装済み — ハンドル位置はメニュー内）

**事実: pull ハンドルは Bubble 本体・結び目・キャラクター Hossii 上にはない。**`BubbleActionMenu` **portal 内の ✦ ボタンのみ。**


| 項目      | 実装                                                                                                  |
| ------- | --------------------------------------------------------------------------------------------------- |
| コンポーネント | `BubbleActionMenu.tsx` — `data-connection-pull-handle`、`aria-label="つながりを引っ張る"`                     |
| 表示条件    | ① 対象 Bubble **選択中** ② `actionMenuOpen === true`（Bubble 再クリックでメニュー open）③ `directPeerIds.length ≥ 1` |
| 操作起点    | ✦ の `onPointerDown` のみ（Bubble 本体 drag とは排他）                                                         |
| hook    | `useSpaceConnectionPull` + `useConnectionPullInteraction`                                           |
| 効果 DOM  | source + direct peer の `[data-hossii-id]` Bubble に CSS 変数適用                                         |
| 星粒 UI   | pull 中、**メニュー内** ✦ 横に 1〜3 粒（progress tier）+ **direct peer 吹き出し**に 2-hop star marker（`connectionTwoHopStars.ts`） |
| mobile  | **portrait / landscape とも pull 入口 UI 非表示**（`shouldShowConnectionPullHandles`）。PC viewport のみ |




#### 120px / 36px clamp（実装済み）


| 定数                               | 値       | 用途                                |
| -------------------------------- | ------- | --------------------------------- |
| `DEFAULT_MAX_PULL_DISTANCE_PX`   | **120** | source の `--pull-x/y` 上限          |
| `DEFAULT_CONNECTED_FOLLOW_RATIO` | 0.35    | peer shift 計算比率                   |
| `MAX_CONNECTED_SHIFT_PX`         | **36**  | peer の `--connected-shift-x/y` 上限 |


- `left` / `top` / DB `positionX` / `positionY` は **変更しない**（transform + CSS 変数のみ）



#### 2-hop 星粒（実装済み）


| 項目                        | 状態                                                              |
| ------------------------- | --------------------------------------------------------------- |
| 2-hop **糸** SVG           | **描画しない**（仕様どおり）                                                |
| pull 中メニュー ✦ 横星粒       | **実装済み** — `computeTwoHopStarParticleCount(progress)` |
| pull 中 peer 吹き出し marker | **実装済み** — `data-connection-two-hop-stars`（1〜3）。`buildDirectPeerTwoHopStarCounts` |
| 件数テキスト / 展開 UI         | **未実装**（§C） |




#### position 非保存（実装済み）

- pull: CSS 変数 + `transform: translate(...)` のみ
- pull 中: `handlePositionSave` / connection API **呼ばれない**
- pointer up / cancel / Esc / blur で CSS 変数クリア



#### reduced-motion（実装済み）

- `usePrefersReducedMotion` hook
- JS: peer shift **0**、`glow` 上限 `REDUCED_MOTION_GLOW_CAP = 0.25`、星粒 **常に 1**
- CSS: `@media (prefers-reduced-motion: reduce)` で pull peer `transition: none` 等



#### gate（実装済み）

```typescript
isConnectionsContextEnabled =
  viewMode !== 'slideshow'
  && !renderAsStar
  && presentationMode === 'custom'
  && layoutMode !== 'byAuthor'
```

- fetch 追加: `spaceId.length > 0 && paneId.length > 0`
- pull 追加: `directPeerIds.length > 0`、editor phase ∉ `{pickingTarget, pickingStrength, saving, editing, deleting, error}`、Bubble drag/resize なし
- pull 入口 UI: `shouldShowConnectionPullHandles` — **mobile portrait / landscape とも非表示**（PC のみ）
- `renderAsStar`: **mobile portrait のみ** Star 固定 → **mobile landscape custom では吹き出し描画**（Type A / Type B 有効）



#### 権限（Production DB + main UI）


| 操作                           | クライアント                                                           | DB                           |
| ---------------------------- | ---------------------------------------------------------------- | ---------------------------- |
| Type A CRUD                  | `typeAConnectionWriteGate` / `canEditTypeAConnection`（#68）        | #65 participant RLS          |
| 閲覧（overlay / ✦N / 一覧 / pull） | gate 内なら **participant / guest / archived も可**                   | SELECT RLS                   |
| Type B create                | §B.3 — authenticated member のみ（guest ❌ v2）                        | Type B RPC 未適用（Production） |
| Bubble 配置編集                  | `bubbleEditPermission`                                           | —                            |
| 糸 CRUD                       | `bubbleEditPermission` **と独立**                                   | participant / admin RLS      |




#### reset 条件（実装済み）

**connection 編集状態**


| トリガー                                              | 挙動                                                                  |
| ------------------------------------------------- | ------------------------------------------------------------------- |
| 背景クリック / deselect                                 | `resetConnectionState`（saving 中は block）                             |
| Esc                                               | 一覧 open → 一覧 close。editor 非 idle → editor reset。それ以外 → bubble reset |
| Pane 切替                                           | 一覧 close + editor reset（saving 中 block）                             |
| `presentationMode` / `layoutMode` / `viewMode` 変更 | editor reset                                                        |
| `layoutMode → byAuthor` / `viewMode → slideshow`  | editor reset                                                        |
| `selectedBubbleId` 解除                             | editor reset（idle/error 以外、saving 中 block）                          |
| 一覧 open 中に N=0 化                                  | 一覧 auto close                                                       |


**pull 状態**


| トリガー                  | 挙動                                          |
| --------------------- | ------------------------------------------- |
| pointer up / cancel   | CSS 変数クリア                                   |
| Esc / window blur     | 同上                                          |
| `pullEnabled → false` | 即 reset（gate 解除、editor phase、drag/resize 等） |




#### overlay reason tooltip（main ✅ — §B.3）

- 糸 hit path hover → `formatConnectionReasonDisplay(reasonText, reasonEmoji)` tooltip（viewport clamp — #80）
- 作成者 / 日時コンパクト詳細は **未実装**（§D.1）

#### Space Hossii connection handle — Phase 1 click（main ✅ — §B.3）

- **キャラクター Hossii**（Space Hossii）横 ✦ — click で 1-hop 糸強調 toggle（`aria-label="つながりを見る"` — #84）
- PC viewport のみ。mobile / Star / slideshow / byAuthor では非表示
- drag → pull 移設は **未実装**（§E.1 Phase 2）

#### Production 未反映（§B.3 / §B.4）

- **Type B v1**（main コード ✅ / Production RPC ❌ — §B.4）
- connection UX 強化の frontend deploy 状態 — **未確認**

#### 引き続き未実装（§C / §E）

- 吹き出し上 **結び目の直接 drag**（§16 当初 UX 案）
- mobile **pull** 入口（意図的に非対応 — §C13）
- Star **星座線**（§E.4）
- 2-hop 件数テキスト / 展開 UI



### B.3 main コード追加分（Production frontend / Type B migration 未反映）✅

> **main に merge 済み。** Production DB には Quest 5 migration（reason / Type A RLS）**適用済み**（§B.1）。  
> **Production 未反映**: Type B migration（§B.4）+ Quest 5 以降 frontend deploy（**deploy 状態は未確認**）。

#### PR 対応表（Quest 5 — Production DB 反映済み）


| PR | 内容 | main | dev DB | Production DB |
| --- | --- | --- | --- | --- |
| #63 | `activeSpaceMembershipStatus` / `retryActiveSpaceMembershipJoin` 公開（§F.14 案 2 基盤） | ✅ | — | — |
| #64 | `reason_text` / `reason_emoji` 列 + CHECK + 正規化 trigger | ✅ | ✅ | ✅ |
| #65 | Type A participant INSERT RLS + 本人 UPDATE/DELETE RLS | ✅ | ✅ | ✅ |
| #66 | reason typed API（`updateConnectionReason` partial update 含む） | ✅ | — | — |
| #67 | membership retry guard（error 時のみ retry、in-flight dedupe） | ✅ | — | — |
| #68 | Type A UI gate + membership 状態 UI + edit gate | ✅ | — | — |
| #69 | reason create/edit UI（`ConnectionStrengthPopover` 折りたたみ） | ✅ | — | — |

#### PR 対応表（connection UX 強化）


| PR | 内容 | main |
| --- | --- | --- |
| #79 | Type A editor 中 overlay 切替ガード | ✅ |
| #80 | overlay 糸 hover → reason tooltip | ✅ |
| #81 | create 不可時メニュー blocked reason 短文 | ✅ |
| #82 | connection fetch retry 並列抑止 | ✅ |
| #84 | Space Hossii ✦ handle（click → 1-hop 強調） | ✅ |
| #86 | mobile landscape custom Bubble + Type A | ✅ |
| #87 | mobile 全 viewport pull handle 非表示 | ✅ |
| #89 | mobile landscape editor keyboard fix | ✅ |



#### Type A — DB / RLS（#65）✅


| 項目 | 実装 |
|------|------|
| helpers | `is_active_space_member`, `is_personal_space_owner`, `can_create_hossii_connection` |
| INSERT | active member / personal owner / `is_hossii_connection_admin` |
| UPDATE/DELETE | `created_by = auth.uid() OR is_hossii_connection_admin(space_id)` |
| guard trigger | 変更なし（`created_by` 強制、archived 拒否、endpoint 検証等） |
| migration | `20260723153000_add_hossii_connection_type_a_participant_rls.sql` |
| 検証 | `scripts/verify-hossii-connections.mjs` + RLS coverage script |



#### Type A — UI gate（#63, #67, #68）✅


| 主体 / 状態 | 作成 UI | 根拠 |
|-------------|---------|------|
| active member | 「つないでみる」 | `activeSpaceMembershipStatus === 'active'` |
| personal owner | bypass（membership 待たない） | `isViewingOwnPersonalSpace` |
| community admin / super admin | bypass | `canManageSpace` |
| guest | write UI 非表示 | `currentUser === null` |
| idle / joining | 「参加確認中…」、create 不可 | `membership_joining` |
| error | 「もう一度試す」、create 不可 | `membership_error` + `retryActiveSpaceMembershipJoin` |
| none | write UI 非表示 | `membership_none` |
| archived | 全員 create/edit 不可 | `isContentArchived` |

- pure gate: `typeAConnectionWriteGate.ts` — `evaluateTypeAConnectionWriteGate` / `canEditTypeAConnection`
- 配線: `HossiiStore` → `SpaceScreen` → `useSpaceConnectionIntegration` → `BubbleActionMenu`
- **read 経路**（overlay / ✦N / pull / 一覧 / fetch）は membership 状態に**非依存**（変更なし）



#### Type A — 本人 connection 編集（#65, #68）✅


| 操作 | 一般参加者 | admin / super admin |
|------|-----------|---------------------|
| 自分の connection UPDATE/DELETE | ✅（`createdBy === uid`） | ✅ |
| 他人の connection | ❌（overlay click 無効） | ✅ |
| `createdBy === null` | ❌ | admin のみ |
| archived | ❌ | ❌ |

- strength / reason / delete は **同一 edit gate**（`canEditTypeAConnection` → `ConnectionEditorPortals`）



#### reason — DB（#64）✅


| 列 / 制約 | 内容 |
|-----------|------|
| `reason_text` | `varchar(50)` nullable。空白のみ → NULL（trigger） |
| `reason_emoji` | 8 プリセットのみ（CHECK）: 💡 🔗 🌱 💬 ↔️ 🎯 ❤️ ❓ |
| 改行 | CHECK で拒否（`\n\r\x0B\x0C`） |
| migration | `20260723140000_add_hossii_connection_reason_columns.sql` |



#### reason — API（#66）✅


| API | 内容 |
|-----|------|
| `createConnection` | reason 省略可。Production 列適用済み（§B.1） |
| `updateConnectionReason` | partial update — `undefined`=省略、`null`=明示クリア |
| `updateConnectionStrength` | reason 列に触れない（後方互換） |
| validation | `connectionReasonValidation.ts` — 50 字 / 改行 / preset emoji |
| fetch | 列なし行は `reasonText` / `reasonEmoji` = `null` |



#### reason — UI（#69）✅


| 項目 | 実装 |
|------|------|
| 配置 | `ConnectionStrengthPopover` 内、strength の下**折りたたみ** |
| create / edit | emoji preset 1 つ + `reason_text` 50 字 |
| reason-only 編集 | strength 変更なしで reason のみ更新可 |
| 個別 clear | text / emoji をそれぞれ NULL 化可 |
| overlay tooltip | 糸 hover → emoji + text ✅（#80） |
| 一覧・作成者・日時詳細 | **未実装** — §D.1 |



#### Production 公開順序

**完了 ✅**

1. Production DB: `20260723153000`（Type A RLS）
2. Production DB: `20260723140000`（reason 列）
3. （必要に応じて）Quest 5 frontend deploy — **deploy 状態は未確認**

**残り 🎯 — §B.4 参照**

1. Production DB: `20260724120000`（Type B RPC）
2. frontend deploy（Type B UI + connection UX 強化分）
3. smoke QA




#### Type B v1 — main 実装 ✅（Production RPC 未適用 — §B.4）

| PR | 内容 | main | dev DB | Production DB |
| --- | --- | --- | --- | --- |
| #85 | near-origin placement util（`computeTypeBNearOriginPlacement`） | ✅ | — | — |
| #88 | atomic RPC migration + dev verifier | ✅ | ✅ | ❌ |
| #90 | typed client API（`typeBCreateApi.ts`） | ✅ | — | — |
| #91 | write gate + menu「つなげて作る」 | ✅ | — | — |
| #92 | compose flow + 仮糸 + PostScreen 配線 | ✅ | — | — |
| #93 | idempotency key を `crypto.randomUUID()` に統一 | ✅ | — | — |

| 項目 | v1 仕様（main） |
|------|----------------|
| 利用者 | **authenticated member 限定**（active / personal owner / admin bypass）。**guest は v2 🎯** |
| 入口 | 吹き出し操作メニュー「**つなげて作る**」 |
| prompt | **「ここから、何が広がった？」** |
| strength | RPC 固定 **`medium`**。UI 選択なし |
| reason | **作成時入力なし**。事後編集は Type A 糸 edit 経由 |
| 起点 | `origin_hossii_id` = 選択中吹き出し |
| 仮糸 | compose 中のみ破線 preview（`TypeBProvisionalThread`） |
| atomic RPC | `create_type_b_connected_hossii` |
| idempotency | compose 開始時 `idempotencyKey = crypto.randomUUID()` — retry まで固定 |
| newHossiiId | compose 開始時 `newHossiiId = generateId()` — retry まで固定 |
| success | hossiis refetch → connections refetch → 新吹き出し選択 |
| error | PostScreen バナー +「もう一度試す」（draft / key / newHossiiId 維持） |
| cancel | Esc / 背景 tap / pane・mode 切替 / portrait 回転 |
| mobile landscape | Type A と同様 **有効**（custom Bubble） |
| Type A 排他 | 相互 block（`isTypeAEditorBlockingTypeB` / `isTypeBEditorBlockingTypeA`） |
| blocked UI | Type B は gate 不可時 **ボタン非表示**（Type A blocked 短文なし） |

#### non-blocking follow-up（改善余地）

**strength 更新成功 → reason 更新失敗時の editor stale**

- edit 保存は strength → reason の**順次 API**。strength 成功後に reason が失敗すると、editor 内の `editingConnection.strength` が DB とずれる可能性がある
- retry 時に strength 更新が**再送**される（冪等だが無駄）。DB 破損はない
- 改善案: partial success 後の editor 状態再同期、または transaction 的 UI 設計 — **未着手**




### B.4 Type B — Production 未反映 🎯

| 層 | 状態 |
|----|------|
| main コード | Type B v1 **merge 済み**（§B.3） |
| dev DB | `20260724120000` **適用済み** |
| Production DB | `20260724120000` **未適用** |
| Production frontend | Type B UI deploy — **未確認** |

**Production 公開手順（残タスク）**

1. Production DB へ `20260724120000_add_type_b_atomic_create.sql` を適用（明示 promote 後）
2. frontend deploy（Type B UI + 未 deploy の connection UX 強化分）
3. smoke QA — `scripts/verify-type-b-atomic-create.mjs` + 手動 QA

RPC 不在時: client は `RPC_NOT_AVAILABLE` を返し UI にエラー表示。

### B.2 詳細仕様（旧 §1〜§33）

設計意図・受入条件の参照用。**実装の正本は §B.1**。将来目標は §D / §E。

1. 背景

スペースに投稿が増えてくると、投稿同士の間に、

なんとなく近そう
同じテーマに関係していそう
一方がもう一方のきっかけになっていそう
今は説明できないが、一緒に見ておきたい

といった、明確に分類しきれない関係が見えてくる。

これらをタグやフォルダで整理するのではなく、カスタムモード上で投稿同士を自由に動かしながら、仮の関係として糸でつないでおける機能を追加する。

投稿が蓄積された空間を触りながら、パズルの一部が少しずつ組み合わさるように、新しい関係やまとまりを発見できる体験を目指す。

1. コンセプト

ふわっと、つながる。

吹き出しは、宇宙や水面のような空間に、ぷかぷか浮かぶ小さなかけらとして存在する。

気になる2つの吹き出しを糸でつなぐと、互いを少しだけ意識するようになる。

強く固定したり、ひとつのグループに閉じ込めたりするのではなく、

仮につなぐ
後から強さを変える
違うと思ったらほどく
つながりを見ながら配置を変える

ことができる。

本機能は、情報を正確に分類するためではなく、関係を試しながら考えるための機能とする。

1. 用語

用語	意味
糸	2つの吹き出しの間に設定された、方向を持たないつながり
つないでみる	新しい糸を作成する操作
つながりを見る	「つながり N」メニューから 1 階層先のつながりを表示する操作（§10.3）
糸の強さ	2つの吹き出しがどの程度近いと感じるかを示す3段階
糸をほどく	設定済みの糸を解除する操作
結び目	糸と吹き出しが接続する部分に表示する小さな装飾
接続点	吹き出し（Bubble）の輪郭上に設ける、糸の始点・終点
connection編集状態	糸の作成・編集操作中の内部状態（画面上に「つながりモード」とは表示しない）

ユーザー向けの主な文言
場面	文言
操作開始	つないでみる
相手選択	近そうな吹き出しを選んでね
強さ選択	糸の強さをえらぼう
保存	つなぐ
解除	糸をほどく
つながり一覧	つながり N（Nは接続数）

糸の作成は、カスタムモード内で吹き出しを選択した際に表示される操作のひとつとして扱う。独立した「つながりモード」という表示・名称は使用しない。

内部的にはconnection編集状態を持ってよいが、画面上に「つながりモード」とは表示しない。

内部の実装名はconnectionまたはrelationとしてよいが、ユーザーに表示する文言は、原則として「糸」「つなぐ」「ほどく」に統一する。

3.1 用語の区別（実装前提）

本仕様でいう「カスタムモード」は、スペース画面の `PresentationMode === 'custom'`（Bubble / `Tree.tsx` による吹き出し表示）を指す。

これは管理画面の `SpaceModeId: 'custom'`（スペースプリセット・投稿項目設定）とは **別概念** である。混同しないこと。


| 用語                          | 意味                                   |
| --------------------------- | ------------------------------------ |
| `PresentationMode 'custom'` | 星モード（StarView）ではなく Bubble を描画する表示モード |
| `SpaceModeId 'custom'`      | スペース設定のカスタムプリセット。糸機能とは無関係            |


1. 基本方針

4.1 方向を持たない関係

糸は、親子関係や因果関係を表す矢印ではない。

AからB、BからAという方向は持たず、2つの吹き出しが対等につながっている状態として扱う。

4.2 関係を確定しすぎない

糸は、正しい分類や確定済みの関係ではない。

「今のところ近そう」という関係を、あとから自由に変更・解除できるものとする。

4.3 必要なときだけ、関係が浮かび上がる

糸を常時すべて表示すると、投稿、吹き出し、操作メニュー、糸、結び目が重なり、空間が複雑になる。

通常時は糸そのものを原則として表示せず、吹き出しを選び、覗き、辿るときだけ関係を浮かび上がらせる。

4.4 可愛さと軽さを優先する

糸は硬い直線ではなく、少し曲がり、たるみ、伸びる表現とする。

本格的な物理演算は初期実装に含めず、軽量なSVGまたはCanvas表現を優先する。

1. 対象デバイスと有効条件

> **実装済み gate の正本: §B.1**

本機能の初期実装は、**Bubble が実際に描画されている画面** のみを対象とする。

画面幅だけで判定せず、次の **有効条件（**`connectionsEnabled`**）をすべて満たすとき** のみ糸 UI を表示・操作可能とする。

```
connectionsEnabled =
  !renderAsStar                    // PresentationMode === 'custom' かつモバイル常時星を除く
  && presentationMode === 'custom'
  && layoutMode !== 'byAuthor'    // 投稿者別まとめは MVP 対象外（§7）
  && viewMode !== 'slideshow'
```


| 条件                              | 理由                                 |
| ------------------------------- | ---------------------------------- |
| `presentationMode === 'custom'` | 星モード（StarView）では個別 Bubble が存在しない   |
| `!renderAsStar`                 | 狭幅モバイル縦など、幅に関わらず StarView 固定の環境を除外 |
| `layoutMode !== 'byAuthor'`     | クラスター表示では個別吹き出し DOM がない（§7）        |
| `viewMode !== 'slideshow'`      | スライドショー中は対象外                       |


初期対象

- PC 相当環境で Bubble が描画されていること
- `layoutMode === 'random'` または `'ordered'`
- マウスまたはトラックパッド操作

初期対象外

- スマートフォン（`renderAsStar === true` の環境）
- 星モード（`presentationMode === 'stars'`）
- `layoutMode === 'byAuthor'`（投稿者別まとめ表示）
- タッチ操作を前提とした糸編集
- 横持ち等、画面幅だけ広く Bubble が描画されない環境

`connectionsEnabled === false` のとき、糸の描画・操作メニュー・結び目印は **一切表示しない**（取得 API も呼ばない）。

1. UIの基本思想

本機能は、全体の関係図を一度に閲覧するためのものではない。

吹き出しを起点として、小さな星座をひとつずつ覗いていく体験を目指す。

何かがつながっていることに気づく
気になる吹き出しを選ぶ
その周囲のつながりだけを見る
必要に応じて、さらに先へ辿る

という段階的な体験を採用する。

1. 接続可能な範囲

MVPでは、以下の条件を **すべて** 満たす吹き出し同士のみ接続できる。

- `connectionsEnabled === true`（§5）
- `layoutMode === 'random'` または `'ordered'`（`'byAuthor'` は対象外）
- 同じスペースに属している
- 同じ Pane に属している
- 現在のカスタム空間に表示されている（`filteredHossiis` に含まれる）
- 削除されていない（`deleted_at IS NULL`）
- 非表示状態ではない（`is_hidden`）
- archived スペースではない
- 自分自身ではない

別 Pane 間のつながりは初期実装に含めない。

**Pane 切替時**（§10.2, §12, §32）: 現在 Pane に属する糸のみを取得・表示する。あわせて選択状態・connection 編集状態・仮糸・操作メニューを **すべて解除** する。

1. 糸の強さ

糸の強さは3段階とする。

数値ではなく、感覚的な名前と説明を表示する。

8.1 ほのか

表示文言:

なんとなく近いかも

見た目:

細い
半透明
たるみが大きい
必要に応じて部分的な点線
結び目は小さい

動き:

一方の吹き出しを移動しても、もう一方の吹き出しは基本的に動かない
糸だけが伸びて追従する
引っ張り操作中は、接続先はほとんど動かず淡く光る程度

内部値: soft

8.2 やわらか

表示文言:

関係がありそう

見た目:

標準的な太さ
なめらかな曲線
適度なたるみ
結び目が少し見える

動き:

一方を移動した際、もう一方が数pxだけ引かれるように揺れる
手を離すと元の位置へ戻る
引っ張り操作中は、接続先が数px揺れ、少しだけ引かれる

内部値: medium

8.3 しっかり

表示文言:

かなりつながっている

見た目:

やや太い
たるみが小さい
少し張りのある曲線
結び目が少し大きい
選択時に淡く発光する

動き:

一方を移動した際、もう一方も少し引かれる
完全には固定せず、独立した位置を保つ
引っ張り操作中は、接続先がやや明確に反応し、結び目と糸が少し強く光る

内部値: strong

1. 糸のビジュアル

9.1 基本表現

糸は、次の特徴を持つ。

完全な直線にはしない
吹き出し同士の距離に応じて緩やかにカーブする
少しだけたるみを持つ
半透明で、背景を大きく遮らない
接続部分に小さな結び目を表示する
選択時・hover時だけ少し明るくなる

イメージは、毛糸そのものよりも、光の糸と水中を漂う繊維の間とする。

9.2 結び目

糸の両端には、小さな結び目を設ける。

候補：

小さな丸
星の粒
ぷくっとした光
小さなリボン状の形

初期実装では、小さな丸または星粒程度のシンプルな表現とする。

結び目は通常時には控えめに表示し、次の場合に明確にする。

吹き出し選択時
糸hover時
つながり閲覧時
引っ張り操作中

9.3 色

MVPでは、ユーザーによる色選択は行わない。

スペースの背景やキャラクター Hossii の色に対して見える、淡い共通色を使用する。

ただし、糸の強さを色だけで区別しない。太さ、たるみ、透明度を組み合わせて表現する。

9.4 重なり順と SVG overlay 配置

表示の重なり順は、原則として以下とする。

```
背景（壁紙・StarLayer）
  ↓
SVG overlay（`bubbleArea` 内、`data-bubble-area`、`z-index: 0`）
  ↓
吹き出し（Bubble）— pin `z-index: 25`、like badge `22`
  ↓
既存編集 UI（リサイズハンドル、カラーパレット、本人操作バー、editToolbar）
  ↓
My Hossii（`MyHossiiLayer`、`z-index: 92–96`）— **`bubbleArea` 外**
  ↓
スペース Hossii（`HossiiLive`、`z-index: 100`）— **`bubbleArea` 外**
  ↓
コンパクト操作メニュー・pull ✦ ハンドル・強さ選択ポップオーバー（portal、`z-index: 320`）
  ↓
モーダル・ポップオーバー
```

**レイヤー上の注意**: 糸 overlay は **吹き出しの背面** に描画される。キャラクター Hossii（`HossiiLive` / My Hossii）は `bubbleArea` **外の別レイヤー** で、Bubble とは DOM 上も重なり順も独立する（§G.1）。

**SVG overlay の配置（MVP 確定）**

- 親: `bubbleArea`（`SpaceScreen` 内、`data-bubble-area` 付き div）
- スタイル: `position: absolute; inset: 0; z-index: 0; pointer-events: none`（糸 hit area のみ `pointer-events: stroke`）
- Bubble より **後ろ**、背景より **前**
- `ScaledContent` の `displayScale` zoom 配下に置き、座標追従を DOMRect ベースで行う

糸が本文や操作ボタンの上に表示されないようにする。

9.5 星座をつなぐような描画表現

糸の作成操作は、メニューやフォームで完結するだけではなく、吹き出し同士を星座の点と点のようにつないでいく感覚を重視する。

基本イメージ
最初の吹き出しを選択すると、接続点が小さく光る
相手を選ぶまで、接続点から仮の糸がポインターへ伸びる
別の吹き出しに近づくと、相手側の接続点が反応して光る
相手をクリックすると、糸が「ピッ」と結ばれる
結ばれた瞬間に小さな星粒が弾ける
操作は重くせず、軽く、短く、気持ちよく完了する

糸を引く動作は、本格的な線描画ではなく、点から点へピッピッと星座を結ぶ操作として設計する。

仮の糸

相手を選ぶ前の糸は仮表示とする。

少し薄い
点線または光の粒を含む
ポインターの動きに追従する
保存前であることが見た目で分かる
キャンセルすると消える

相手の吹き出しに近づいた際は、仮の糸が軽く吸い寄せられるように接続点へスナップしてよい。

9.6 接続点の表現

糸は吹き出し矩形の中心から直接伸ばさず、Bubble 矩形の辺上に接続点を設ける。

**MVP の接続点計算（確定）**

1. 各 Bubble の `getBoundingClientRect()` で DOMRect を取得
2. 2 点の **矩形中心** を結ぶ方向ベクトルを算出
3. 中心から相手方向へ伸ばした半直線と、Bubble 矩形の辺（padding 適用後）との **交点** を接続点とする
4. `bubbleArea` 基準の SVG 座標へ変換して描画

**padding（UI 回避）**

接続点が次の UI と重ならないよう、矩形に方向別 padding を設ける。


| 回避対象             | 目安             |
| ---------------- | -------------- |
| いいね badge（右下外側）  | 右・下方向に 12〜16px |
| 本人操作バー（選択時・直下）   | 下方向に 24〜32px   |
| リサイズハンドル・カラーパレット | 四隅から 8〜12px    |


padding 適用後の矩形で交点を求める。交点が取れない場合は、padding なし矩形の最近傍辺に fallback する。

吹き出しを移動した場合、接続位置を毎フレーム再計算する（`requestAnimationFrame` 推奨）。

9.7 糸を結ぶ瞬間

糸の作成時に、短いアニメーションを入れる。

推奨表現
接続点が一度だけぷくっと膨らむ
糸が端から端へ短時間で描かれる
小さな星粒が1〜3個だけ散る
糸が一度だけ軽く揺れる
0.2〜0.5秒程度で完了する

長い演出や、操作を待たせる演出は避ける。

糸をほどく瞬間
糸が中央または端からふっと消える
小さな光の粒にほどける
吹き出し自体は動かさない

1. 糸を常時見せない3段階の表示

10.1 通常状態

通常時は、糸そのものを原則として表示しない。

つながりを持つ吹き出しには、小さく控えめな印だけを表示する。

表示候補
小さな結び目
小さな星粒
ごく淡い光
接続数を示す小さな数字
結び目と接続数の組み合わせ

例：

✦ 3

この表示によって、

この吹き出しには、何かつながりがある

ことだけが分かる。

表示ルール
本文やリアクションを隠さない
常時アニメーションしない
接続数が0件の場合は表示しない
数字を表示する場合も、視線を奪わない大きさにする
同じ吹き出しに複数の印を重ねない

10.2 吹き出し選択状態

吹き出しを短くクリックすると、その吹き出しを **選択状態**（既存 `selectedBubbleId`）にする。

**既存選択 UX は置き換えない**（§11）。選択と同時に、従来どおり次を表示する。

- 4 角リサイズハンドル・カラーパレット（編集権限あり時）
- 本人操作バー（`canManageOwn` 時）
- 画面下部 `editToolbar`
- **新規** コンパクト操作メニュー（Bubble 近傍）

選択された吹き出しに直接つながる糸のみを、ふわっと表示する。

表示内容

- 選択した吹き出しにつながる糸
- 1 階層先の接続先の吹き出し
- 接続先の吹き出しの淡い発光
- 糸の強さに応じた形状
- 接続先ごとの小さな結び目

選択した吹き出しに関係しない糸は表示しない。

解除

以下の操作で **選択・connection 編集状態・仮糸・操作メニューをすべて解除** する。

- 背景をクリック
- Esc キー
- 別の吹き出しを選択
- カスタムモード（Bubble 表示）を終了
- **Pane を切り替える**

10.3 つながりを広げて見る状態

コンパクト操作メニューの **「つながり N」** を選択すると、対象吹き出しから 1 階層先のつながりを明示的に表示する（N は直接接続数。0 件のときは項目非表示または空状態）。

さらに、接続先の吹き出しを選ぶことで、その吹き出しを新しい起点として、次の 1 階層を表示できる。

基本ルール

- 一度に表示するのは、現在の起点から 1 階層先まで
- 接続先を選ぶと、起点が切り替わる
- 以前の起点の糸は薄く消える
- 関係を全件一括展開しない
- パンくずや複雑なツリーナビゲーションは初期実装に含めない

「広げる」というより、ひとつずつ辿る操作とする。

1. 吹き出し選択時の UI 構成



### 11.1 既存 UX（維持・置き換え禁止）

カスタムモードの Bubble 選択フローは **現行実装を維持** する。


| 操作                    | 挙動                            |
| --------------------- | ----------------------------- |
| 1 回目クリック（未選択）         | `selectedBubbleId` を設定（選択）    |
| 選択済み + 距離閾値超ドラッグ      | 位置移動（§12.1）                   |
| 選択済み                  | リサイズハンドル、カラーパレット（`canEdit` 時） |
| 選択済み + `canManageOwn` | 本人操作バー（編集・公開範囲・削除）            |
| 選択済み                  | 画面下部 `editToolbar`            |


投稿の編集・削除・配置変更は、従来の UI から行う。コンパクト操作メニューに「編集する」項目は **MVP では置かない**。

### 11.2 コンパクト操作メニュー（新規追加）

選択中の吹き出しの近傍に、**既存 chrome と併存** する小さなカード型メニューを追加する。

**MVP 表示項目（実装済み）**


| 項目          | 挙動                                                   |
| ----------- | ---------------------------------------------------- |
| ✦ pull ハンドル | **メニュー最上部**（`actionMenuOpen` 時のみ）。direct 接続 ≥1。§B.1  |
| くわしく見る      | `PostDetailModal` を開く（星クリックと同等の詳細閲覧）                 |
| つないでみる      | 糸作成フロー開始（§13）。**community admin / super admin のみ表示** |
| つながり N      | 直接接続一覧ポップオーバー（§14）。N=0 のとき非表示                        |


表示例（つながり 3 件あり・管理者・メニュー open）:

```
✦                    ← pull ハンドル（direct ≥1 のとき）
くわしく見る
つないでみる
つながり 3
```

表示例（つながりなし・一般参加者）:

```
くわしく見る
```

（「つないでみる」は権限なしのため非表示）

見た目

- ぷくっとした小さなカード、角丸、軽い影
- 選択中の吹き出しを隠しすぎない（portal または absolute 配置）
- 「つないでみる」は小さな糸または接続点アイコン付き
- `HossiiFullTextPopover` と同様、`createPortal` で zoom 外配置してよい

「つないでみる」は独立したモード切替ではなく、このメニュー内のひとつの操作とする。

1. ポインター操作の優先順位

カスタムモード内で、操作同士が競合しないよう、以下の優先順位を採用する。

12.1 通常状態


| 操作             | 挙動                                   |
| -------------- | ------------------------------------ |
| 短いクリック（未選択）    | 吹き出しを選択。既存 chrome + コンパクト操作メニューを表示   |
| ドラッグ（選択済み・閾値超） | 吹き出しを移動（配置保存）                        |
| 長押し            | 現行仕様を維持。Bubble 上には新操作を割り当てない         |
| ダブルクリック        | Bubble 上では使用しない（背景 dblclick はクイック投稿） |
| 背景クリック         | 選択・connection 状態・メニューを解除             |
| Esc            | 選択・connection 状態・メニューを解除             |


**クリックとドラッグの分離（Bubble 移動）**

- 現行の「未選択クリック＝選択のみ、選択済み pointerdown＝ドラッグ開始」フローは維持する
- 選択済み Bubble の移動開始に **5〜6px の距離閾値**（`DRAG_THRESHOLD_PX`、既存 `AuthorClusterBubble` / `SpacePaneBar` と同程度）を追加する
- 閾値未満の pointer up はクリック扱いとし、誤ドラッグを防ぐ
- いいね・Pin・owner bar・画像等の `stopPropagation` ルールは維持

12.2 糸作成中


| 操作          | 挙動                |
| ----------- | ----------------- |
| 別の吹き出しをクリック | 接続先として選択          |
| 接続元をクリック    | 選択を維持。新しい接続先にはしない |
| 背景クリック      | 糸作成をキャンセル         |
| Esc         | 糸作成をキャンセル         |
| 吹き出しのドラッグ   | 糸作成中は無効           |
| 糸をクリック      | 既存糸の編集を開始しない      |
| Pane 切替     | 仮糸・編集状態・メニューを破棄   |


糸作成中は、接続先選択を最優先とする。

12.3 つながり閲覧中


| 操作               | 挙動                 |
| ---------------- | ------------------ |
| 接続先の吹き出しをクリック    | その吹き出しを新しい起点として表示  |
| 糸を hover         | 対象の糸を強調            |
| 糸をクリック           | 糸の編集メニュー（管理者のみ）    |
| ✦ pull ハンドルをドラッグ | つながりを視覚的に引き出す（§16） |
| 背景クリック           | 閲覧状態を終了            |
| Esc              | 閲覧状態を終了            |
| Pane 切替          | 閲覧状態・仮糸・メニューを破棄    |


1. 操作フロー（作成・編集）

13.1 糸の作成

吹き出しを選択し、コンパクト操作メニューから「つないでみる」を選択する（管理者のみ）。

独立した「つながりモード」画面や、大きなモード切替表示は設けない。

13.2 相手を選ぶ

「つないでみる」を選択した後は、接続元の吹き出しから仮の糸を表示する。

案内文：

近そうな吹き出しを選んでね

ユーザーは、同じPane内の別の吹き出しをクリックして接続先を選ぶ。

選択時の見た目
接続元の吹き出し：淡い発光または輪郭
仮の糸：ポインターに追従
選択可能な吹き出し：近づいた時だけ小さく光る
選択不可の吹き出し：反応しない
選択完了時：小さな星粒または「ピッ」という視覚的反応

自分自身を接続先にはできない。

13.3 糸の強さを選ぶ

接続先を選んだ後に、糸の強さを選択するUIを表示する。

見出し：

糸の強さをえらぼう

選択肢：

ほのか
やわらか
しっかり

強さを変更すると、仮の糸の太さ、たるみ、透明度、張りがリアルタイムで変化する。

PC では、選択中の吹き出しの近くにコンパクトなポップオーバーを表示する（§26 参照）。

13.4 保存

保存ボタンは、画面幅いっぱいの大きなボタンにはしない。

選択肢の近くに、小さく分かりやすい確定ボタンを配置する。

文言例

つなぐ

または

この糸でつなぐ

後者を使用する場合も、ボタンサイズはコンパクトにする。

保存完了時には、糸が軽く光って正式な表示へ切り替わる（9.7 参照）。

13.5 キャンセル

作成途中で背景またはEscを押した場合、仮の糸を破棄する。

保存前の状態はDBへ登録しない。

13.6 連続してつなぐ操作

将来的には、ひとつの吹き出しから複数の吹き出しへ、星座のように連続して糸を引ける操作を検討する。

ただしMVPでは、誤操作を防ぐため、1回の操作で1本の糸を作成する。

保存後に再び「つないでみる」を選択することで、複数の糸を追加できる。

13.7 既存の糸の編集

糸をクリックすると、コンパクトな編集メニューを表示する。

強さを変える
糸をほどく

「強さを変える」を選ぶと、作成時と同じ3段階の選択UI（見出し：糸の強さをえらぼう）を表示する。

「糸をほどく」を選ぶと、確認を表示する。

この糸をほどきますか？

操作ボタン：

ほどく
やめる

糸をほどいても、接続されていた吹き出し自体は削除・移動しない。

1. 「この吹き出しの糸一覧」

吹き出し選択時に、接続数を含む操作を表示する。

つながり 3

クリックすると、対象の吹き出しに直接つながっている吹き出しの一覧を、コンパクトなポップオーバーで表示する。

表示例
つながっている吹き出し

やってみたい
やわらか

旅行に行きたい
ほのか

誰かの役に立ちたい
しっかり

各行に含むもの
接続先の投稿本文の冒頭
糸の強さ
必要に応じて小さな投稿者アイコン
接続先を示すhover状態

操作

一覧内の吹き出し（行）をクリックすると、

対象の吹き出しを淡く強調
対象までの糸を表示
必要に応じて表示領域内へスクロール
その吹き出しを新しい起点として選択可能

一覧を開いても、全糸は表示しない。

1. 糸の操作

15.1 hover

PCでは糸にhoverできる。

hover時は、

糸を少し太くする
透明度を上げる
両端の結び目を表示する
接続先の吹き出しを淡く光らせる

糸の見た目より広い、透明な当たり判定を設ける。

当たり判定
見た目：1〜4px程度
操作用の透明領域：10〜16px程度を目安
隣接する糸の選択を妨げない範囲で調整

15.2 click

糸をクリックすると、コンパクトな編集メニューを表示する。

強さを変える
糸をほどく

編集メニューは、糸の中央付近またはポインターの近くに表示する。

1. 引っ張ると、つながりが視覚的に釣れる



### 16.0 現行実装と当初 UX 案の区別（Production HEAD `a929fa4`）

**詳細: §B.1「pull-to-reveal」**


| 項目            | 当初 UX 案（§16.2 以降の記述） | **現行実装（事実）**                                                                  |
| ------------- | -------------------- | ----------------------------------------------------------------------------- |
| 操作起点          | 吹き出し上の **結び目** をドラッグ | `BubbleActionMenu` **内の ✦ ボタン**（`data-connection-pull-handle`）を `pointerdown` |
| 表示条件          | 選択時に結び目が見える          | **操作メニューが開いている** かつ `directPeerIds.length ≥ 1` かつ editor idle                 |
| 対象 DOM        | 結び目要素                | `bubbleArea` **内** `[data-hossii-id]` の source Bubble + direct peer Bubble    |
| 視覚効果          | CSS 変数 + transform   | `--pull-x/y`（source 最大 120px）、`--connected-shift-x/y`（peer 最大 36px）           |
| キャラクター Hossii | （案では言及なし）            | **未実装** — **§E.1 追加予定・未実装**                                                     |


以下 §16.1〜16.6 は **体験目標** を記述する。実装詳細は **§B.1**・上表を正本とする。

16.1 目的

✦ ハンドル（現行）または結び目（将来案）を引っ張ることで、

この吹き出しには何がつながっているんだろう？

という関係を、空間の中から視覚的に引き出せるようにする。

これは吹き出しや投稿の保存位置を変更する操作ではない。

関係を覗くための、一時的な視覚表現である。

16.2 操作（体験目標）

**現行**: コンパクト操作メニュー内の ✦ をドラッグする（§16.0）。

**将来案**: 吹き出し上の結び目をポインターでドラッグする。

ドラッグ中は、対象の吹き出しからつながる糸が引き出される。

見た目の流れ
✦ または結び目が少し膨らむ
糸がポインター方向へ伸びる
直接つながる吹き出しが少し揺れる
接続先の吹き出しが淡く浮かび上がる
その先にもつながりがある場合、小さな星粒が現れる
ポインターを離すと、すべて元の表示へ戻る

16.3 接続先の吹き出しの反応

接続先の吹き出しの **保存座標（**`positionX` **/** `positionY`**）および DOM の** `left` **/** `top` **は変更しない**。

ドラッグ中のみ、次の **一時的な視覚効果** を適用する。

- CSS カスタムプロパティ（現行: `--pull-x`, `--pull-y`, `--connected-shift-x`, `--connected-shift-y`）と `transform: translate(...)` **のみ**
- 数 px 程度の揺れ、淡い発光、軽い浮遊
- 糸の方向へ少し引かれる表現

`requestAnimationFrame` で SVG パスを更新してよいが、**React state の毎フレーム更新は避ける**。

pointer up またはキャンセル時に、transform と CSS 変数をクリアし、元の表示へ戻す。

**DB 更新・**`handlePositionSave`**・配置保存 API は呼び出さない。**

16.4 その先のつながり

直接接続された吹き出しが、さらに別の糸を持っている場合、その存在だけを小さな星粒で示す。

表現
接続先の吹き出しの輪郭付近に小さな星粒
接続数に応じて最大1〜3粒
2階層目の糸そのものは表示しない
星粒をクリックした場合の展開は初期実装に含めない

これにより、画面を複雑にせず、

この先にも何かある

ことだけを伝える。

16.5 糸の強さによる反応差
ほのか
糸が大きく伸びる
接続先はほとんど動かない
淡く光る程度
やわらか
糸が適度に伸びる
接続先が数px揺れる
少しだけ引かれる
しっかり
糸に張りがある
接続先がやや明確に反応する
結び目と糸が少し強く光る

強さによって動きの大きさを変えてよいが、投稿位置は保存しない。

16.6 キャンセル・終了

以下で引っ張り状態を終了する。

pointer up
pointer cancel
Esc
ウィンドウフォーカス喪失
カスタムモード終了
Pane切替

終了時は、糸、吹き出し、星粒を必ず元の表示へ戻す。

1. ごちゃつかせないための表示ルール

以下を必須ルールとする。

- 通常時は、すべての糸を常時表示しない
- 選択中の吹き出しに関係する糸だけを表示する
- 一度に表示するのは、原則として 1 階層先までとする
- 2 階層目は、糸ではなく星粒のみで示す
- コンパクト操作メニューは MVP で最大 3 項目
- 糸の強さは、色だけでなく、太さ・張り・たるみ・透明度で表現する
- 結び目と接続数は、小さく控えめにする
- 糸を表示しても、投稿本文や操作 UI を隠さない
- 糸作成中以外は、吹き出しの通常操作（選択・移動・いいね等）を優先する
- 保存済みの吹き出し配置を、糸によって自動変更しない
- 関係は「一覧で管理する」より、「覗く・辿る・引き出す」体験として扱う
- 全体のネットワーク図を初期画面に表示しない
- 同時に複数の吹き出しを選択状態にしない
- 選択対象以外の糸は非表示、または十分に薄くする
- 引っ張り中でも、2 階層目の糸は描画しない
- `connectionsEnabled === false` **の環境では糸 UI を一切出さない**

1. 吹き出し移動時の挙動

18.1 基本

吹き出しを移動すると、接続されている糸の始点・終点も追従する。

糸の描画位置は、移動中も更新する。

18.2 MVP

初期実装では、吹き出しの保存座標そのものを糸によって自動変更しない。

次の表現のみ実装対象とする。

糸が伸びる
糸のカーブが変わる
接続先の吹き出しが数px揺れる（引っ張り操作時のみ一時的）
手を離した時に糸がぷるんと戻る

18.3 将来拡張

将来的には、強さに応じて接続された吹き出しが少し引かれる物理表現を検討する。

ただし、ユーザーが保存した配置を勝手に変更しないことを優先する。

1. 権限

> **実装済み — 詳細 §B.1**

19.1 MVP（Phase 1）— **Production 反映済み**


| 操作                       | 対象                                                                                  |
| ------------------------ | ----------------------------------------------------------------------------------- |
| 糸の作成・編集・解除               | **community admin**（`AppUser.isAdmin`）または **super admin**（`AppUser.isSuperAdmin`）のみ |
| 糸の閲覧（✦N、選択時表示、一覧、引っ張り覗き） | スペースにアクセスできる全参加者                                                                    |
| archived スペース            | 作成・編集・解除不可（閲覧はスペースポリシーに従う）                                                          |


`bubbleEditPermission`**（吹き出し配置編集権限）とは連動させない。**

一般参加者が Bubble を動かせる設定（`'all'`）でも、糸の CRUD は管理者のみとする。

クライアント判定: `currentUser?.isAdmin ?? false`（super admin 含む）。  
RLS: `is_space_community_admin(space_id)` または super_admin JWT（`space_panes` と同型）。

19.2 将来拡張

将来的に、スペース設定で以下を選択できるようにする。

- 管理者のみ（MVP 既定）
- 投稿者は自分の投稿に関係する糸のみ操作可能
- 参加者全員
- 糸機能を使用しない

初期実装では権限設定 UI は追加しない。

1. データ仕様

> **DB migration Production 適用済み（PR #53）。API 実装済み — §B.1**

20.1 テーブル定義

現行 schema に合わせた定義とする。

```sql
create table public.hossii_connections (
  id                 uuid primary key default gen_random_uuid(),
  space_id           text not null references public.spaces(id) on delete cascade,
  pane_id            text not null references public.space_panes(id) on delete cascade,
  source_hossii_id   text not null references public.hossiis(id) on delete cascade,
  target_hossii_id   text not null references public.hossiis(id) on delete cascade,
  strength           text not null check (strength in ('soft', 'medium', 'strong')),
  created_by         uuid references auth.users(id) on delete set null,
  created_at         timestamptz not null default now(),
  updated_at         timestamptz not null default now(),
  constraint hossii_connections_no_self check (source_hossii_id <> target_hossii_id),
  constraint hossii_connections_pair_unique unique (
    space_id,
    pane_id,
    source_hossii_id,
    target_hossii_id
  )
);
```

**DB 側で担保する制約**


| 制約                | 実装                                                                                     |
| ----------------- | -------------------------------------------------------------------------------------- |
| 自己接続禁止            | `CHECK (source_hossii_id <> target_hossii_id)`                                         |
| A–B / B–A 重複禁止    | INSERT 前に ID を lexicographic sort して正規化 + `UNIQUE (space_id, pane_id, source, target)` |
| 同一 space          | `space_id` FK + trigger で両 hossii の `space_id` 一致を検証                                   |
| 同一 pane           | `pane_id` FK + trigger で両 hossii の `space_pane_id` が `pane_id` と一致を検証                  |
| archived write 禁止 | `BEFORE INSERT/UPDATE/DELETE` trigger で `space_is_archived(space_id)` を拒否              |
| cascade delete    | hossii / space / pane 削除時に FK `ON DELETE CASCADE`                                      |
| `created_by`      | trigger または RPC で `auth.uid()` を強制設定                                                   |


20.2 データモデル（TypeScript）

```typescript
type HossiiConnectionStrength = 'soft' | 'medium' | 'strong'

type HossiiConnection = {
  id: string
  spaceId: string
  paneId: string
  sourceHossiiId: string
  targetHossiiId: string
  strength: HossiiConnectionStrength
  createdBy: string | null  // uuid
  createdAt: string
  updatedAt: string
}
```

20.3 アプリケーション制約

- 同じ 2 投稿間に複数の糸を作成不可
- `source` と `target` の順序に意味を持たせない（正規化して保存）
- 接続先の吹き出しが削除・非表示の場合、取得結果から除外（FK cascade で DB からも削除）
- 異なるスペース / Pane の吹き出し同士はつなげない
- archived スペースでは作成・編集・解除不可
- 閲覧権限のない `hossiis` レコードを含む糸は取得・描画しない

20.4 重複防止

A–B と B–A を同じ組み合わせとして扱う。

```typescript
const [sourceId, targetId] = [firstId, secondId].sort()
```

DB の unique 制約と INSERT trigger の両方で担保する。

1. RLS・セキュリティ

- **SELECT**: `can_access_space(space_id)`。必要に応じ両端 hossii の visibility / deleted を JOIN 検証
- **INSERT / UPDATE / DELETE**: `is_space_community_admin(space_id)` OR super_admin JWT
- archived スペース: write trigger で拒否（`assert_space_not_archived_for_write` 相当）
- クライアント側の権限制御だけに依存しない
- 異なる space / pane の `hossiis.id` を送信しても登録できない（trigger 検証）
- `created_by` は `auth.uid()` から自動設定（クライアント値を信頼しない）
- service role 以外への不要な direct grant を避ける

詳細な RLS は、既存 `space_panes` の実装パターンに合わせて設計する。

1. フロントエンド状態管理

`HossiiStore`**（global reducer）には connections データも connection 編集状態も入れない。**


| データ / 状態                     | 置き場所                                                                                 |
| ---------------------------- | ------------------------------------------------------------------------------------ |
| `connections[]`              | `SpaceScreen` local state + `useHossiiConnections(spaceId, paneId)` hook             |
| `connectionCountsByHossiiId` | 上記から `useMemo` で derived                                                             |
| `selectedBubbleId`           | 既存 `SpaceScreen` state をそのまま利用                                                       |
| `connectionEditPhase`        | `SpaceScreen` local enum（`'idle' | 'pickingTarget' | 'pickingStrength' | 'viewing'`） |
| `draftConnection`            | local `{ sourceId, targetId?, strength? }`                                           |
| `hoveredConnectionId`        | local                                                                                |
| `draggingConnectionHandle`   | **ref 中心** + rAF（毎フレーム React setState 禁止）                                            |


URL / hash には一時編集状態を載せない。  
糸 fetch 失敗時もスペース全体を loading にしない（§32）。

1. UI文言

場面	文言
コンパクト操作メニュー	くわしく見る
操作メニュー	つないでみる
つながり一覧	つながり N
相手選択	近そうな吹き出しを選んでね
強さ選択見出し	糸の強さをえらぼう
弱い糸	ほのか
弱い糸の説明	なんとなく近いかも
中間の糸	やわらか
中間の糸の説明	関係がありそう
強い糸	しっかり
強い糸の説明	かなりつながっている
保存	つなぐ（または「この糸でつなぐ」。いずれもコンパクトなボタン）
編集	強さを変える
解除	糸をほどく
解除確認	この糸をほどきますか？
空状態	まだ糸はありません
空状態補足	気になる吹き出し同士を、つないでみよう
保存失敗	糸をつなげませんでした
解除失敗	糸をほどけませんでした

1. エラー時の挙動

24.1 保存失敗
仮の糸を正式表示へ切り替えない
強さ選択UI（ポップオーバー）を維持する
role="alert"でエラー表示
再試行可能にする

24.2 編集失敗
元の強さへ戻す
UIだけ変更された状態を残さない
エラーを表示する

24.3 解除失敗
糸を画面から消さない
元の状態を維持する
エラーを表示する

24.4 接続先が削除済み

取得データに存在しない `hossiis` レコードを含む糸は描画しない。

可能であればDBの外部キーとcascade deleteで自動削除する。

1. パフォーマンス

- 糸は表示中の吹き出しに関係するものだけ描画する
- 画面外・非表示 Pane の糸は描画しない
- 吹き出し移動中の描画更新は `requestAnimationFrame` で制御する
- React state を毎フレーム大量更新しない（§16, §22）
- 糸の本数が多い場合でも、投稿操作を妨げない
- SVG overlay は `bubbleArea` 内（§9.4）
- 大量表示で問題が出た場合は Canvas 化を検討する

1. PC版UI詳細

`connectionsEnabled === true` の環境で、選択中の吹き出しの近傍にコンパクト操作メニューと強さ選択ポップオーバーを表示する。

操作の流れ

1. 吹き出しをクリック → 既存選択 chrome + コンパクト操作メニュー（direct 接続ありなら ✦ 表示）
2. 「つないでみる」を選択（管理者のみ）
3. ポインターに仮の糸が追従
4. 接続先の吹き出しをクリック
5. 選択元または接続先の近くに強さ選択
6. 小さな「つなぐ」ボタンで確定



配置

- コンパクト操作メニュー: 選択 Bubble の近傍（portal 可）
- 強さ選択: 吹き出し直下 / 2 点中間 / 接続先近く
- 画面下部固定は不要（既存 `editToolbar` とは別）

画面外にはみ出す場合は、自動で上下左右を切り替える。

1. アクセシビリティ

- 色だけで糸の強さを区別しない
- 太さ・たるみ・透明度を併用する
- 糸と結び目に十分なクリック領域を設ける
- ボタンに明確なラベルを設定する
- エラーは `role="alert"` で通知する
- 動きを減らす設定が有効な場合、揺れやぷるんとしたアニメーションを抑える
- キーボード操作で、吹き出し選択・相手選択・保存・解除が可能な構成を目指す

1. MVPの対象範囲

> **§B.1 が Production 実装の正本。** 以下は受入観点との対応表。

28.1 MVPで実装する — **すべて Production 反映済み（PR #53〜#60）**

**有効条件**: `connectionsEnabled === true`（§5）、`layoutMode` は `random` / `ordered` のみ

表示

- 通常時は糸を非表示
- つながりがある吹き出しに小さな結び目または接続数（✦N）
- 吹き出し選択時、その吹き出しにつながる糸だけ表示
- 接続先の吹き出しを淡く強調
- コンパクト操作メニュー「つながり N」から直接接続一覧
- 1 階層先まで。2 階層目は星粒のみ
- SVG overlay（`bubbleArea` 内）

操作 UI

- 既存選択 UX 維持（§11.1）
- コンパクト操作メニュー: くわしく見る / つながり N / つないでみる
- Bubble 移動に 5〜6px ドラッグ閾値

作成（community admin / super admin のみ）

- 「つないでみる」→ 接続先選択 → 強さ選択 → 「つなぐ」
- 同一 Pane 内の `filteredHossiis` のみ

編集（管理者のみ）

- 糸 hover / click → 強さ変更 / ほどく

覗く操作

- **実装済み**: `BubbleActionMenu` 内 ✦ drag → CSS 変数 + transform（§B.1, §16.0）
- 120px / 36px clamp、position 非保存、reduced-motion 対応
- pointer up / Esc / blur / gate 解除で復帰
- **未実装**: 吹き出し上結び目の直接 drag

状態管理

- `SpaceScreen` local state / hook（§22）。`HossiiStore` 不使用

その他

- reload 後も維持
- Pane 切替で全状態解除
- エラー時 rollback

28.2 MVPでは実装しない

- スマートフォン / 星モード（`connectionsEnabled === false`）
- `layoutMode === 'byAuthor'`
- 「つながりモード」独立画面
- 全体星座表示 / 2 階層以上の糸同時描画
- 自動レイアウト / 保存位置を変える物理演算
- 参加者全員による編集（`bubbleEditPermission` 連動）
- 1 回の操作で複数糸連続作成
- 別 Pane 間のつながり
- 糸への文章入力 / 自由な色変更 / AI 提案
- 糸履歴 / 糸専用一覧画面
- **キャラクター Hossii の「つながりハンドル」**（§E.1 — MVP 外・**追加予定・未実装**）
- **既存吹き出しから「つなげて作る」**（§D.2 — リリース目標・**未実装**）

1. 将来拡張

29.1 関係の意味

糸の強さとは別に、関係の種類を設定できるようにする。

例：

似ている
続き
きっかけ
影響した
一緒に考えたい

ただし、分類を増やしすぎない。

29.2 まとまり

複数の吹き出しを、雲や島のようなひとつのまとまりとして囲めるようにする。

29.3 AIによる提案

本文、タグ、投稿時期などから、近そうな吹き出しを提案する。

AIが自動で糸を確定するのではなく、

この2つ、近いかも？

と提案する形式にする。

29.4 表示切替

将来的に、以下の切替を追加できる設計とする。

すべて表示
選択中だけ表示
強いつながりだけ表示
糸を隠す

29.5 ゆるい物理表現

強いつながりを持つ吹き出し同士が、少し近づきやすくなる表現を検討する。

保存済みの配置を勝手に変更しないことを優先する。

29.6 スマートフォン対応

星モードとは別途、タッチ操作に適した糸UIを検討する。

1. 受入条件

30.1 通常表示

- `connectionsEnabled === false` では糸 UI が一切出ない
- 糸が常時すべて表示されない
- つながりがある吹き出しだけに小さな印が表示される
- 印が本文や既存操作を妨げない
- 接続数が正しく表示される

30.2 選択表示

- 吹き出し選択時、既存 chrome（ハンドル・パレット・owner bar・editToolbar）が維持される
- コンパクト操作メニューが表示される
- 直接接続の糸だけが表示される
- 背景クリック / Esc / Pane 切替で選択・connection 状態・メニューがすべて解除される

30.3 つながり一覧

- 「つながり N」で直接接続一覧を開ける
- 一覧から接続先を選択できる

30.4 引っ張り操作（実装済み — §B.1）

- `BubbleActionMenu` **内 ✦** drag（Bubble 再クリックでメニュー open 後）
- direct 接続 0 件のとき ✦ 非表示
- source 最大 120px、peer shift 最大 36px
- pointer up / Esc / blur / gate 解除後に元へ戻る
- `left`**/**`top` **および DB 座標が変更されない**
- 配置保存 API が呼ばれない
- reduced-motion: peer shift 0、glow 上限、星粒 1

30.5 操作開始

- 管理者の選択時に「つないでみる」が表示される
- 一般参加者には「くわしく見る」「つながり N」のみ
- 「つながりモード」見出しは表示しない

30.6 接続操作

- 「つないでみる」→ 仮糸 → 強さ → 保存
- 同一 Pane / `filteredHossiis` 内のみ
- キャンセル時 DB 未登録

30.7 作成・編集・解除

- community admin / super admin のみ CRUD 可能
- reload 後も維持。重複不可

30.8 権限・制約

- `bubbleEditPermission` と独立（参加者が Bubble 編集可でも糸 CRUD は不可）
- 異 space / 異 Pane / archived / 自己接続は不可
- DB trigger + RLS で担保

30.9 エラー

- 保存 / 更新 / 解除失敗時に UI と DB の不整合が残らない

30.10 非回帰

- 1 回目クリックで選択、5〜6px 超ドラッグで移動
- リサイズ・カラーパレット・owner bar・editToolbar・いいね・Pin が壊れない
- `layoutMode === 'byAuthor'` では糸 UI 非表示
- guest 直リンク・Pane 切替・星モード非回帰

1. 実装Phase

→ **統合 Phase 表は §H。** 以下は B 内の旧記載。

**main HEAD** `12cab9a` **— §B.1（Production DB）+ §B.3（main 追加分）。詳細 §H.0。**


| Phase             | PR       | 状態                                                  |
| ----------------- | -------- | --------------------------------------------------- |
| 0 調査              | —        | ✅ §0                                                |
| 1 DB・API          | #53, #57 | ✅ Production migration 適用済み                         |
| 2a 描画基盤           | #54      | ✅                                                   |
| 2b 最小表示           | #54, #55 | ✅ ✦N / 1-hop / メニュー                                 |
| 2c ドラッグ閾値         | #55      | ✅                                                   |
| 3 作成・編集           | #55      | ✅ CRUD + rollback                                   |
| 4 pull-to-reveal  | #58, #60 | ✅ **ハンドル =** `BubbleActionMenu` **内 ✦**             |
| 5 仕上げ             | —        | 🚧 結び目 drag / 2-hop 展開 UI 未実装（§C） |
| 6 キャラ Hossii ハンドル | #84      | ✅ click Phase 1（§B.3）。drag 移設 📋 §E.1 |
| 7 つなげて作る          | —        | 🎯 **§D.2 リリース目標・未実装**                                  |


1. 実装時の注意

- 最初から物理演算を導入しない
- 糸の描画と DB 保存を分けて実装する
- 1 つの PR に DB・描画・編集をすべて含めない
- **既存選択 UX（§11.1）を置き換えない**
- `HossiiStore` **に connections を入れない**（§22）
- 糸 fetch 失敗でスペース全体を loading にしない
- 引っ張り操作で配置保存 API を呼び出さない
- **Pane 切替時に選択・connection 状態・仮糸・メニューを全解除**
- Production migration は UI 実装と別工程

1. 仕様の中心となる一文

吹き出し同士の、まだ言葉にならない「近さ」を、強さの異なる糸で仮につないでおける機能。

糸は、投稿を整理しきるためのものではない。
投稿を触り、動かし、つなぎ直す中で、まだ見えていなかった関係を発見するためのものである。

つながりは常に見せるのではなく、吹き出しを選び、糸を引き、ひとつずつ辿るときだけ、小さな星座として浮かび上がる。

---



## C. 現状 UI の課題

main `12cab9a` 時点の**残課題**。解消済み項目は ~~取り消し線~~ または ✅ へ移行。


| #   | 課題              | 事実                                                       |
| --- | --------------- | -------------------------------------------------------- |
| C1  | pull 入口         | ✦ は `BubbleActionMenu` **内**のみ。Bubble / キャラ Hossii 上にはない |
| C2  | メニュー前提          | `actionMenuOpen` 必須。押している間だけ糸が見える感覚に違和感の余地               |
| C3  | pin 等との近接       | メニュー・✦N・pin が吹き出し周辺に集中                                   |
| C4  | キャラ pull 未移設    | Space Hossii ✦ **click 入口 ✅**（§B.3）。drag → pull 移設は §E.1 Phase 2 📋 |
| ~~C5~~  | ~~Type A 権限~~       | ✅ **解消**（#65 RLS + #68 UI gate — §B.3）         |
| ~~C5b~~ | ~~auto-join race~~  | ✅ **解消**（#63 状態公開 + #67 retry + #68 UI gate — §B.3） |
| ~~C6~~  | ~~Type B~~          | ✅ **main 解消**（#85–#93 — §B.3）。Production RPC 未適用（§B.4） |
| ~~C7~~  | ~~reason / emoji~~  | ✅ **解消**（#64–#66, #69 — §B.1 Production DB 適用済み） |
| C8  | Star 星座線        | **未実装**（§E.4）                                            |
| C9  | 接続直後引力          | **未実装**（§E.2）                                            |
| C10 | connection 数サイズ | **未実装**（§E.3）                                            |
| C11 | 関係レイアウト         | **未実装**（§E.5）                                            |
| C12 | 2-hop 展開 UI   | pull 中 peer marker ✅。件数テキスト / 展開 UI は未実装              |
| C13 | mobile pull     | **mobile portrait / landscape とも pull 入口非表示**（意図的）      |
| C14 | reason 詳細表示   | create/edit UI + overlay tooltip ✅。**一覧・作成者・日時詳細は未実装**（§D.1） |
| C15 | editor partial success | strength 成功後 reason 失敗時の editor stale（§B.3 follow-up） |


---



## D. リリース時の目標仕様

> **Type A + reason**: ✅（§B.1 Production DB + main）。**Type B v1**: main ✅ / Production RPC 🎯（§B.4）。

connection 作成を **Type A** / **Type B** に分ける。

### D.1 Type A｜既存の吹き出し同士をつなぐ ✅

すでに存在する 2 つの吹き出しについて、利用者があとから関係を見いだして接続する。

#### 利用可能な人 ✅（main — §B.3）


| 利用者             | Type A                         |
| --------------- | ------------------------------ |
| guest           | **不可**                         |
| スペース内アカウント参加者   | **可能**（§F.1 — active `space_memberships` 正本） |
| personal space owner | **可能**（membership 行なしでも §F.1 例外） |
| community admin | **可能**（membership 不要） |
| super admin     | **可能**（membership 不要） |


**Production DB ✅（§B.1）**: #65 participant RLS + #68 UI gate と一致。

#### 起点・接続先（目標）

利用者に**見えている**吹き出しすべて（自分 / 他人 / 過去 / 他人同士のペアも可）。

条件:

- 同一 space / 同一 Pane
- 閲覧権限のある `hossiis` のみ
- hidden / deleted 不可
- archived: write 不可



#### connection 情報 ✅（main — §B.3）


| 項目 | 仕様 | main | Production DB |
|------|------|------|---------------|
| `strength`, `created_by`, timestamps | 既存 | ✅ | ✅ |
| `reason_text` | 任意、最大 **50 字**、空白のみ → NULL | ✅ | ✅ |
| `reason_emoji` | 任意、**8 プリセット 1 つのみ** | ✅ | ✅ |

プリセット: 💡 🔗 🌱 💬 ↔️ 🎯 ❤️ ❓（#64 CHECK と TS 型一致）

#### 糸の詳細表示

| 項目 | 状態 |
|------|------|
| create/edit UI（`ConnectionStrengthPopover`） | ✅ reason 入力・表示 |
| overlay 糸 hover tooltip（emoji + text） | ✅ #80 |
| 一覧 / 作成者 / 日時コンパクト詳細 | 🎯 未実装 |
| `updated_at` 表示要否 | ⚠️ §F.6 |

#### Type A UI gate ✅（main — #63, #67, #68）

**背景**: public shared space では auto-join と UI 表示が**並行**（§G.8）。

| 要件 | 内容 | main |
|------|------|------|
| 有効化タイミング | membership 確認完了後に create 有効 | ✅ |
| idle / joining | 「参加確認中…」、create **無効** | ✅ |
| auto-join 成功後 | `activeSpaceMembershipStatus → active` | ✅ |
| join 失敗 | create **無効**、「もう一度試す」 | ✅ |
| 再試行 | `retryActiveSpaceMembershipJoin`（error 時のみ） | ✅ |
| personal owner | membership 待たず bypass | ✅ |
| admin | community admin / super admin bypass | ✅ |

実装方式: **§F.14 案 2** — `HossiiStoreProvider` + `useActiveSpaceMembershipJoin`（#63, #67）。

---



### D.2 Type B v1｜既存吹き出しから、つながる新規投稿を作る ✅（main） / 🎯（Production RPC 未適用）

既存 1 吹き出し起点 → 新規吹き出し + 同時 connection。マインドマップ的展開。

#### 利用可能な人 ✅（v1 — main）


| 利用者                 | Type B v1 |
| ------------------- | ------ |
| guest               | **不可**（**v2 🎯** — §F.2） |
| active member       | **可能** |
| personal space owner | **可能**（bypass） |
| admin / super admin | **可能**（bypass） |


guest Type B は **v2** で別設計（§F.2）。

#### 起点 ✅

見えている吹き出し（`origin_hossii_id` = 選択中吹き出し）。元投稿は変更しない。

#### 体験フロー ✅（main — §B.3）

1. 起点吹き出しを選択
2. 「**つなげて作る**」
3. 起点から**仮糸**（破線 preview）
4. PostScreen（prompt: **「ここから、何が広がった？」**）
5. atomic RPC — 新規吹き出し + connection + 初期座標
6. **両成功時のみ完了**（idempotent replay も success 扱い）
7. 新規吹き出しを**起点近く**に表示・選択

Production RPC 未適用時: `RPC_NOT_AVAILABLE`（§B.4）。

#### ボタン名 ✅

**「つなげて作る」**（§F.3 確定）

#### 投稿フォームの問い ✅

**「ここから、何が広がった？」** — v1 固定文（§F.4 確定）

#### 初期配置 ✅

`computeTypeBNearOriginPlacement` — 右優先 45° 探索、衝突回避（#85）

#### atomic 作成 ✅

`create_type_b_connected_hossii` RPC（#88）。`origin_hossii_id` 列 + idempotency 表。

#### strength / reason（Type B v1）✅

| 項目 | v1 |
|------|-----|
| strength | **`medium` 固定**（RPC） |
| reason | **作成時なし**。事後 Type A edit で reason 追加可 |
| idempotency | `crypto.randomUUID()` at compose start |
| newHossiiId | `generateId()` at compose start |

#### mobile landscape ✅

custom Bubble 表示時 — Type A と同様 **有効**。pull / Space Hossii handle は **非表示**。

#### 削除 ✅（設計原則 — 変更なし）

- 吹き出し削除 → 接続 connection も削除（FK cascade ✅）
- 起点削除 → **派生新規投稿は残す**
- connection 解除 → 両投稿残す
- 新規削除 → 起点残す

---



### D.3 権限 ✅（main Type A） / 🎯（Type B・guest 拡張）



#### Production DB ✅（§B.1）


| 操作 | 一般参加者 | admin | guest |
|------|-----------|-------|-------|
| Type A INSERT | active member / personal owner | bypass | ❌ |
| 本人 UPDATE/DELETE | ✅ | — | ❌ |
| 他人 UPDATE/DELETE | ❌ | ✅ | ❌ |
| 糸閲覧・overlay | ✅（gate 内） | ✅ | ✅ |
| Type B create | — | — | ❌（RPC 未適用） |

RLS: #65 participant RLS + reason 列（#64）。

#### main ✅（§B.3 — UI gate 含む）


| 操作 | 一般参加者 | admin | guest |
|------|-----------|-------|-------|
| Type A INSERT | active member / personal owner | bypass | ❌ |
| 本人 UPDATE/DELETE | ✅ `createdBy === uid` | — | ❌ |
| 他人 UPDATE/DELETE | ❌ | ✅ | ❌ |
| 糸閲覧・一覧・pull | ✅（membership 非依存） | ✅ | ✅ |
| archived write | ❌ 全員 | ❌ | ❌ |

RLS（#65）:

- INSERT: `can_create_hossii_connection(space_id)`
- UPDATE/DELETE: `created_by = auth.uid() OR is_hossii_connection_admin(space_id)`

UI gate（#68）: RLS と**意味的に一致**（`typeAConnectionWriteGate.ts`）。

#### v2 🎯（未実装）


**guest Type B**: authenticated v1 とは別設計 — ⚠️ §F.2  
**guest 本人 connection 管理**: `created_by` モデル限界 — §G.6

---



### D.4 削除・変更の原則 ✅（main Type A）

§D.2 削除 + Type A 共通:

- 他人 connection: 一般参加者は解除不可 — ✅ #65 RLS + #68 UI
- admin: 全解除可 — ✅
- reason / emoji 変更権限 = connection 本体と同じ — ✅ #65 RLS + #69 UI（同一 edit gate）
- `createdBy === null`: 一般参加者は編集不可 — ✅

---



### D.5 データモデル ✅（reason 列） / 🔍（Type B 候補）

#### 実装済み（main + dev DB — #64）✅

- `reason_text` varchar(50) nullable — CHECK（50 字・改行拒否）+ 空白 NULL 正規化 trigger
- `reason_emoji` text nullable — 8 プリセット CHECK

migration: `20260723140000_add_hossii_connection_reason_columns.sql` — **Production DB 適用済み**

#### Type B — dev DB ✅ / Production ❌

| 列 / 表 | 内容 | dev DB | Production DB |
|---------|------|--------|---------------|
| `origin_hossii_id` | Type B 起点参照（#88） | ✅ | ❌ |
| `type_b_create_idempotency` | idempotency 表（RPC-only） | ✅ | ❌ |

#### 追加候補 🔍 ⚠️（v2 以降）

- `guest_created_by_token` 等 — §F.2
- `updated_by`, `metadata` jsonb — 要否 ⚠️ §F.7

---



## E. 追加予定仕様

> 📋 **追加予定・未実装** — §D リリース後〜拡張 UX



### E.1 Hossii キャラクターの「つながりハンドル」 — Phase 2 📋

**Phase 1 ✅（main — §B.3）**: **キャラクター Hossii**（Space Hossii）横 ✦ — click で 1-hop 糸強調 toggle（#84）。PC viewport のみ。

**Phase 2 📋（未実装）**: drag → pull-to-reveal 移設、My Hossii 搭載、メニュー ✦ 共存/廃止判断。


| 項目   | Phase 1 ✅ | Phase 2 📋 |
| ---- | ---------- | ---------- |
| 配置   | Space Hossii 横 ✦ | 右上 portal / sibling（§G.1 clip 対応） |
| クリック | 1-hop 糸強調 ON/OFF | （Phase 1 継続） |
| ドラッグ | なし | pull-to-reveal（120/36 clamp）へ移設の可能性 |
| 非表示  | mobile / Star / slideshow / byAuthor / Type B editor active | 同上 + 追加 reset |

**現行 ✅**: pull ✦ は `BubbleActionMenu` 内（PC のみ — §B.1）。

**論点 ⚠️**: drag 移設 → §F.10 / メニュー ✦ 共存 → §F.13

---



### E.2 connection 成立時の一時引力 📋

Type A 接続直後: 2 吹き出しが数 px〜20px **一時 transform** で引き寄せられ、終了後復帰。

- 保存座標変更なし / position save API 非呼出 / reduced-motion
- pull-to-reveal とは**別演出**

---



### E.3 connection 数による吹き出し表示倍率 📋


| connection 数 | 表示倍率候補            |
| ------------ | ----------------- |
| 0〜1          | 100%              |
| 2〜3          | 104%              |
| 4〜6          | 108%              |
| 7+           | 112%（上限 110〜115%） |


- DB 保存サイズは変更しない / selected scale・pull と競合しない設計 ⚠️
- visible のみ数えるか DB 全 readable か: ⚠️ §F.11

---



### E.4 Star モードの星座表示 📋

**原則**: 閲覧中心 / 作成・編集・解除なし / 星座線 click 不可 / 同一 Pane / Hossii ハンドル非表示

**通常時**: **strong のみ**、非常に薄い星座線、pointer-events none

**星選択時**: 選択星の **1-hop** をやや明るく（soft/medium/strong すべて対象）。2-hop 線は非表示

**上限候補 ⚠️**:

- 通常: Pane 全体 strong 線 **最大 30 本**
- 選択時: **その星から直接**の強調線 **最大 10 本**（Pane 全体ではない）

超過時優先: strong > medium > soft > 新しい > 画面内 — ⚠️ §F.12

**性能 🔍**: SVG vs Canvas、mobile 上限、virtualized 整合 — §F.12

---



### E.5 将来「関係から並べ直す」 📋

connection 構造から配置再計算（force-directed 等）。**自動保存禁止**。

必須: プレビュー / 確定 / キャンセル / snapshot / Undo / 元に戻す / 権限者のみ / Pane 単位 — §H Phase 6

---



## F. 未確定事項

> ⚠️ **結論を出さない**（選択肢の列挙のみ）。調査未完は 🔍 のまま。

| # | 論点 | ラベル | 主参照 |
|---|------|--------|--------|
| F.1 | Type A 参加者の正本 | ✅ | §B.3, §D.1 — **main 実装済み**（#65, #68） |
| F.2 | guest identity | ⚠️ | §D.3, §G.6 — **#1 調査済・現行方式の限界あり** |
| F.3 | Type B ボタン名 | ✅ | §D.2 — **「つなげて作る」**確定（#91） |
| F.4 | Type B フォームの問い | ✅ | §D.2 — **「ここから、何が広がった？」** v1 固定（#92） |
| F.5 | Type B 初期 strength | ✅ | §D.2 — **medium 固定**（RPC #88）。投稿者選択 UI は v2 |
| F.6 | reason 入力タイミング | ✅ / ⚠️ | §B.3 — **UI 実装済み**（#69）。詳細表示・grapheme 等は ⚠️ |
| F.7 | Type B 方向 metadata | ⚠️ / ✅ | §D.5 — `origin_hossii_id` ✅（#88 dev DB）。他 metadata ⚠️ |
| F.8 | Hossii ハンドル搭載先 | ⚠️ | §E.1, §G.1 — **#2 第一推奨: Space Hossii** |
| F.9 | Hossii ハンドルの root | ⚠️ | §E.1 — **#2 第一推奨: selectedBubbleId** |
| F.10 | click / drag 共存 | ⚠️ | §E.1 — **#2 調査: 両立可能** |
| F.11 | Bubble 倍率のカウント対象 | ⚠️ | §E.3 |
| F.12 | Star 表示上限 | ⚠️ | §E.4 |
| F.13 | Hossii ハンドル drag 移設 | ⚠️ | §E.1 Phase 2 — click Phase 1 ✅（#84） |
| F.14 | Type A UI gate — membership 状態取得方式 | ✅ | §B.3 — **案 2 採用・実装済み**（#63, #67, #68） |

### F.1 Type A — 参加者の正本 ✅

> **ユーザー確認済み採用仕様**（Quest #1 + auto-join 追加調査）。**main + Production DB 実装済み**（#65 RLS + #68 UI gate）。

Type A を作成できる一般参加者:

```text
対象 space に active な space_memberships を持つログインユーザー
```

**membership の有無にかかわらず作成可能な例外:**

- personal space の `owner_user_id = auth.uid()`
- 対象 community の **community admin**
- **super admin**

**Type A 不可:**

- guest
- space を**閲覧できるだけ**で membership を持たないログインユーザー（public shared space の閲覧のみユーザー等）

| 経緯 | 内容 |
|------|------|
| 旧 案 A | ログインユーザー全般 — **不採用** |
| 旧 案 B | community 参加者 + access — **不採用** |
| **採用** | **active `space_memberships` 正本** + personal owner / admin 例外（#1 案 C） |

**personal owner 補足（§G.4, §G.8）**: `ensure_my_personal_space` は `space_memberships` を**作らない**。owner に membership 行がないのは**正常**。Type A 権限判定で owner 例外が**必須**。

**auto-join race（§G.8）**: public shared space では join 完了前に SpaceScreen が表示され得る → **§D.1 UI gate 実装済み**（#68）。

→ RLS + UI gate とも **§F.1 採用仕様 1 モデルのみ**（§H.7）。UI gate 方式は **§F.14 案 2**（#63）。

### F.2 guest identity ⚠️

**Quest #1 調査済。** server token なし。詳細: **§G.6**。

| 論点 | 調査結果 |
|------|----------|
| 正本 | localStorage profile UUID（`author_id`） |
| 継続 | 同一端末 + localStorage 存続時のみ |
| 別端末 / 消失 | 別人扱い |
| アカウント化 | authorship **自動移行なし** |
| connection 本人管理 | 現行 `created_by = auth.uid()` では **guest 不可** |

Type B guest 本人編集を実現するには **別設計**（例: `guest_created_by_token` 等 §D.5 候補）が必要 — **方式未決定** ⚠️。

→ §H.2 ステップ 1「guest identity 方式決定」が Type B の前提。

### F.3 Type B — ボタン名 ✅

**確定: 「つなげて作る」**（#91）。他候補は v2 以降で再検討可。

### F.4 Type B — フォームの問い ✅

**v1 固定: 「ここから、何が広がった？」**（#92）。スペース設定で変更可能にするかは v2 ⚠️。

### F.5 Type B — 初期 strength ✅

**v1: RPC で `medium` 固定**（#88）。投稿者選択 UI / 3 段階 UI は v2 ⚠️。

### F.6 reason — 入力タイミング ✅ / ⚠️

| 対象 | 論点 | 状態 |
|------|------|------|
| Type A create/edit | strength popover 内折りたたみ、CTA 前 | ✅ #69 |
| Type A reason-only 編集 | strength 変更なしで reason のみ更新 | ✅ #66, #69 |
| text / emoji 個別 clear | partial update（`undefined` / `null` 区別） | ✅ #66 |
| 一覧・overlay 詳細表示 | emoji + text + 作成者 + 日時 | 🎯 未実装 |
| Type B v1 | 作成時 reason **なし**。事後 Type A edit | ✅ #92 |
| 共通 | space 設定で reason 必須化 | ⚠️ 未決定 |
| `updated_at` 表示 | 詳細 UI 要否 | ⚠️ 未決定 |

**実装（#69）**: `ConnectionStrengthPopover` — strength の下に折りたたみ。emoji プリセット 1 つ + `reason_text` 50 字。

**検証（#64, #66, dev script）**: null 可 / 50 字可 / 51 字拒否 / 空白 NULL / 改行拒否 / preset 外 emoji 拒否 — ✅。Unicode grapheme 数え方は ⚠️（`char_length` vs JS `.length`）。XSS 表示 escape — React デフォルト。

### F.7 Type B — 方向 metadata ⚠️

- 糸は方向なしのまま + `created_from_hossii_id` 等で方向保持（優先候補）
- `connection_type` enum の要否
- `metadata` jsonb の要否
- 最終スキーマは **§D.5 候補のみ**（migration 未作成）

### F.8 Hossii ハンドル — 搭載先 ⚠️

**Quest #2 調査済。** 確定しない。

| 候補 | 評価（#2） |
|------|------------|
| **Space Hossii** | **第一推奨** — 1 体、`bubbleArea` 外、入口として単純 |
| My Hossii | 投稿者 root 案 — **初期非推奨**（N 体・Pane 制約） |
| 両方 | 複雑 — 初期非推奨 |

`overflow: hidden` により外側右上は clip → sibling / portal 必須（§G.1）。Star モードでは**非表示**（§E.4）。

### F.9 Hossii ハンドル — root 吹き出し ⚠️

**Quest #2 調査済。** 確定しない。

| 候補 | 評価（#2） |
|------|------------|
| **`selectedBubbleId` を root** | **第一推奨** — 選択 Bubble の **1-hop のみ**表示。Hossii は入口のみ、糸は Bubble 間 |
| ハンドル押下後に root 選択 | **第二候補** |
| My Hossii → 投稿者の吹き出し群 | **初期非推奨** |
| Pane 全体の糸 | **初期非推奨**（常時フルネットワークに近い） |

`selectedBubbleId` なし → **吹き出し選択を促す**（第一推奨フロー）。`BubbleActionMenu` 内 ✦ は**当面共存**（#2）。

### F.10 click / drag 共存 ⚠️

**Quest #2 調査: 同一ハンドルで両立可能。** 確定しない。

| 操作 | 推奨挙動（#2） |
|------|----------------|
| move **5px 未満** | **click** — 糸表示 ON/OFF |
| move **5px 以上** | **pull** — 現行 120/36 clamp |
| 実装 | pointer capture、`HossiiLive.handleTap` へ**伝播させない** |
| その他 | click 誤発火抑制、reduced-motion は現行 pull 継承、Star 非表示 |

メニュー内 ✦ との**移設 vs 共存** — §H.3 ステップ 5 で判断。

### F.11 Bubble 倍率 — カウント対象 ⚠️

- visible な connection のみ（1-hop / 画面内）
- DB 上の全 readable connection
- archived / hidden 端点の扱い

### F.12 Star — 表示上限 ⚠️

| 論点 | 候補 |
|------|------|
| 通常時 strong 上限 | Pane 全体 **30 本**（§E.4） |
| 選択時 1-hop 上限 | **その星から直接** **10 本**（Pane 全体ではない） |
| 超過時優先 | strong > medium > soft > 新しい > 画面内 — **順位未確定** |
| 件数テキスト | 「15件中10件」等 — Star 世界観との両立 **未決定** |

性能（SVG vs Canvas、mobile 上限、virtualized 整合、低スペック自動非表示）は 🔍 — 実装 Quest で調査。

### F.13 Hossii ハンドルをリリースに含めるか ⚠️

**確定しない。** 比較案のみ:

| 案 | 分類 | 内容 |
|----|------|------|
| **A（原則）** | 📋 §E / Phase 3 | Type A・B・reason を先にリリース。ハンドルは §C の UX 改善として後追い |
| **B** | 🎯 §D に昇格 | pull 入口（§C1–C3）をリリース必須とし、Phase 1–2 と並行または直後に実装 |

判断材料: §F.8–F.10 の調査、§G.3 技術コスト、§C 課題の深刻度、リリース日程。

### F.14 Type A UI gate — membership 状態取得方式 ✅

**§D.1 UI gate — 案 2 採用・main 実装済み**（#63, #67, #68）。

#### 採用: 案 2 — `HossiiStoreProvider` で auto-join 状態を公開

| 状態 | 意味 | UI（#68） |
|------|------|-----------|
| `idle` | 未開始 / 対象外 | 「参加確認中…」（create 不可） |
| `joining` | `MembershipJoinController` 実行中 | 「参加確認中…」 |
| `active` | active membership 確認済 | 「つないでみる」 |
| `none` | membership なし | write UI 非表示 |
| `error` | join 失敗 | 「もう一度試す」 |

- `retryActiveSpaceMembershipJoin` — error 時のみ RPC 再実行（#67 dedupe）
- personal owner / admin — membership 状態に**依存せず** bypass（`typeAConnectionWriteGate.ts`）

#### 案 1（不採用）

`fetchMyMembershipForSpace` を SpaceScreen から都度取得 — auto-join 成功通知がなく refetch タイミングが推測になるため**不採用**。

---

## G. 実装前調査

> 🔍 実装前調査 + **Quest 5 / Type B 実装反映**。Production DB 正本: **§B.1**（PR #53–#60 + Quest 5 migration）。main 追加分: **§B.3**（HEAD `12cab9a`）。  
> **Quest #1（DB/API/RLS）・#2（UI）・#3（テスト）** の結果を Quest 4 で統合。Quest 5 で Type A / reason を main 実装。



### G.1 Hossii キャラクター


| 項目             | 事実                                                                                                              |
| -------------- | --------------------------------------------------------------------------------------------------------------- |
| Space Hossii   | `HossiiLive` 1 体 / space。`idleImageOverride` = `space.characterImageUrl`                                        |
| My Hossii      | `MyHossiiLayer` **→** `MyHossiiAvatar`。`fetchMyHossiiParticipants(spaceId)` の **N 体**                           |
| 投稿者紐づき         | My Hossii: `userId` **1 参加者 = 1 アバター**。Space Hossii: **紐づきなし**                                                  |
| 表示 gate（Space） | `controlState.hossiiVisible`（`showHossii`）。**custom / star 独立**                                                 |
| 表示 gate（My）    | `myHossiiEnabled` + **default Pane のみ** + `!isVisiting`                                                         |
| Star モード       | `renderAsStar = isMobile || presentationMode === 'stars'`。**HossiiLive は star でも表示され得る**（connection gate は off） |
| DOM            | `bubbleArea` **外**、`position: fixed`、`data-hossii-id` **なし**                                                    |
| サイズ            | PC 180×180 / SP 120×120                                                                                          |
| clip            | `overflow: hidden` — **外側右上ハンドルは container 内で clip される**（#2）                                              |
| タップ             | container 全体に `handleTap`。ハンドル操作時は **伝播抑制が必要**（§F.10）                                                    |
| ハンドル配置          | sibling または **portal** 配置が必要（container 内 absolute 右上は clip される）                                           |
| pointer-events | HossiiLive: **クリック可**（tap 反応・逃げ/寄り）。My layer: none、子 auto                                                       |
| z-index        | overlay 0 → pin 25 / badge 22 → My 92–96 → HossiiLive 100 → menu 320                                            |
| 右上ボタン          | Space Hossii ✦ connection handle **Phase 1 実装済み**（#84 — §B.3） |
| mobile portrait | Star 固定 → 糸 UI **無効** |
| mobile landscape custom | 吹き出し描画 → Type A / Type B **有効**。pull / Space Hossii handle **非表示** |




### G.2 吹き出し


| 項目                 | 事実                                                    |
| ------------------ | ----------------------------------------------------- |
| コンポーネント            | `Tree.tsx` `Bubble`、`data-hossii-id`                  |
| pin / like / owner | 右上 pin z25 / 右下 like / 選択時 owner bar                  |
| 選択                 | `selectedBubbleId`、再クリックで action menu                 |
| **pull ✦**         | `BubbleActionMenu` **portal 内**、menu open + direct ≥1 |
| 右上外ボタン             | pin・✦N と z-index / hit 競合の余地（§C）                      |




### G.3 糸機能・キャラハンドル技術（#2）

| 項目 | 事実 |
|------|------|
| overlay / pull DOM | `bubbleArea` **内** `[data-hossii-id]` **のみ** |
| Space / My Hossii | `bubbleArea` **外** — 現行 pull は Bubble DOM のみ対象 |
| pull CSS | Bubble `[data-connection-pull-source/peer]` |
| 座標系（推奨最小構成） | Hossii → Bubble へ糸は**描かない**。Hossii は**表示入口のみ** |
| click 後 | 現行 `ConnectionOverlay` を**流用** |
| drag 後 | 現行 Bubble transform + overlay 追従を**流用** |
| portal overlay 新設 | **原則不要**（#2 調査） |

キャラへハンドルを載せるには anchor 配線 + sibling/portal（§E.1、§G.1 clip 事実）。



### G.4 認証・membership（#1 調査結果）

#### 認証の事実

| 項目 | 事実 |
|------|------|
| ユーザー ID | `AppUser.uid` === Supabase `user.id` === `auth.uid()` |
| guest | `currentUser === null`（auth 上の guest） |
| `isAdmin` | **community admin** フラグ。space admin そのものではない |
| `canManageSpace` | super admin、または対象 community の **community admin**（Quest 5 以降 Type A admin bypass も同一関数） |

#### space access と membership の違い

| 概念 | 意味 |
|------|------|
| `can_access_space` | **閲覧可能性**（SELECT RLS 等） |
| `space_memberships` | **space 単位の正式参加者**正本 |
| shared public space | guest や membership なしログインユーザーも**閲覧可能** |
| invite_only space | **active な** `space_memberships` が必要 |
| public space | ログイン後に `join_space_as_member` が**非同期**実行される（詳細 §G.8） |
| personal space | `owner_user_id` に紐づく。**owner に membership 行がないのは正常**（§G.8） |

#### personal space owner（#1 追加調査）

| 項目 | 事実 |
|------|------|
| `ensure_my_personal_space` | `space_memberships` を**作らない** |
| owner 正本 | `owner_user_id = auth.uid()` |
| membership 行なし | **正常仕様** — Type A 判定で **owner 例外が必須**（§F.1） |
| auto-join RPC | personal space では**拒否**される |

→ Type A 参加者: **§F.1 採用仕様** — **main 実装済み**（#65, #68）。Production DB 未反映。

### G.8 public shared space auto-join（#1 追加調査）

`MembershipJoinController` の実行・UI 表示の**並行**に関する事実。

| 項目 | 事実 |
|------|------|
| 実行元 | `MembershipJoinController` — `HossiiStoreProvider` から**非同期** |
| SpaceScreen | join **完了を待たない**で表示される |
| 並行処理 | space 表示・投稿取得・connection 取得と membership 作成が**並行** |
| 成功後 | React state / Context へ **`activeSpaceMembershipStatus` 更新**（#63） |
| SpaceScreen | `activeSpaceMembershipStatus` を UI gate に利用（#68） |
| join 失敗 | `error` + `console.error` |
| 再試行 | `retryActiveSpaceMembershipJoin` — **error 時のみ**（#67） |
| 閲覧 | public shared space では membership なくても `can_access_space = true` |
| personal | auto-join RPC は**拒否**（§G.4 owner 例外） |

**race の意味**: join 完了前に Type A create UI が有効だと RLS 拒否または誤 UX → **§D.1 UI gate で解消**（#68）。

### G.5 connection 所有権・RLS ✅（main — #65）

#### DB / trigger の事実


| 項目 | 事実 |
|------|------|
| `created_by` | `uuid references auth.users(id) on delete set null` |
| INSERT trigger | `created_by := auth.uid()` を**強制** |
| UPDATE | trigger により **`created_by` 変更不可** |
| archived | write 拒否 trigger |
| reason | `reason_text` / `reason_emoji` も **connection 本体と同じ所有権**（#65 RLS + #69 UI） |
| guest | `auth.users` UUID を持たない → **`created_by` 方式では所有者にできない** |

#### 本人 UPDATE / DELETE RLS ✅（#65）

```sql
created_by = auth.uid()
OR is_hossii_connection_admin(space_id)
```

migration: `20260723153000_add_hossii_connection_type_a_participant_rls.sql`  
**Production DB: 適用済み**

### G.6 guest identity（#1 調査結果）

| 項目 | 事実 |
|------|------|
| guest 投稿 `author_id` | 端末 **localStorage** の profile UUID |
| server-side guest token | **存在しない** |
| reload / ブラウザ再起動 | localStorage が残れば**同一 guest として継続** |
| 別端末 | **継続不可** |
| localStorage 消失 | **別人扱い** |
| アカウント化後 | guest 投稿 authorship は**自動移行されない** |
| guest 本人 connection 管理 | 現行 `created_by`（`auth.uid()`）では**実現不可** |

→ Type B guest 本人編集・§F.2 の設計判断が必要（§H.2）。

### G.7 reason / Type B UI 資産（#2 調査結果）

| 項目 | 調査結果 |
|------|----------|
| reason 入力 UI | ✅ `ConnectionStrengthPopover` 内折りたたみ（#69） |
| reason API | ✅ `updateConnectionReason` partial update（#66） |
| Type B シェル | `PostScreen` typeBMode + mobile split ✅（#92） |
| メニュー | 「つなげて作る」✅（#91） |
| 初期配置 | `computeTypeBNearOriginPlacement` ✅（#85） |
| atomic | `create_type_b_connected_hossii` RPC ✅（#88 dev DB） |
| 仮糸 | `TypeBProvisionalThread` ✅（#92） |
| idempotency | `crypto.randomUUID()` at compose start（#93） |

---



## H. Phase 案

> **DB と UI を同一 PR に混ぜない。** Quest #1–#5 の依存順序。**Quest 5（Type A + reason）は main 完了** — §B.3 / §H.1。

### H.0 Production 完了 ✅ + Quest 5 main 実装 ✅

PR #53〜#60 = 本書 **§B.1**（Production 反映済み）。

| Phase | PR | Production | main |
|-------|-----|------------|------|
| 0 調査 | — | ✅ | ✅ |
| 1 DB・API | #53, #57 | ✅ | ✅ |
| 2 描画・UI | #54, #55 | ✅ | ✅ |
| 3 CRUD | #55 | ✅ admin-only | ✅ + Type A（#65, #68） |
| 4 pull-to-reveal | #58, #60 | ✅ | ✅ |
| 5 仕上げ残 | — | 🚧 §C | 🚧 §C |
| **Quest 5 Type A** | #63, #65, #67, #68 | ✅ DB | ✅ |
| **Quest 5 reason** | #64, #66, #69 | ✅ DB | ✅ |
| **connection UX** | #79–#89 | — | ✅ |
| **Type B v1** | #85–#93 | ❌ DB | ✅ |

**Quest #1–#3 調査**: ✅ — 本書 §G / §F / §H に統合。  
**Quest 5 実装**: ✅ main + Production DB。**Type B migration のみ Production 未適用**（§B.4）。

---

### H.1 Type A / reason — ✅ 完了

**Quest 5 完了**（main + Production DB migration 適用済み。frontend deploy 状態は未確認）。

| 順 | ステップ | 層 | PR | 状態 |
|----|----------|-----|-----|------|
| 1 | Type A 参加者定義確定 | 仕様 | — | ✅ §F.1 |
| 2 | membership UI 取得方式 | 判断 | #63 | ✅ 案 2 |
| 3 | auto-join 成功/失敗/retry | design | #67 | ✅ |
| 4 | personal owner 共通判定 | design | #65, #68 | ✅ |
| 5 | Type A INSERT RLS | DB | #65 | ✅ dev + Production DB |
| 6 | 本人 UPDATE/DELETE RLS | DB | #65 | ✅ dev + Production DB |
| 7 | Type A UI gate | UI | #68 | ✅ main |
| 8 | join 失敗 retry UI | UI | #67, #68 | ✅ main |
| 9 | DB 契約テスト | test | #65 | ✅ |
| 10 | UI / integration テスト | test | #68 等 | ✅ 49+ 件 |

**reason トラック:**

| 順 | ステップ | 層 | PR | 状態 |
|----|----------|-----|-----|------|
| R1 | reason データ仕様 | 仕様 | — | ✅ |
| R2 | reason 列 migration | DB | #64 | ✅ dev + Production DB |
| R3 | reason API 型 | API | #66 | ✅ main |
| R4 | reason create/edit UI | UI | #69 | ✅ main |

**Phase 1 完了条件（main）**: ✅ Type A CRUD + reason UI + UI gate 成立。  
**Production DB 公開条件**: ✅ 完了。**frontend deploy**: 未確認。

#### H.1.1 Type A テスト要件（#3）— ✅ 実装済み

dev verifier + unit/integration テスト（#65, #68）でカバー。UI gate / retry / edit ownership / read 非回帰を含む。

| ケース | 期待 | 状態 |
|--------|------|------|
| guest INSERT | **不可** | ✅ |
| membership なし INSERT | **不可** | ✅ |
| active member INSERT | **可** | ✅ |
| personal owner INSERT | **可** | ✅ |
| admin / super admin INSERT | **可** | ✅ |
| 本人 UPDATE/DELETE | **可** | ✅ |
| 他人 UPDATE/DELETE | **不可** | ✅ |
| archived write | **不可** | ✅ |
| cross-space 等 | **拒否** | ✅ |
| UI gate joining/error | **無効** + retry | ✅ |
| personal owner gate | membership 待たない | ✅ |

#### H.1.2 reason テスト要件（#3）— ✅ 実装済み

| ケース | 状態 |
|--------|------|
| null 可 | ✅ |
| 50 字可 / 51 字拒否 | ✅ |
| 空白 NULL / 改行拒否 | ✅ |
| preset 外 emoji 拒否 | ✅ |
| partial update（undefined/null 区別） | ✅ #66 |
| Unicode grapheme | ⚠️ 未確定 |
| XSS escape | ✅ React デフォルト |
| DB 最終防衛線 | ✅ CHECK + trigger |

---

### H.2 Type B v1 — ✅ main 完了 / 🎯 Production 未公開

**main 実装 ✅（#85–#93）**

| 順 | ステップ | 層 | PR | 状態 |
|----|----------|-----|-----|------|
| 1 | near-origin placement util | util | #85 | ✅ |
| 2 | atomic RPC migration + dev verifier | DB | #88 | ✅ dev DB / ❌ Production |
| 3 | typed client API | API | #90 | ✅ |
| 4 | write gate + menu entry | UI | #91 | ✅ |
| 5 | compose + 仮糸 + PostScreen | UI | #92 | ✅ |
| 6 | idempotency UUID 統一 | fix | #93 | ✅ |
| 7 | guest Type B | — | — | 📋 v2（§F.2） |

**Production 公開 — 残タスク（§B.4）**

1. Production DB: `20260724120000`
2. frontend deploy
3. smoke QA

**Phase 2 完了条件（main）**: ✅ atomic post + connection + position。idempotent replay 対応。

#### H.2.1 Type B テスト要件 — ✅ dev 実装済み

- `scripts/verify-type-b-atomic-create.mjs` — atomic / idempotency / rollback
- unit / integration — `useTypeBEditor` / `useSpaceTypeBIntegration`
- idempotency: `idempotencyKey = crypto.randomUUID()`、`newHossiiId = generateId()` — compose start 固定、retry 維持

---

### H.3 追加予定 📋 — Hossii ハンドル Phase 2

> click Phase 1 ✅（#84 — §B.3）。drag 移設以降。

| 順 | ステップ | 層 | PR | 状態 |
|----|----------|-----|-----|------|
| 1 | click 入口（Space Hossii ✦） | UI | #84 | ✅ |
| 2 | drag → pull 移設 | UI | — | 📋 |
| 3 | `BubbleActionMenu` 内 ✦ 共存/廃止 | ⚠️ UX | — | 📋 |
| 4 | integration / regression | test | — | 📋 |

#### H.3.1 Hossii ハンドル テスト要件（#3）

- click / drag **threshold**（5px 境界 §F.10）
- Hossii `handleTap` **排他**
- pointer cancel / blur / Esc
- **Star 非表示**
- Pane 切替 **reset**
- reduced-motion
- `selectedBubbleId` **なし**
- connection **0 件**

**DB 変更なし**（§G.3）。

---

### H.4 追加予定 📋 — 演出・倍率

| ID | タスク | 層 | PR | 依存 |
|----|--------|-----|-----|------|
| T4.1 | connection 成立時の一時引力 | UI | 単独 PR | Type A 後 |
| T4.2 | connection 数 Bubble 倍率 | UI | T4.1 と**別 PR** | §F.11 |

---

### H.5 追加予定 📋 — Star 星座線

**依存順序:**

| 順 | ステップ | 層 | PR | 依存 |
|----|----------|-----|-----|------|
| 1 | 表示ルール・**上限確定**（§F.12） | ⚠️ 判断 | — | — |
| 2 | **pure filtering function** + unit test | util + test | 描画と**別 PR** | 1 |
| 3 | 描画基盤（SVG / Canvas） | UI | 2 と**別 PR** | 2 |
| 4 | 選択 1-hop 強調 | UI | 3 と**別 PR** 可 | 3 |
| 5 | performance / mobile 検証 | test | — | 3, 4 |

#### H.5.1 Star テスト要件（#3）

- **pure filter function を先にテスト**
- 通常時 **strong のみ**
- 選択時 1-hop **全 strength**
- 2-hop **非表示**
- `pointer-events: none`
- 上限値は **§F.12 確定後**

---

### H.6 追加予定 📋 — 関係から並べ直す

| ID | タスク | 層 | PR | 依存 |
|----|--------|-----|-----|------|
| T6.1 | preview / snapshot / Undo / 確定 UI | UI + algo | 複数 PR | Phase 1–2 安定後 |
| T6.2 | force-directed layout 試作 | 🔍 algo | T6.1 と**別 PR** | — |

---

### H.7 PR 分割・migration ルール（#3 反映）— Quest 5 で実証 ✅

1. **migration PR ≠ UI PR** — ✅ #65 ≠ #68、#64 ≠ #69
2. **RLS INSERT ≠ UPDATE/DELETE** — ✅ #65 に両方（別 PR 可としていたが同一 PR で実施）
3. **reason 列 ≠ Type A RLS** — ✅ #64 / #65 分割
4. **DB 契約テスト = migration と同 PR** — ✅ verifier 同梱
5. **§B.1 admin RLS を壊さない** — ✅ 新ポリシーで**置換**（Production 適用済み）

**Quest 5 で採用した順序**: membership 状態（#63）→ Type A RLS（#65）→ reason API（#66）→ retry guard（#67）→ UI gate（#68）→ reason UI（#69）。

---

### H.8 依存・リスク概要

| リスク | 影響 | 緩和 | Quest 5 |
|--------|------|------|---------|
| ~~§F.14 UI gate 未決定~~ | ~~H.1 step 7 ブロック~~ | 案 2 採用 | ✅ #63, #68 |
| ~~auto-join race~~ | ~~誤 UX~~ | UI gate + 状態公開 | ✅ |
| Type B Production migration 未適用 | Production で Type B RPC 不可 | §B.4 手順 | 🎯 残 |
| Production frontend deploy 未確認 | main コードと Production UI の差 | deploy 後に §B.1 更新 | ⚠️ |
| editor partial success stale | retry 時 strength 再送 | §B.3 follow-up | ⚠️ 改善余地 |
| §F.2 guest Type B v2 | guest identity 未設計 | v2 で別途 | 📋 |
| §F.7 追加 metadata | analytics 等 | v2 以降 | ⚠️ |
| Star 上限未確定 | H.5 ステップ 1 | §F.12 確定 Quest | 🎯 残 |