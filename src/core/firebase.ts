// Mock Firebase - Firebase SDK is disabled for demo mode
// This file is kept for compatibility but doesn't use real Firebase

// All Firebase functionality is now mocked in AuthContext
// No actual Firebase initialization occurs

// Dummy exports to prevent import errors
export const app = null;
export const auth = null;
